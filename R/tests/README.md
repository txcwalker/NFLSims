# R/tests/ — Test Harnesses

> [!NOTE]
> `auto_test_fourth_downs.R` and `test_reviewer_ui.R` have been moved to `legacy/R/tests/`.
> These were earlier-generation test approaches.
>
> This directory is reserved for future structured test coverage as the project
> moves toward production quality. Current testing is handled through the
> Python test suite in the root `tests/` directory.
