# R/reports/ — Reports

> [!NOTE]
> `run_fd_backfill.R` has been removed — it wrote to `data/fd_backfill/` which
> no longer exists. Backfill debugging is now handled through the main bot pipeline.
>
> This directory is reserved for any future structured reporting scripts.
