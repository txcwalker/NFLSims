# AGENTS.md — NFLSims AI Handoff Contract
<!-- Token-efficient AI-to-AI handoff. Updated: 2026-09-04 (all 32 teams reviewed + applied; apply_team_season_overrides now syncs traits.json's player SET, not just values, to the sheets -- fixed a real deleted/added-player gap) -->

---

## 0. Current Priorities (active as of last session)

- **2026-09-04 — season-long vs. DFS-weekly split into two separate roster JSON trees (Cam's design call).** `NFLGameEngine`/`BatchSimulator` gained an additive `rosters_dir="data/current_rosters"` kwarg (default = exact prior behavior, zero risk to existing callers) so a sim can point at a parallel roster tree. New [`apply_team_week_overrides_v_0_1_0.py <week>`](scripts/roster_management/apply_team_week_overrides_v_0_1_0.py) compiles `data/overrides/2026/week_NN/{TEAM}.csv` → `current_rosters/dfs/{TEAM}_traits_2026.json` (fresh copy of the CURRENT season-long JSON as the base, then that week's fields overlaid) — separate from season-long's `current_rosters/{TEAM}_traits_2026.json`. Only the current week's compiled JSON is kept (no per-week history — the CSVs are the record). Shared create/zero/overlay logic factored into [`apply_sheet_helpers_v_0_1_0.py`](scripts/roster_management/apply_sheet_helpers_v_0_1_0.py) (`apply_sheet_to_traits`). **Now built:** [`scripts/simulation_runners/run_week_sim_2026.py <week>`](scripts/simulation_runners/run_week_sim_2026.py) sims a week off `rosters_dir="data/current_rosters/dfs"` → `data/interim/dfs_week_{N}_{games,players}.parquet` (overwritten each run, no history). `src/api/app.py`'s `get_week_projections()` prefers that file over the season-long slice when present (`_get_dfs_week_players()`). **Only that one endpoint is DFS-roster-aware** — `/api/week_sim_results` / `/api/optimize` still go through a separate `get_rosters()`/`run_simulation()` path that hasn't been investigated; the DFS site is not fully wired end to end yet. Josh Jacobs' 0/0 season-long usage confirmed intentional by Cam (treating him as done for the season) — not a bug. Full detail: [WORKLOG.md](WORKLOG.md) 2026-09-04 entry.
- **2026-09-04 — full 32-team review applied; `apply_team_season_overrides` now syncs traits.json's player SET to the sheets (real bug fixed).** All 32 `data/overrides/2026/season_long/{TEAM}.csv` reviewed by Cam and applied for real for the first time. Confirmed **`game_engine.py`/`batch.py` load rosters exclusively from `current_rosters/{TEAM}_traits_2026.json` — never any CSV** — so "the sim reads the sheets" only happens via a successful `apply_team_season_overrides` run. That first real run exposed a genuine gap: `apply_preseason_overrides`/`apply_zone_usage_overrides` only ever *update* an existing traits.json entry, never insert/delete one, so **32 players removed during the review still held their old nonzero shares** (invisible removals) and **8 players given a real hand-assigned role (GB Kaleb Johnson 27% carry, HOU Kayshon Boutte 11% target, CAR Darren Waller, etc.) didn't exist in traits.json at all** (invisible additions — "player not found" every run). Fixed with a new `sync_roster_membership()` step (zero-out deleted players, `enrich_player`-create new ones, re-sort by position) — see the 2026-09-03 entry below for the sheet mechanics. Verified 0 orphans / 0 missing after the fix; `validate_rosters.py 2026` 32/32 PASSED. **`current_rosters/*_traits_2026.json` is gitignored — no git safety net; `data/overrides/2026/` (the real source of truth) is untracked too and should probably get committed.** Full detail: [WORKLOG.md](WORKLOG.md) 2026-09-04 entry.
- **2026-09-03 — roster source switched to nfl.com; red-zone / goal-line share columns added to the sheets.**
  - New [`scripts/roster_management/scrape_nfl_rosters_v_0_1_0.py`](scripts/roster_management/scrape_nfl_rosters_v_0_1_0.py) — stdlib scrape of all 32 `nfl.com/teams/{slug}/roster` pages → `data/overrides/2026/_nfl_com_roster.csv` (cached; run it to refresh). **Far more accurate than nfl_data_py's 2026 feed:** exactly 53 `ACT`/team, correct `EXE` (Jacobs), `SUS` (Jeshaun Jones, Rechsteiner), `RSR`/`RES` (IR), `PUP`, `DEV`. `roster_feed_v_0_1_0` now reads this cache (`NFL_STATUS_TO_SLOT`); nfl_data_py is consulted only, best-effort, for a `player_id` bridge. `_strip_tags` runs `html.unescape` (nfl.com uses `&#x27;` etc.). Name matching: exact `match_key`, then a skill-position-only `(team, lastname, first-initial)` fuzzy fallback (Kenny↔Kenneth) — export flags every fuzzy match + every projected-role player with no nfl.com match for review.
  - **Sheet schema grew to 26 cols:** added `rz_target_share, rz_carry_share, gl_target_share, gl_carry_share` (red-zone 6–20 / goal-line ≤5), seeded from `zone_usage_overrides_2026.csv` (`rz_*`/`five_*`). `resolve_week_rows` redistributes all six shares. `apply_team_season_overrides` now also regenerates `zone_usage_overrides_2026.csv`'s 4 tunable columns (preserving its `hist_*`/`median_*` research columns) and runs `apply_zone_usage_overrides(2026)` → `traits.splits.redzone/goalline`.
  - Ran `scrape` + `export` + `build_week_overrides 1` + `build_roster_md`. **Did NOT run `apply_team_season_overrides`** — Cam should resolve the review flags first (18 role-cuts to reassign, ~20 no-match players, 4 fuzzy matches). Full list: [WORKLOG.md](WORKLOG.md) 2026-09-03 entry. Suite: 92 passed.
  - **`Totals` row support:** Cam adds a trailing `player_name=Totals` row (blank pos) to sheets as a running column-sum check. `is_player_row()` in `week_roster_v_0_1_0.py` gates it — `apply_team_season_overrides` / `build_roster_md` skip it, `resolve_week_rows` passes it through + recomputes it (`recompute_totals_rows`), `export_team_season_overrides` re-adds a computed one on reseed. The sim renormalises shares per position (`game_engine.py:479-497`, `shares /= total_shares`) and drops backup-QB carry shares from the rush pool, so totals >1 are cosmetic only.
  - **Cam's first-4-teams review applied:** ARI/BUF no change; ATL removed Trevor Siemian (not in NFL) + Nate Carter (cut) + Casey Washington (→CAR, already there as PS); BAL removed the 3 `cut` rows (Cornelius Johnson / Dayton Wade / Lucas Scott). All were 0-share, no redistribution.
- **2026-09-02 session — per-team 2026 override sheets replace `preseason_overrides_2026.csv` as the edit surface; readable roster markdown; per-week injury resolver.** The 2026 season's hand-tune layer is now **32 per-team CSVs**, Excel-editable, in `data/overrides/2026/`:
  - `season_long/{TEAM}.csv` — the season-long projection. Columns: `player_name, team, pos, player_id, roster_slot, return_week, note` + 15 flat tunable fields + 4 zone-share fields (see 2026-09-03 above). `roster_slot` ∈ {active, practice_squad, ir, pup, nfi, suspended, exempt, cut}. Seeded (`export_team_season_overrides_v_0_1_0.py`) from `preseason_overrides_2026.csv` + `zone_usage_overrides_2026.csv` + `_nfl_com_roster.csv`: zero-role players nfl.com dropped are removed, **role players nfl.com dropped kept as `roster_slot=cut` with a "reassign+delete" note** (never silently drop a real projected role), nfl.com players new to a team added at 0/0. Every retained player's hand-tuned values are preserved byte-for-byte.
  - `week_NN/{TEAM}.csv` — one week's expectation, `build_week_overrides_v_0_1_0.py <week>`: reserve players whose `return_week` hasn't arrived (and `cut` players) zeroed, share redistributed pro-rata to `active` same-position players (equal-split fallback, team totals preserved, `!! LOST share` if a position empties). Once real games are in, the weekly DNA refresh overwrites these with the rolling L4 average.
  - `apply_team_season_overrides_v_0_1_0.py` — concat the 32 sheets → regenerate `data/dna/preseason_overrides_2026.csv` → run existing `apply_preseason_overrides` → `current_rosters/{TEAM}_traits_2026.json`. The downstream pipeline is unchanged. **Loop: edit sheet → apply_team_season_overrides → build_roster_md.**
  - `docs/rosters/2026/{TEAM}.md` — readable roster (`build_roster_md_v_0_1_0.py`): active by position, Injured/Reserve w/ est. return week, practice squad, new-to-team.
  - Shared feed helpers: [`scripts/roster_management/roster_feed_v_0_1_0.py`](scripts/roster_management/roster_feed_v_0_1_0.py) — `build_feed_index` (reads `_nfl_com_roster.csv`), `resolve_status`, `clean_name`, `match_key`, `NFL_STATUS_TO_SLOT`, `RETURN_WEEK_SEED`.
  - Per-week logic: [`src/data_pipeline/week_roster_v_0_1_0.py`](src/data_pipeline/week_roster_v_0_1_0.py) (flat schema) — `parse_return_week`, `is_available(row, week)`, `resolve_week_rows(rows, week)` (redistributes all 6 share fields). Tests: [`tests/test_week_roster.py`](tests/test_week_roster.py) (14).
  - **Encoding:** `preseason_overrides_2026.csv` / `zone_usage_overrides_2026.csv` are latin-1 ("Audric Estimé"); the sheet scripts read tolerantly (`read_rows`: utf-8-sig → latin-1) so Excel "CSV" or "CSV UTF-8" both round-trip. `.md` files ASCII-only.
  - **Not yet wired into any season-sim driver.** Full design + workflow: [`data/overrides/2026/README.md`](data/overrides/2026/README.md).
  - **Superseded false starts (retired, were untracked):** a per-team JSON layer (`build_2026_week1_files_v_0_1_0.py` + 96 `{TEAM}_roster_final/full/week1_usage_2026.json`), and league-wide `data/dna/injury_overrides_2026.csv` / `week1_usage_overrides_2026.csv` + their export wrappers.
- **2026-08-13 session — first concrete step on the red-zone/inside-the-5 usage gap (flagged 2026-08-05, below): `data/dna/zone_usage_overrides_2026.csv` + `..._notes.md` built.** New [`scripts/roster_management/export_zone_usage_overrides_v_0_1_0.py`](scripts/roster_management/export_zone_usage_overrides_v_0_1_0.py) pulls real 2024+2025 PBP, splits by zone (goalline `yardline_100<=5`, redzone 6-20 — mutually exclusive of goalline, not the traditional inclusive "inside the 20" stat), and produces a `preseason_overrides_2026.csv`-style hand-editable sheet: `rz_target_share`/`rz_carry_share`/`five_target_share`/`five_carry_share` (2026 placeholders, currently just copies of the flat share — not yet hand-tuned) plus `hist_2024_*`/`hist_2025_*` real usage columns for reference. Usage only (target_share/carry_share) — efficiency-field zone splits (catch_rate, adot, etc.) and any actual game-engine wiring are still open, undecided work. Full detail: [WORKLOG.md](WORKLOG.md) 2026-08-13 entry.
- **2026-08-05 session — `preseason_overrides_2026.csv` hand-editing pass finished for all 32 teams; `coach_dna.json`'s 2026-first-time-HC gap closed for a baseline run. Full detail: [WORKLOG.md](WORKLOG.md) 2026-08-05 entry.**
  - **`data/dna/preseason_overrides_2026_notes.md`** now covers all 32 teams (ARI→WAS) plus a general/cross-team section — the qualitative reasoning behind every hand-tuned `preseason_overrides_2026.csv` value (why a share/efficiency field was moved off the mechanically-rebuilt career baseline). Read it before assuming a CSV value is wrong or a typo. Cam confirmed the CSV itself is now fully updated to match — **known, accepted gaps:** several just-drafted/just-signed players not yet promoted from `rookie_projections_2026.json`'s curve form into the flat CSV (see notes doc for the current list, e.g. Bernard/PIT, Stribling/SF, Stowers/PHI, Hurst/TB, A.Williams+K.Allen/WAS), a couple of very recent FA moves not in either file yet (Diggs/WAS, Deebo re-sign/SF), and **red-zone/inside-the-5 usage-and-efficiency splits are not modeled anywhere** — season-long shares stand in near the goal line too. All three are deliberate deferrals per Cam, not oversights — do not "fix" them without checking with him first, they may already be planned for a specific later pass.
  - **Real, confirmed (not data-error) 2026 team-assignment surprises found while cross-checking notes against the CSV this session** — same class of gotcha as any dataset this volatile, worth a sanity-check before trusting a name/team pairing that looks wrong: Isiah Pacheco → DET, Isaiah Likely → NYG, David Njoku → LAC, Kyler Murray → MIN, Kenneth Walker III → KC, Rachaad White + Jerome Ford → WAS. (Tua/ATL was already flagged pre-session, see the 2026-07-22-adjacent note below.)
  - **`coach_dna.json`'s 5 real first-time-2026-HC gaps closed** (new [`scripts/roster_management/merge_2026_coach_placeholders.py`](scripts/roster_management/merge_2026_coach_placeholders.py), rerun any time `coach_dna.json` is rebuilt — see §8): of `team_to_coach_2026.json`'s 9 first-time HCs, 4 (Schottenheimer/DAL, Coen/JAX, Moore/NO, Glenn/NYJ) already had real 2025 HC-level data (they were already their team's credited HC last year, nothing to do). The other 5 have none, because `coach_dna.json` is keyed off nflfastR's `home_coach`/`away_coach` columns, which only ever credit the actual HC of record — an OC/DC's individual play-calling never gets its own entry no matter how much of the offense was really theirs. **Klint Kubiak (LV)** was Seahawks OC 2024-2025, and Mike Macdonald's own entry happens to span exactly those 2 seasons (`seasons_observed: 2`) — **aliased** directly, a clean non-diluted proxy. **Jesse Minter (BAL)** and **Jeff Hafley (MIA)** were DCs (LAC, GB) — defensive background, so a team-offense proxy from their old team would misattribute someone else's scheme entirely — **set to a freshly-computed league-average placeholder.** **Joe Brady (BUF)** and **Todd Monken (CLE)** were real play-calling OCs, but the existing McDermott (9 seasons)/Harbaugh (11 seasons) entries span years before they arrived and would badly dilute their real numbers if reused directly — **also set to the league-average placeholder** as an interim measure; the real fix is a tenure-filtered rebuild of their actual OC-era PBP (BUF/BAL 2023-2025) using the R script's per-season `coach_season` table (computed but not currently written to JSON), not yet done. Every alias/placeholder entry carries an added `_note` field — check for it before trusting any single coach's numbers as a real personal career average. `coach_dna.json` now has 103 coaches.
  - **Given the above, nothing identified this session blocks a baseline 2026 sim run** — Cam explicitly said not to run one yet, so none has been executed. The first real run will be the first true end-to-end exercise of the full 2026-07-22 blending pipeline (Phases 0-7) against real hand-tuned data — treat it as a genuine verification pass, not a formality, per the same lesson §0's 2026-07-22 entry already learned once (individually-verified pieces ≠ a verified whole system).
- **2026-07-22 session — sim-inputs bug list closed out; 2026 Roster & DNA Blending Pipeline built end to end (Phases 0-6); Phase 7 rewires `game_engine.py` so every model input is actually the L4/season blend, not just target_share/carry_share. Full detail: [WORKLOG.md](WORKLOG.md) 2026-07-22 entry.** Plan doc referenced throughout the session: `giggly-launching-thacker.md` (outside the repo, Cam's local plan-mode file — not a project artifact, no in-repo link).
  - **Bug list close-out:** `trench_tiers_2025.json`'s `LAR`→`LA` rekey + missing `CIN` entry fixed (see `docs/sims/inputs/README.md` §5 — all items now closed, none open).
  - **2026 Roster & DNA Blending Pipeline (Phases 0-6), the season rollover infra:** `data/current_rosters/{TEAM}_traits_2026.json` (32 files, QB/RB/WR/TE only) built from real `nfl_data_py` 2026 rosters + draft data, not the old lost-to-history manual process that silently dropped real rookies (root cause confirmed: the 2025 files were built by a process that only included players with existing career DNA, dropping anyone with zero prior seasons — e.g. Ashton Jeanty, Raiders' actual 2025 starting RB, is absent from `LV_traits_2025.json` despite `nfl_data_py` correctly listing him). New blend formula, confirmed with Cam: preseason projection tapers out over games 1-5 (100/80/60/40/20% projection weight), then game 6+ is pure `(2/3)*L4 + (1/3)*season_to_date`. Rookies get a separate parametric usage/efficiency curve (`rookie_curves_v_0_1_0.py`) instead of a single static number, since usage is coach-gated separately from real talent. Weekly production job (`refresh_weekly_dna_v_0_1_0.py`) also detects real starter changes (compares recent snap volume between same-position teammates, flips `status`) since `_get_starter_static()` has no other way to react to an in-season change. **Not automated/scheduled** — Cam wants to run it by hand for at least the first few weeks of the 2026 season.
  - **`data/dna/qb_dna.json`/`rb_dna.json`/`wr_dna.json`/`te_dna.json`/`skill_dna.json`/`coach_dna.json` are now GITIGNORED** (large, mechanically regeneratable) — regenerate via `scripts/roster_management/regenerate_dna_v_0_1_0.py` after a fresh clone. `trench_dna.json` stays tracked (its composite z-score fields depend on an EDA CSV chain that isn't itself fully regeneratable by a verified script yet). See §10.
  - **Phase 7 — closed a real gap between what the blend pipeline computes and what the engine actually reads.** Audit found `target_share`/`carry_share`/`scramble_rate`/team-defense trench fields were already live (read from `self.rosters`/`trench_dna.json` fresh per game), but QB `cpoe`, TTT, `avg_air_yards_per_att`, and receiver traits (`catch_rate`, `adot`, `contested_catch_rate`, `elusiveness`, `broken_tackle_rate`, `deep_target_rate`, `avg_separation_yds`) were all reading the STATIC career-average `qb_dna.json`/`skill_dna.json` — worse, the zone-split "`_by_filter`" features (feeding Play Selection/Air Yards/YAC/Rush Yards) came from a **process-level `ModelRegistry` singleton**, shared and stale across every concurrent game regardless of the weekly refresh. Fixed: `game_engine.py` now sources all of this from `self.rosters` (live, per-game), falling back to the static DNA file only if a player/field is genuinely missing. Confirmed `contested_catch_rate` has zero real signal anywhere in `nfl_data_py` (checked PBP + all 3 NGS categories) — it's random noise assigned once at build time, deliberately excluded from the blend rather than faked with a proxy.
  - **Regression found and fixed along the way (not caused by Phase 7's own code, traced to Phase 1's DNA rebuild earlier the same session):** `coach_dna.json`'s `"proe"` field (merged in from the now-retired `coordinator_atlas.json` on 2026-07-16) got silently wiped when the R script was rerun to extend `coach_dna.json` through 2025 — `build_coach_dna()` never produced that field itself, it was always a separate one-time merge nobody re-ran. Every `get_coach_proe()` call was silently returning `0.0` (league average) for every team instead of real values. Recovered from git history (never actually committed as deleted) and fixed via `scripts/roster_management/merge_coach_proe.py`, rerunnable any time `coach_dna.json` gets rebuilt. **Caught only because Phase 7e's own PROE year-hardcode fix happened to be the first thing this session to actually check real `get_coach_proe()` output values instead of just confirming the code runs.**
  - **A second, larger regression found via real-batch verification, also traced to Phase 1 (not Phase 7's code — confirmed via a stash A/B test): sacks/game jumped from the Round-15-calibrated ~2.41 to ~6.3-6.4/game.** Root cause not fully decomposed (traced ~15% of the raw-model-output shift to `trench_dna.json`'s `def_pressure_rate`/`def_sack_rate`/`sack_rate_allowed` going from a flat, always-identical `0.15`/`0.06`/`0.06` default for every 2025 team-game to real per-team variance — Round 15's `SACK_PROB_CALIBRATION_MULT` was tuned entirely against that flat-default world, per the `trench_dna.json` gap fixed in this session's Phase 1 — but that alone didn't close the gap; `qb_cpoe` and TTT were checked and ruled out as major contributors). **Empirically recalibrated** `SACK_PROB_CALIBRATION_MULT` `1.24` → **`0.494`** (same target-matching method Round 15 used — see §11.14) rather than fully root-causing every contributing feature, given the scope of a complete decomposition. Verified across 8 diverse real matchups: mean 2.386 sacks/game vs. target 2.41 (within 1%).
  - **Still open, not fixed this session: completion % is also elevated (~68% vs. the Round-6/7-calibrated target 64.40%), confirmed present both before and after Phase 7's code changes (same stash-test methodology as the sack finding) — almost certainly the same root-cause family (Phase 1's broader DNA rebuild touching many completion-probability-relevant fields: `catch_rate`, `adot`, `contested_catch_rate`, `elusiveness`, `broken_tackle_rate`), not yet investigated or recalibrated.** Needs its own dedicated round, same rigor as the sack fix — don't assume it's fine.
  - **Environment gotcha this session ran into repeatedly before finding the real cause: a bare `python` on PATH is NOT this project's environment.** See the new §8 entry — activate `venv/` (or use `venv\Scripts\python.exe` directly) before running anything that touches `ModelRegistry`/trained models, or a `ModuleNotFoundError: No module named 'numpy._core'` looks like a real dependency bug but isn't.
- **2026-07-21 session — trench matchup gates (run + pass), 9 critical bug fixes, dead-code cleanup, Gate 2b, `game_engine.py` optimization pass. Full detail: [WORKLOG.md](WORKLOG.md) 2026-07-21 entry.** Supersedes several items below — see inline notes.
  - **v0.4.0 trench-tier rebuild (§0 2026-07-16 entry below) is DONE for both run and pass sides**, closing out the item that entry left "not yet decided": run side uses a 3-way mode-frequency gate (`p_neg_gate`/`p_exp_gate`/`p_rescue_gate`, calibrated against organic tail rates measured from real simulated games) replacing the old flat `run_mult_*_off`; pass side is Gate 2b, a small XGBoost correction gate (features: Gate 2's own calibrated probability + `pass_block_off_z`/`pass_def_z` composites) that adjusts Gate 2's sack probability without retraining Gate 2 itself, replacing the old flat `pass_mult_*_off` trench-tier component. **`data/dna/trench_tiers_2025.json` is now used for exactly one field** (`qb_cpoe_z`, feeding `pass_mult_*_off`'s remaining QB-CPOE-differential piece) — the 1-5 tier grades it was originally built around (`run_block_tier`/`pass_block_tier`/`run_stuff_tier`/`pass_rush_tier`) are no longer referenced anywhere in `game_engine.py`.
  - **9 critical bugs fixed** (full detail in [docs/audit/game_engine_cleanup/README.md](docs/audit/game_engine_cleanup/README.md)): OT was unreachable (`self.quarter` could never exceed 4, so every regulation tie was recorded as a fabricated home win) — **OT is now fully implemented** (`is_playoff` param, reg-season vs. playoff timeout/period rules, tie handling); sack probability missing a clip (could exceed 1.0 for elite pass-rush matchups); kneel-down mathematically unreachable on 4th down; a would-be TD could be silently erased by a same-play fumble roll; QB scrambles were invisible to fumble risk and the big-play counter; punt returns weren't re-clipped into valid field position; return-yardage stats could disagree with actual field position; pass-branch penalty-decline logic was asymmetric vs. run (confirmed via real PBP data that declined penalties read `penalty=0` in the structured column, not free-text only). Safety detection was entirely missing and had to be added as a prerequisite for the OT fix.
  - **`game_engine.py` performance/dead-code pass** (all items from the audit's opportunities list, see §11.13): per-team/per-zone CPOE/target-share/carry-share/trench-feature precompute (`_precompute_matchup_tables()`), precomputed receiver DNA traits, `receivers_cache`/`rusher_cache` now real arrays, fumble mechanics reworked (a per-carrier DNA `fumble_rate` multiplier was deleted — the field doesn't exist in any DNA file, so it was mathematically always 1.0; sack-fumble rate is now a flat, real-data constant, `SACK_FUMBLE_RATE = 0.1253`), 4th-down model's pandas overhead replaced with `booster.inplace_predict`, small items (`is_offensive` random-draw count, module-level `_sigmoid_arr`/`_logit_arr`). `_run_clock` was split into pure runoff mechanics + a new `_resolve_quarter_transitions()` method — see §11.13 and the updated §8 notes (the old §8 entries describing `_run_clock`'s quarter-transition logic now describe `_resolve_quarter_transitions` instead).
  - **New general lesson (bug found in passing, now fixed): any XGBoost model trained with early stopping needs `iteration_range=(0, best_iteration+1)` passed to `booster.inplace_predict()`, or production silently uses more (worse, post-best-checkpoint) trees than what was actually validated.** Found while fixing the 4th-down model's pandas overhead; turned out to also affect the already-deployed Gate 4 (interception) and the just-built Gate 2b — all three fixed. Verified: without the fix, live predictions diverged from `predict_proba()`'s validated output by up to 0.058 probability (Gate 4). Check this on any *future* early-stopped XGBoost model that gets wired into the hot path via the raw-booster pattern.
  - **Known, deliberately-unaddressed gaps flagged this session (not fixed, not urgent):** general (non-sack) fumble rate is calibrated below the raw real per-play rate (~0.5% sim vs. ~1.1-1.5% real for run/completion/scramble) — the history behind the existing `*0.80` calibration constant isn't known, wasn't in scope. Overall sack rate still runs ~5-15% hot vs. real depending on matchup — pre-existing (confirmed not caused by Gate 2b, which mildly *improves* it), candidate for a future Gate 2 retrain (previously rejected mid-session specifically to avoid disturbing Gate 2's verified calibration — may be worth revisiting given this drift).
  - **Follow-on same-day round: the audit's "Fragile / confusing" list, all items closed.** Full detail: [WORKLOG.md](WORKLOG.md)'s second 2026-07-21 entry. `qb_fumble_rate` mislabeling confirmed already fixed by the sack-fumble rework above. `regular_clock`'s hand-maintained NOT-list replaced with an additive `clock_handled` mask (future categories can't be silently forgotten). 5 duplicated `yardline_100<=5/<=20` zone-classification copies (not 3 — one more found in the completion-probability model) collapsed into `_classify_zone()` + `GOALLINE_YARDLINE`/`REDZONE_YARDLINE`. **`off_score`/`def_score` renamed to `away_score`/`home_score`** throughout the simulation path (`game_engine.py`, `batch.py`, `app.py`, 4 `scripts/simulation_runners/*.py`, `diagnose_scoring_efficiency.py`) — was always away/home there, never real offense/defense; `src/live/` correctly keeps the old names (real dynamic possession there, a different concept, confirmed zero crossover) — see the §8 note. Kickoff/punt/FG clock-runoff constants verified/fixed against real 2021-2025 PBP (new script: [`scripts/eda/analyze_kicking_timing.py`](scripts/eda/analyze_kicking_timing.py), output: [`docs/eda_outputs/kicking_timing/README.md`](docs/eda_outputs/kicking_timing/README.md)) — kickoff touchback rate (`0.2068`) turned out to already be exactly right for 2025 (just uncited; real 2025 rate is 20.68%, a dramatic drop from 2021-2024's 57-73% following the new kickoff rule); kickoff now differentiates near-instant touchbacks from ~5s returns (previously shared one range); punt base runoff `4-7s → 7-10s`; FG runoff (made and missed) now a flat 4s. `game_sec`'s inverted-quarter representation was reviewed and deliberately left as-is (Cam's call — it's a trained-model feature, already computed efficiently, and the only real complaint was human readability of raw threshold checks, not worth the change).
  - **Second same-day follow-up: the un/under-documented-constants pass, delivered.** Full detail: [WORKLOG.md](WORKLOG.md)'s third 2026-07-21 entry. Findings and Cam's per-item call: (1) kickoff/punt/interception/fumble return-yardage distribution params (`lognormal`/`gamma`/`exponential`, all uncited) — left as-is, documented as flagged for a future recheck, low priority (rare plays; any recheck should weight the clock-timing effect over yardage-shape accuracy per Cam). (2) QB time-to-throw `Normal(avg_ttt, 0.6)` — confirmed intentional design, not a gap; a future trench-matchup tie-in (better protection → more time to throw) noted as deferred, not started. (3) **Scramble yardage recalibrated** — `Normal(5,4)` → **`Normal(7.0, 6.1)`**, verified via new script [`scripts/eda/analyze_scramble_yardage.py`](scripts/eda/analyze_scramble_yardage.py) against real 2020-2025 PBP (mean 6.995, std 6.074) using `nfl_data_py`'s `qb_scramble` column directly — no heuristic needed, that column already reliably separates real scrambles from designed QB runs (Cam had assumed it might not). (4) Completion-probability model's separation-roll std and its 1-yard contested-vs-open cutoff — left as-is, documented as future research (the cutoff is believed to match the community "contested catch" definition, unverified). (5) YAC noise formula's elusiveness/broken-tackle-rate coefficients (1.8, 6.0) — left as-is, flagged as an offseason investigation. (6) 4th-down desperation thresholds — confirmed fine as-is (football knowledge, not a data-fit gap).
- **Repo audit (2026-07-15) — landed, one item still open.** Full-repo audit brought the entire working tree into git for the first time (it was previously uncommitted/OneDrive-only), upgraded Python 3.8 (EOL) → **3.12** with a validated `requirements.txt`, and did a bounded dead-code pass. Merged as [PR #1](https://github.com/txcwalker/NFLSims/pull/1), [PR #2](https://github.com/txcwalker/NFLSims/pull/2), [PR #3](https://github.com/txcwalker/NFLSims/pull/3). Full detail: [WORKLOG.md](WORKLOG.md) 2026-07-15 entry.
  - **The Model Development Standard is now written** (2026-07-15, work in progress) — a generic model-building standard (data splitting, metric selection, versioning via SemVer, sparse-segment handling, end-to-end validation, etc.). By Cam's choice it's a **cross-project reference kept outside any single repo** (Cam's personal template set, not published to GitHub), not a file in this project — so there's no in-repo link to it. It is the audit gate for this project's models; it does **not** track per-model compliance itself, which is what this note does. First real application: 4 of 11 model families (`win_probability`, `rush_yards`, `fg`, `fourth_down_conversion` — hand-written R-era carryovers) have no committed training script, and `positional_ep_v_0_1_0` uses a plain (non-group-aware) split. Cam's call: **defer retraining these until just before the 1.0.0 season launch**, not now.
  - Function-level docstrings (Inputs/Outputs/Purpose per the project's global doc standard) across `src/` — deliberately being done **case-by-case**, not as one pass. `src/nfl_sim/optimizer.py` (10 functions, 0 documented) is a representative gap. Module-level docstrings on 10 core files landed in PR #2 already.
  - Also still open, lower priority: delete `venv_py38_old/` (the pre-upgrade fallback venv) once 3.12 is fully trusted; no code-quality/performance pass on `src/` has been done yet (out of scope for the audit).
- **Sim-inputs follow-up session (2026-07-16)** — worked through the bug list in [docs/sims/inputs/README.md](docs/sims/inputs/README.md) §5. Fixed: the `position` key mismatch (`game_engine.py:1049`, was reading `'pos'` from `skill_dna.json`, which uses `'position'`); removed a confirmed-dead z-score block in the rush-yards path (`yac_att_z`/`btk_rate_z`/`run_block_z`/`box_density_z`/`efficiency_z` — none reach the real 7-feature `X_run_zone` model call); removed the remaining dead air-yards QB/coach/receiver arrays (14 of the original 17 found 2026-07-13, plus a wholly-unused `ay_feats` 32-col allocation) — the other 3 (`play_ttt`, `avg_air_yards_per_att`, `deep_target_rate`) are now genuinely live, feeding the rebuilt `air_yards_v_0_1_1` `V.0.3.0` model (11 features, verified live feature-build matches exactly). **Important correction:** the doc's bug #8 claim (`self.coach_aggression` is vestigial) was wrong — it actively biases 4th-down GO probability by coach `deep_shot_rate`; corrected in place, not deleted. Fumble-rate gap (bug #6) resolved as intentional (Cam: fumbles are largely random at the individual level; uniform baseline is fine) — no fix planned.
  - **Done (2026-07-16):** consolidated `coordinator_atlas.json`'s PROE data into `coach_dna.json` (each coach's new `"proe"` field) and retired the file — `proe_overlay_v_0_1_0.py` and `src/api/app.py` both updated to read `coach_dna.json`. This also fixed the dead PROE override-detection check for free (verified: a PROE-only change is now correctly detected). Pre-check confirmed all 32 current HCs match correctly; one edge case (`Ben Johnson`, new for 2025, no 2015-2024 HC history) already handled by the existing default-fallback.
  - **DONE as of 2026-07-21 (see the entry at the top of this section) — full trench-tier rebuild ("v0.4.0"), all 4 cells of the run/pass x offense/defense matrix.** Goal per Cam: rebuild what `trench_tiers_2025.json`'s old 1-5 tier grades were trying to do, but with real, EDA-validated per-team data. The game-engine integration point mentioned as undecided below **is now decided and live**: run side is a 3-way mode-frequency gate, pass side is Gate 2b (a correction gate on top of Gate 2, not a standalone replacement) — see the 2026-07-21 entry for the mechanism. EDA status per cell (historical record, still accurate):
    - **Run-blocking (offense) — EDA done.** 4 metrics chosen, mirroring `trench_dna.json`'s existing pass-blocking set: `ybc_per_att`, `rush_pct_over_expected`, `stuff_rate`, `avg_time_to_los` (source: `data/external/pfr_adv_rushing_stats.csv` + `nfl_data_py` NGS rushing; chosen over 3 more-redundant alternates via correlation check). Real signal — year-over-year persistence + face-validates against known team reputations. See `docs/eda_outputs/run_block/README.md`.
    - **Run-stopping (defense) — EDA done (2026-07-16).** Derived via opponent-join (no native defense-side rushing stat exists anywhere in the pipeline): `ybc_allowed_per_att`, `rush_pct_over_expected_allowed`, `stuff_rate_forced`, `aly_allowed`, `pct_stacked_box_forced`. Confirmed a genuinely distinct skill from pass-rush (weak correlation, `|r|`≤0.40, against the existing `def_pressure_rate`). Note: `ybc_allowed_per_att`/`rush_pct_over_expected_allowed` are noisy year-to-year (r≈0.12) — trust `stuff_rate_forced`/`aly_allowed` (r≈0.4) more. See `docs/eda_outputs/run_defense/README.md`.
    - **Pass-blocking (offense) — EDA done (2026-07-16).** 4-metric set: `sack_rate_allowed` (existing, already live) + `pressured_pct_allowed`, `hurry_rate_allowed`, `hit_rate_allowed` (new, `nfl_data_py.import_weekly_pfr(s_type='pass')`). None redundant with the existing metric or each other (`hurry_rate_allowed` vs. `sack_rate_allowed` r=-0.01) — all 4 kept. **`off_pass_block_win_rate` is NOT available** — NGS-proprietary, confirmed unreachable without an API partnership; `pressured_pct_allowed` is the real substitute, not an addition to it. See `docs/eda_outputs/pass_block/README.md`.
    - **Pass-rushing (defense) — EDA done (2026-07-16), 3 metrics not 4, deliberately.** `def_pressure_rate` + `def_sack_rate` (existing, already live in Gate 2) + `hurry_rate_forced` (new). **Important data-quality finding:** `def_times_blitzed`/`def_times_hurried`/`def_times_hitqb` (the native "defense" columns in the PFR pass table) are confirmed **100% NULL across every season 2020-2024** — unusable despite existing as column names. Derived the same info instead via opponent-join of the real offense-side columns (same technique as run-defense). Of the 3 derived candidates, 2 (`pressure_rate_forced` r=0.86, `hit_rate_forced` r=0.90 vs. existing `def_pressure_rate`) are near-duplicates of what's already live — only `hurry_rate_forced` (r=0.21) is genuinely new. Deliberately not padded to 4 with a redundant metric just for symmetry with the offense side.
    - **Not yet done, any cell:** the actual multiplier/feature formula, multi-year blending (single-season values are noisy across the board — see both EDA docs' caveats), and the game-engine integration decision itself. Still following the Model Development Standard step by step.
  - **Deferred to off-season:** revisit whether scaling 4th-down aggression directly off `deep_shot_rate` (`self.coach_aggression`) is the right design — Cam's not fully sold on it, but it's a real, working mechanic for now, not touched.
- **`clock_physics_v020` audit is the active thread** — see [docs/audit/clock_physics_v020/README.md](docs/audit/clock_physics_v020/README.md) for the full round-by-round history (9 rounds so far). Do not start a fresh investigation without reading this doc first; it has already ruled out several plausible-sounding hypotheses (see §11.5).
- **YAC is fixed (Round 9).** Was −14.4% overall (worst on deep/screens); now −1.6% overall, essentially exact in primary/deep/screen/standard, and goalline improved 5x (+241%→+49.9%, small residual on a tiny-volume, tiny-absolute-yardage zone). Two root causes, both fixed: (1) the model was trained with `reg:absoluteerror` (median-targeting) instead of `reg:squarederror` (mean-targeting), which systematically undershoots the mean on YAC's right-skewed distribution; (2) a post-model noise-injection step used a flat minimum scale (6.0) that, combined with floor-clipping at zero, mechanically inflated the mean wherever the real YAC is small (goalline worst, redzone next, primary fine) — fixed by scaling the noise to the play's own predicted YAC. See §11.7.
- **Snap-count overshoot: squeeze-play mechanic removed (Round 10), partial fix.** Squeeze-play (the discrete "rush the snap to guarantee an extra down before the two-minute warning" mechanic) is gone entirely — replaced with a plain `_sample_pace_runoff` call, since the empirical pace pools already encode real teams' hurry-up pace near the warning via fine-grained time-window buckets (no separate mechanic needed for realistic speedup, and no artificial extra-play guarantee needed either). Measured, isolated effect: offensive snaps +7.2%→+5.4%, total snaps +6.6%→+4.7% (~2.3-2.9 fewer combined plays/game) — Cam's instinct that this was "a couple of plays a game" was right. **~6-7 combined plays/game still unexplained.** See §11.8.
- **Air yards retrained (Round 11) — architecture fixed, but barely moved the numbers.** Unlike YAC, the orphaned script already used mean-targeting (`reg:squarederror`), so there was no known objective-function bug to fix. Pass yards/game and snap count both stayed flat (−7.8%→−8.1%, +5.4%→+5.2%). Main value: a real, working, git-visible zone-split training script now exists where there was none. Goalline air_yards remains a real residual (−42%, small volume ~4.5% of attempts) — a fallback-donor bug (defaulting to `primary` instead of the adjacent `redzone` zone) was found and fixed but didn't meaningfully move the number; not chased further given its small volume share. See §11.9.
- **Full 18-week 2025 season dataset regenerated (Round 16), now the default data source for the frontend tools.** `data/interim/sim_results_2025_{games,players}.parquet` — all 272 REG-season matchups at N=1000, current (post-all-fixes) engine. `src/api/app.py` already reads these by default (`/api/week_projections` already defaults to `week=1`); nothing needed there. Frontend UI wiring (actually consuming these endpoints in HistoricalLab/GameSummary/DFS site) is a deferred follow-up — this round was data generation only. **Footgun to know about:** `scripts/simulation_runners/run_week_1_only_2025.py` unconditionally overwrites the *entire* parquet cache with just Week 1's 16 games — that's what made the cache stale before this round. Don't run it expecting it to "add" week 1; it replaces everything. Use `run_full_season_sim_2025.py` to (re)generate the full cache.
- **Quarter-transition state corruption: found and fixed (Round 13).** Cam spotted repeated identical down/distance/yardline lines in a readable play-by-play (different players/gains, same situation) and asked for a real audit rather than a clock-physics explanation. Confirmed genuine bug: the Q2→Q3 kickoff logic was overwriting a just-resolved play's real yardline/down/distance with the hardcoded second-half-kickoff state, in the same step, discarding real game progress on roughly half of all simulated games (every game hits this once, whenever the Q2-ending play is a real snap rather than a kickoff/kneel). Fixed; verified via a new permanent regression test, `scripts/audit_play_continuity.py` (95→1 violations across 1000 games, the 1 a diagnostic-only artifact). Season-level snap counts barely moved (a wash — see §11.10) — this does not explain the still-open overshoot, but was a real correctness bug independent of it.
- **Sacks: fixed (Round 15).** Net sacks/game now 2.410 vs. real 2.41 (+0.2%, essentially exact). Two changes: throwaway-diversion rate cut 0.20→0.06 (kept `scramble_rate` untouched — real per-QB data, not an arbitrary constant), plus `SACK_PROB_CALIBRATION_MULT = 1.24` on gate 2's raw output (same pattern as gate 4's existing `* 0.80`), tuned empirically via `scripts/eda/test_sacks_diversion_hypothesis.py`. Bonus: snap-count overshoot improved slightly as a side effect (offensive +5.2%→+4.2%, total +4.6%→+3.8%). Expected trade-off, not a new bug: pass yards/game widened −8.1%→−10.2% (sacks now correctly subtract real negative yardage instead of some being zero-yard throwaways) — ties to the already-open air-yards gap (Round 11). The L4 duplicate-feature bug (Round 12) is deliberately bundled into the v0.4.0 trench-tier retrain rather than fixed now — it needs real per-week trench data that doesn't exist in the current pipeline (`trench_dna.json` is season-level only), the same kind of data-build as the tier-matchup work. See §11.11-11.12.
- **New, smaller residual: air_yards is now the largest remaining piece of the pass-yards gap** (−4.4% to −7.3% depending on zone/depth), untouched by the Round 9 YAC work. `air_yards_v_0_1_1` has the identical orphaned-training-script problem YAC had — the same fix pattern (live pull + DNA joins, mean-targeting objective, grouped split) would likely apply directly if tackled next.
- **Resolved by the 2026-07-15 repo audit:** `src/nfl_sim/models/` used to have zero files tracked in git, ever (the Round 9 finding below) — same for the small curated `data/` subdirs. **This is fixed:** all 11 model families are now committed (re-saved under Python 3.12's xgboost/scikit-learn, predictions verified byte-identical to the pre-resave versions), and `data/dna`, `data/current_rosters`, `data/external` are tracked. Large/regeneratable data (`data/processed/`, `data/interim/`, R's legacy `.rds` training splits) remains deliberately gitignored — see `.gitignore` for the exact scoping. The original Round 9 finding, kept for history: <details><summary>expand</summary>`src/nfl_sim/models/` had zero files tracked in git, ever (`git ls-files src/nfl_sim/models/` returned nothing) — same for `data/` (gitignored outright). This explained why the "orphaned V.0.2.0 retrain script" from Round 8 was never findable in git history.</details>
- **Sacks recalibration (§11.4) — the trench-tier-migration piece is DONE as of 2026-07-21 (Gate 2b, see the top of this section).** Still open, not scoped to any version: the ~5-15%-hot sack-rate drift flagged in the 2026-07-21 entry, and the L4 duplicate-feature bug (Round 12 Finding 2) which still needs real per-week trench data that doesn't exist in the pipeline. **Still deferred to v0.4.0** (do not start without Cam's go-ahead): injury stoppages, other administrative clock stoppages (replay review, etc.).
- **Deferred, parameters already agreed:** throwaway mechanism rework. Target: ~5% of all dropbacks should be throwaways, split 75% pressure-related / 25% clean-pocket. Current mechanism only fires as a 20% escape from would-be-sacks — too narrow, needs a clean-pocket path too. See §11.6.
- **Must land before season launch (Sept 2026), not started:** extra-point/2-point-conversion modeling. Every touchdown currently awards a flat +7 unconditionally — there is no PAT or 2-point-attempt logic anywhere in the engine (`docs/models/README.md` §2.14). Acknowledged gap, deliberately not being worked on right now.

---

## 1. Repo Layout (active code only)

```
src/
  api/app.py                          ← FastAPI — analytics backend (port 8000)
  live/espn_adapter.py                ← ESPN scraper + play-state parser
  nfl_sim/
    game_engine.py                    ← Monte Carlo play engine (vectorized, N games in parallel). MOST ACTIVE FILE — see §11.
    model_registry.py                 ← Singleton loading all trained models; game_engine.py calls self.registry.*
    batch.py                          ← BatchSimulator wraps NFLGameEngine + StatAggregator for N-iteration batch runs
    nfl_positional_evaluator.py       ← KEP/EP evaluator; evaluate_one_step (one-play KEP delta) + suggest_lines (PV chains)
    week1_2025.py                     ← nfl_data_py loader for Week 1 2025 schedule + PBP; computes home_kep per play
    models/
      win_probability/                ← WP XGBoost model + calibration artifacts
      clock_pace_v_0_1_0/              ← NEW (clock_physics_v020): empirical bootstrap pools for snap-to-snap runoff, keyed by (quarter/time-window x score-margin tier). pace_pools.json is generated, not hand-edited — rebuild via scripts/eda/analyze_clock_pace_grid.py.
      positional_ep_v_0_1_0/
        train_positional_ep.py        ← EP model training script
        positional_ep_inference.py    ← EP inference wrapper
        positional_ep_model.json      ← Trained XGBoost artifact (531 KB)
        metadata.json                 ← Version, features, training metrics

scripts/
  simulation_runners/run_weeks_1_to_4_2025.py  ← Full batch audit driver. Writes docs/audit/v_0_2_0_audit/sim_post_cal_metrics.json + docs/boxscores/week_N/. THE way to measure any game_engine.py change at scale.
  eda/run_historical_eda.py                    ← Real 2021-2025 comparison metrics → historical_eda_metrics.json (the "ground truth" the sim is compared against)
  eda/analyze_clock_pace_grid.py                ← Rebuilds src/nfl_sim/models/clock_pace_v_0_1_0/pace_pools.json from real pbp data
  print_play_by_play_v020.py                    ← Non-interactive single-game (N=1) play-by-play dump for manual clock/mechanic verification. Different from (and supersedes for clock work) the legacy scripts/play_by_play_runner.py, which drives the old sequential engine and is interactive.
  eda/build_trench_dna_composites.py            ← Run-blocking/run-defense composites (2026-07-21) into data/dna/trench_dna.json
  eda/build_trench_dna_pass_composites.py       ← Pass-blocking/pass-rush composites (2026-07-21) into data/dna/trench_dna.json, feeds Gate 2b
  model_training/measure_rush_organic_tail_rates.py  ← Measures organic negative/explosive rush rates from real simulated games (gate forced to 0) — used to calibrate the rush trench gate
  eda/analyze_kicking_timing.py                 ← Kickoff touchback rate + kickoff/punt/FG live-play timing vs. real 2021-2025 PBP (2026-07-21), same game_clock_elapsed methodology as analyze_clock_pace_grid.py. Output: docs/eda_outputs/kicking_timing/
  eda/analyze_scramble_yardage.py               ← QB scramble yardage vs. real 2020-2025 PBP (2026-07-21), using nfl_data_py's qb_scramble column directly. Output: docs/eda_outputs/qb_scramble_yardage/

  roster_management/                            ← 2026 Roster & DNA Blending Pipeline (2026-07-22), see §0. Build order matters -- see WORKLOG.md's 2026-07-22 entry for the full phase sequence.
    build_full_name_dna.py                      ← Rebuilds qb/rb/wr/te/skill_dna.json from nfl_data_py, seasons 2021-2025. Seeded (np.random.seed(2026)) for reproducibility -- was NOT seeded before this session, a real gotcha if this file is ever gitignored+regenerated (see §10).
    merge_trench_dna_legacy.py                  ← Merges R-script-built legacy trench fields (def_pressure_rate/def_sack_rate/sack_rate_allowed) into trench_dna.json without disturbing the v0.4.0 composite z-score fields a separate pipeline owns.
    merge_coach_proe.py                         ← Restores coach_dna.json's "proe" field from git history (coordinator_atlas.json, retired but never actually committed as deleted) -- rerun any time coach_dna.json gets rebuilt from scratch, see §0's regression note.
    merge_2026_coach_placeholders.py             ← Adds coach_dna.json entries for the 5 first-time-2026-HC gaps with no real HC-level nflfastR history (Kubiak/LV aliased from Mike Macdonald's real 2-season SEA-OC-tenure entry; Minter/BAL, Hafley/MIA, Brady/BUF, Monken/CLE set to a freshly-computed league-average placeholder, each flagged via an added "_note" field). SUPERSEDED for 4 of those 5 by build_2026_ooc_proxies.py below (Hafley/MIA is the only one still on the league-average placeholder from this script) -- run this one first, then build_2026_ooc_proxies.py to overwrite Minter/Brady/Monken/Kubiak with real proxies. Rerun any time coach_dna.json gets rebuilt from scratch (run alongside merge_coach_proe.py).
    build_2026_ooc_proxies.py                    ← 2026-08-20: replaces 4 of merge_2026_coach_placeholders.py's placeholders with real team+season-filtered PBP proxies, using coaching history Cam supplied (nflfastR only credits the HC of record, never the actual OC, so these have to be built from the OC's real team+season(s) directly): Mike LaFleur/ARI ← LA/2023-2025 (Rams OC); Jesse Minter/BAL ← CHI/2025 (proxying real OC Declan Doyle, since Minter himself is a DC); Joe Brady/BUF ← BUF/2023-2025; Todd Monken/CLE ← BAL/2023-2025; Klint Kubiak/LV ← NO/2024 + SEA/2025 (CORRECTS the prior Macdonald alias, which wrongly assumed Kubiak was in Seattle for 2024 too -- he was actually Saints OC that year). Also overwrites the matching rows in coach_coordinator_levers_2026.csv. Hafley/MIA untouched (no real-history info supplied yet, still league-average). Rerun any time coach_dna.json gets rebuilt from scratch, AFTER merge_2026_coach_placeholders.py.
    build_2026_rosters_v_0_1_0.py                ← Builds data/current_rosters/{TEAM}_traits_2026.json (QB/RB/WR/TE only). QA gate: every round-1 offensive skill-position draft pick must appear by name or the build fails loudly. Self-heals players missing from nfl_data_py's roster feed by patching them in from draft data.
    build_2026_trench_shell_v_0_1_0.py           ← Establishes trench_dna.json's "2026" season entry (carries forward 2025, freezes a preseason_projection baseline).
    build_rookie_projections_v_0_1_0.py          ← Generates data/dna/rookie_projections_2026.json's placeholder skeleton (usage/efficiency curves) for every entry_year==2026 rookie. Values are explicitly-flagged placeholders for Cam to hand-edit, not real projections. SUPERSEDED 2026-08-20: Cam had add_2026_missing_skill_players.py (below) promote all 214 skill-position entries this had produced to the flat/no-ramp track instead -- rookie_projections_2026.json is now just an empty `_metadata` shell. Only re-populates if this script is rerun after a future full roster rebuild.
    add_2026_missing_skill_players.py            ← 2026-08-20: (1) hand-adds skill-position free-agent signings nfl_data_py's live 2026 roster feed doesn't have under ANY team yet, via a NEW_SIGNINGS dict meant to keep growing (skips rather than errors on a name already present, so it's safe to add one more and rerun) -- so far: Najee Harris→NYG (RB, real 2024 PIT stats -- Cam's call, not his 2025 LAC year), Stefon Diggs→WAS (WR, real 2025 NE stats), Deebo Samuel→SF (WR, real 2025 WAS stats), Keenan Allen→IND (WR, real 2024+2025 CHI+LAC stats POOLED across both seasons -- Cam's call, both were real volume; pooling raw plays rather than averaging two yearly rates naturally volume-weights it). target_share/carry_share forced to 0 per Cam's instruction (a new team's share isn't inferrable from old-team data), other fields from real PBP+NGS via the exact per-player formulas build_full_name_dna.py already uses, just restricted to the specified season(s). (2) Promotes every remaining curve-based 2026 rookie (214 of them) to the flat track the same way build_2026_rosters_v_0_1_0.enrich_player() already freezes veterans' preseason_projection -- and correspondingly deletes each from rookie_projections_2026.json, since refresh_weekly_dna_v_0_1_0.py's is_rookie check keys off THAT file's membership, not preseason_projection's presence (confirmed by reading blend_one_player() before writing this). A small number of rookies (8, e.g. Jeremiyah Love/ARI) already had real hand-tuned non-zero shares from earlier sessions -- those are preserved as-is by the freeze, not zeroed; the rest were already 0.0 from build time and stay that way. Run export_preseason_overrides_v_0_1_0.py 2026 + export_zone_usage_overrides_v_0_1_0.py afterward to regenerate both flat CSVs (913 rows each now, up from 695).
    pull_2026_rookie_college_stats.py            ← 2026-08-20: replaces position-average placeholders on the 80 DRAFTED 2026 skill-position rookies (not the 145 UDFA rookies) with real 2024+2025 college production from the CollegeFootballData.com (CFBD) API. Joins our roster's (round, pick) draft_capital directly to CFBD's /draft/picks college_athlete_id -- no name-matching needed. REQUIRES venv_cfbd/ (gitignored, separate from the main venv/ -- `cfbd` pins pydantic<2, which conflicts with the main venv's pydantic 2.x FastAPI backend if installed there; see .gitignore's 2026-08-20 note). Needs CFBD_API_KEY in the repo-root .env (free key, no card, https://collegefootballdata.com/key) -- Cam is getting one. Only ~161 API calls total, nowhere near the free tier's 1,000/month. IMPORTANT: CFBD's college data doesn't have an NFL-Next-Gen-Stats equivalent (no separation/YAC/air-yards tracking), so only `ypc` (RB rushing yards/carry) is realistically computable from it -- everything else on a promoted rookie stays on its position-average default. Stat-name mapping (STAT_NAME_MAP) is UNVERIFIED against a live response (written from CFBD's documented conventions, no API key was available while writing this) -- the script prints raw category/stat names for the first 3 matched players specifically so the first real run can confirm/correct it.
    audit_2026_roster_staleness.py               ← 2026-08-20: the mirror image of add_2026_missing_skill_players.py -- checks preseason_overrides_2026.csv's 913 rows against nfl.import_seasonal_rosters([2026]) for players who've actually LEFT their listed team (trade/cut/retired) rather than ones missing. Cross-checks active/reserve status AND any-status-on-any-team before flagging a "departure" (a raw name-only lookup would false-positive on real feed quirks -- confirmed one team-code mismatch, AZ vs ARI, hardcoded as a fix, and one position-code mismatch, Barion Brown/NO tagged "KR" not "WR" in this table, both real but not real departures). Read-only/report-only, never edits files itself -- first run found 0 real departures, all 5 "not found" flags were either players add_2026_missing_skill_players.py had just hand-added (feed hasn't caught up) or Brown's KR quirk.
    refresh_weekly_dna_v_0_1_0.py                ← THE weekly production job. Pulls real PBP+NGS through the completed week, blends per the taper/steady-state formula (see §0), detects starter changes, writes updated current_rosters + trench_dna.json. NOT scheduled/automated. Usage: `python refresh_weekly_dna_v_0_1_0.py <year> <completed_week>`.
    regenerate_dna_v_0_1_0.py                    ← Orchestrator: reruns build_full_name_dna.py + the R script + build_2026_rosters_v_0_1_0.py in the right order. Run after a fresh clone to rebuild everything §10 gitignores. Does NOT touch trench_dna.json (stays tracked, see §10).
    validate_rosters.py                          ← Schema/field-presence QA, generalized to take a year arg: `python validate_rosters.py 2026`.
    scrape_nfl_rosters_v_0_1_0.py                ← 2026-09-03: stdlib scrape of the 32 nfl.com/teams/{slug}/roster pages -> data/overrides/2026/_nfl_com_roster.csv (cached roster-status source, exactly 53 ACT/team; run to refresh).
    roster_feed_v_0_1_0.py                       ← 2026-09-03: shared roster-status helpers. build_feed_index() (reads _nfl_com_roster.csv), resolve_status() (exact match_key then skill-only lastname+initial fuzzy), clean_name(), match_key(), NFL_STATUS_TO_SLOT, RETURN_WEEK_SEED. nfl_data_py only for a best-effort player_id bridge.
    export_team_season_overrides_v_0_1_0.py      ← 2026-09-03: (re)seed the 32 data/overrides/2026/season_long/{TEAM}.csv (26 cols incl. rz_/gl_ shares) from preseason_overrides_2026.csv + zone_usage_overrides_2026.csv + _nfl_com_roster.csv (drop zero-role cuts, keep role-cuts flagged as roster_slot=cut, add nfl.com-new players at 0/0, fill roster_slot/return_week). OVERWRITES. Preserves every hand-tuned value; prints review flags.
    build_week_overrides_v_0_1_0.py <week> [TEAM] ← 2026-09-02: data/overrides/2026/week_NN/{TEAM}.csv from the season_long sheets -- reserve players not back by `week` (and cut players) zeroed, share redistributed pro-rata to active same-position players.
    build_roster_md_v_0_1_0.py                   ← 2026-09-02: docs/rosters/2026/{TEAM}.md readable roster snapshot from the season_long sheets.
    apply_team_season_overrides_v_0_1_0.py       ← 2026-09-04: concat the 32 season_long sheets -> regenerate preseason_overrides_2026.csv + zone_usage_overrides_2026.csv -> apply_preseason_overrides(2026) + apply_zone_usage_overrides(2026) -> sync_roster_membership() (zeroes traits.json players no longer in any sheet, enrich_player-creates new ones, re-sorts by position) -- the LAST step is what makes traits.json's player SET, not just values, match the sheets. Idempotent, this IS what makes the sim reflect the sheets (game_engine.py only ever reads current_rosters/*_traits_{year}.json, never a CSV).
    apply_team_week_overrides_v_0_1_0.py <week>  ← 2026-09-04: compiles data/overrides/2026/week_NN/{TEAM}.csv -> current_rosters/dfs/{TEAM}_traits_2026.json, SEPARATE from the season-long file. Base = fresh copy of the CURRENT season-long traits.json (all static fields come along free), that week's 15 flat + 4 rz/gl fields overlaid via apply_sheet_helpers_v_0_1_0.apply_sheet_to_traits(). Only the current week's JSON is kept -- overwritten every run, no per-week history. Sim it via NFLGameEngine(..., rosters_dir="data/current_rosters/dfs").
    apply_sheet_helpers_v_0_1_0.py                ← 2026-09-04: shared apply_sheet_to_traits() (create-missing via enrich_player / zero-removed / overlay-values) used by apply_team_week_overrides_v_0_1_0.py. apply_team_season_overrides_v_0_1_0.py's own sync_roster_membership() does the same job inline (not yet refactored onto this -- left alone since it's already verified working).

src/data_pipeline/                              ← Pure-function blending engine (2026-07-22), see §0. No I/O except rolling_stats_v_0_1_0's PBP/NGS reads.
  rolling_stats_v_0_1_0.py                      ← PBP/NGS -> per-week L4/season-to-date rolling stats, player + team + zone-split (primary/redzone/goalline, with a small-sample fallback to the flat value -- MIN_ZONE_SAMPLES=5). contested_catch_rate deliberately excluded, see §0.
  dna_blender_v_0_1_0.py                        ← taper_weights()/steady_state_blend()/blend_player_dna()/blend_team_dna() -- the actual (2/3 L4 + 1/3 season, tapered over games 1-5) formula.
  rookie_curves_v_0_1_0.py                      ← interpolate_curve()/resolve_rookie_curves() -- parametric ramp for a rookie's own projection input, separate from the taper above.
  week_roster_v_0_1_0.py                        ← 2026-09-02: resolve_week_rows(rows, week) -- takes the flat rows of a data/overrides/2026/season_long/{TEAM}.csv and returns that week's rows (reserve players not back by `week`, and cut players, zeroed + share redistributed pro-rata to active same-position). parse_return_week(), is_available(). Not wired into any sim yet. See §0.

src/nfl_sim/models/chaos_v_0_1_0/
  train_gate2b_trench_correction.py    ← Trains Gate 2b (sack-probability correction using pass_block_off_z/pass_def_z); mirrors train_gate4.py's structure
  gate_2b_trench_correction.joblib     ← Trained Gate 2b artifact

docs/
  audit/game_engine_cleanup/README.md  ← 2026-07-21 audit: 9 critical bugs, dead/write-only code, optimization opportunities — task brief for most of the 2026-07-21 session, see §0.
  audit/clock_physics_v020/            ← Active audit doc (7 rounds). Read before touching clock/pace/completion-rate code.
  audit/v_0_2_0_audit/                 ← Generated comparison JSONs (sim_post_cal_metrics.json, historical_eda_metrics.json) + weeks 1-4 game/standings/leader summaries. Regenerated by the scripts above, not hand-edited.
  boxscores/week_1..week_18/           ← Generated per-game boxscores. NOT git-tracked (confirmed — 0 files under version control). Local-only output.

frontend_analysis/                    ← Analytics/strategy site (port 5174)
  vite.config.js                      ← Proxy: /api → http://127.0.0.1:8000
  src/
    api.js                            ← API_BASE='/api'; safeFetch + all mock data
    pagesConfig.js                    ← Page registry; add new pages here + App.jsx
    pages/
      GameSummary.jsx                 ← Main game view: WP graph, 4th down, chess tab
      HistoricalLab.jsx               ← Testing lab: Week 1 2025 chess evaluator (remove before launch)

frontend/                             ← DFS site (port 5173; API on 8002)
  vite.config.js                      ← Proxy: /api → http://127.0.0.1:8002

tests/
  README.md                           ← Directory overview (added 2026-07-15)
  test_positional_evaluator.py        ← 18 unit tests (KEPConverter, EP model, evaluator). Must stay green through any game_engine.py edit.
  test_efsd_evaluator.py              ← EFSD evaluator tests
  test_live_pipeline.py               ← Live game-day pipeline tests

requirements.txt                      ← Pinned dependency set, validated on Python 3.12 (added 2026-07-15; see WORKLOG.md). venv/ = 3.12.13; venv_py38_old/ is the retired 3.8 fallback, delete once 3.12 is fully trusted.
```

**Module docstrings** (top-of-file summary + entry point + pointer to companion `.md`) exist on: `game_engine.py`, `model_registry.py`, `batch.py`, `visuals.py`, `script_chainer.py`, `api/app.py`, and the 4 `data_pipeline/*.py` files — added 2026-07-15. **Function-level Input/Output/Purpose docstrings are still largely absent** across `src/`; being added case-by-case, not as one pass (see §0).

---

## 2. Frozen / Legacy Zones

- `legacy/` — do not modify; kept for archival reference. **Not purely archival, though:** `src/nfl_sim/batch.py` actively imports `legacy.game_engine_sequential.SequentialNFLGameEngine` as its sequential (`vectorized=False`) fallback, so that one file is a live dependency. Its data + model binaries (2.35GB) are gitignored; only code is committed. The retired air_yards/yac `train.py`+`evaluate.py` footgun scripts were moved here in the repo-audit dead-code pass (each carries a RETIRED header) — the real trainers are `src/nfl_sim/models/*/train_zone_split.py`.
- `R/` (root) — **ACTIVE, not deprecated.** This is the live game-day 4th-down bot: `src/live/main.py` shells out to `Rscript R/simulators/fourth_down/run_one_sim.R`, and both GitHub Actions workflows (`nfl_live.yml`, `nfl_manuel.yml`) run R directly. Python is authoritative for the *simulation engine* (`src/nfl_sim/`), but the *live 4th-down bot* remains R. Only the R under `legacy/` is retired.
- `frontend/src/pages/InDevelopment.jsx` — chess code removed; no chess page in pagesConfig.js. Do not re-add chess to the DFS site.
- `scripts/play_by_play_runner.py` — drives the OLD `legacy.game_engine_sequential.SequentialNFLGameEngine`, not the current vectorized engine. Interactive (blocks on stdin). Do not use for verifying current engine behavior — use `scripts/print_play_by_play_v020.py` instead.
- **Model Development Standard** — the model-audit gate referenced by `PROJECT_ROADMAP.md`/`GOAL_TRACKER.md`. Not a file in this repo — it's Cam's cross-project modeling standard, kept outside any single project's GitHub history. Written 2026-07-15; still a work in progress. Not all models comply yet — see the §0 note above.

---

## 3. Port Architecture

| Service | Port | Start command |
|---|---|---|
| Analytics API | 8000 | `python -m uvicorn src.api.app:app --host 0.0.0.0 --port 8000` |
| DFS API | 8002 | `.\start_backend_api.bat` |
| Analytics frontend | 5174 | `cd frontend_analysis && npm run dev` |
| DFS frontend | 5173 | `cd frontend && npm run dev` |

Both frontends proxy `/api` to their respective backends via vite config. Never hardcode `localhost:8000` in frontend JS — use relative `/api/` paths.

---

## 4. Key API Endpoints (port 8000)

| Endpoint | Purpose |
|---|---|
| `GET /api/positional-evaluator` | Slider/manual tool — takes down, distance, yardline_100, clock, score_differential, timeouts, n_sims; returns EP, KEP, ranked concept delta_kep list |
| `GET /api/games/{game_id}/positional-eval` | Play-stream — scrapes ESPN PBP for a live game_id, runs evaluator on each play, returns `evaluations[]` array |
| `GET /api/games/{game_id}/play-by-play` | Raw PBP plays for WP timeline |
| `GET /api/games/{game_id}/fourth-downs` | 4th-down decision rows |
| `POST /api/fourth-down-evaluate` | Evaluate a 4th-down situation |
| `GET /api/historical/week1-2025` | Returns 16 Week 1 2025 game records from nfl_data_py (schedule, scores) |
| `GET /api/historical/plays/{game_id}` | Returns scrimmage plays for a game with home_kep + ep pre-computed per play |
| `GET /api/historical/suggest-lines` | One-step KEP: evaluates current state, returns top N concept chains (principal variations, depth 2) |

All endpoints fall back gracefully: 502 = ESPN fetch failed; 404 = route not found. Frontend `safeFetch` catches non-ok HTTP and uses mock data instead.

---

## 5. Chess Evaluator System (KEP/EP)

**Concept:** Measures positional value in "Kickoff-Equivalent Points" — the score margin at kickoff that yields the same win probability as the current game state. Analogous to centipawns in chess.

**Data flow:**
1. `PositionalEvaluator.evaluate(state)` — rolls out N_SIMS drives from the given state
2. Each drive uses `first_play_concept` classification (from `game_engine.py` snapshot attributes) to bucket drives by opening call: Run / Screen / Short / Medium / Deep
3. `KEPConverter.wp_to_kep(wp)` inverts the WP model (isotonic regression ensures monotonicity) to get KEP
4. Returns `ep_start`, `kep_start`, and per-concept `delta_kep` (concept mean KEP − baseline KEP)
5. All positive delta_kep = concept improves position; all-zero = ceiling/floor hit (blowout — doesn't matter)

**Frontend (GameSummary.jsx chess tab):**
- Chart: Recharts dual-line (KEP solid, EP dashed) over the play timeline
- Detail panel: selected play's EP, KEP, natural-language advantage string, and PLAY CONCEPT RECOMMENDATION ranked bars
- Concept fetch: fires on `[selectedPlayId, activeTab, chessEval]` change; falls back to last evaluation when selectedPlayId is null (race condition guard)

**One-step KEP (current implementation, Historical Lab):**
- `evaluate_one_step()` simulates exactly one scrimmage play per lane, then computes KEP of the resulting state
- Three cases: mid-drive (possession kept, no score) → WP→KEP from new state; scored/terminal → `_drive_end_kep`; turnover → opponent's KEP from exact turnover yardline
- `mean_next_state` captured per concept for chaining into suggested lines
- `suggest_lines()` chains `evaluate_one_step` calls: best concept step 1, then evaluates from `mean_next_state` for step 2 etc.

**KEP reference frame:** home-team reference. `home_kep = kep_off if posteam == home_team else -kep_off`. Positive = home team winning. Sign never flips on possession change — smooth Lichess-style charts, no sine waves.

---

## 6. Mock Data (frontend_analysis/src/api.js)

Five mock games are defined for development without live ESPN:

| game_id | Matchup | Scenario |
|---|---|---|
| live_game_1 | PHI vs KC | Q4 close game |
| live_game_2 | SF vs LAR | Q3 medium leverage |
| live_game_3 | DET vs GB | Q4 critical |
| live_game_4 | MIA @ BUF | **Blowout** — BUF +21, Q3 4:00; KEP arc from 0→+24 ceiling |
| live_game_5 | LAC @ KC | **10-14pt game** — KC +7, Q4 2:00 late-game tension |

live_game_4 and live_game_5 were added to demonstrate KEP ceiling/floor behavior and mid-range leverage scenarios.

---

## 7. Test Commands

Runtime is Python **3.12.13** via `venv/` (pinned in [requirements.txt](requirements.txt); rebuilt from 3.8 on 2026-07-15). **Activate `venv` (or invoke `venv\Scripts\python.exe` directly) before running ANY command below or importing `src.nfl_sim.*` — a bare `python`/`python3` on PATH can resolve to an unrelated system install (see §8 gotcha) that silently fails on the pinned model artifacts.**

```bash
# Full test suite (test_positional_evaluator, test_efsd_evaluator, test_live_pipeline, test_dna_blender, test_week_roster) -- 92 tests
python -m pytest tests/ -v

# Run all positional evaluator tests — must stay green through any game_engine.py edit
python -m pytest tests/test_positional_evaluator.py -v

# 2026 DNA blending pipeline tests (taper_weights/steady_state_blend/blend_player_dna,
# interpolate_curve/resolve_rookie_curves, rolling_average, classify_zone) — 30 tests, pure functions
python -m pytest tests/test_dna_blender.py -v

# Weekly DNA refresh (run by hand after each week's games -- NOT scheduled/automated)
python scripts/roster_management/refresh_weekly_dna_v_0_1_0.py <year> <completed_week>

# Rebuild the gitignored DNA files from scratch (after a fresh clone, or any time you
# don't trust what's on disk) -- see §10. Does NOT touch trench_dna.json.
python scripts/roster_management/regenerate_dna_v_0_1_0.py

# Syntax check after any game_engine.py edit (cheap, do this first)
python -m py_compile src/nfl_sim/game_engine.py

# Full batch audit — the way to measure any clock/pace/passing-model change at scale
# (64 games x 1000 iterations; writes docs/audit/v_0_2_0_audit/sim_post_cal_metrics.json,
# compare against historical_eda_metrics.json in the same dir). Takes several minutes.
python scripts/simulation_runners/run_weeks_1_to_4_2025.py

# Rebuild the real-2021-2025 comparison baseline (only needed if the definition of a
# tracked metric changes, not for every game_engine.py edit)
python scripts/eda/run_historical_eda.py

# Single-game (N=1) play-by-play dump for manual clock/mechanic verification —
# non-interactive, unlike the legacy scripts/play_by_play_runner.py
python scripts/print_play_by_play_v020.py

# Human-readable football-narrative play-by-play for a single game (N=1) —
# "Mahomes complete to Kelce for 12 yards" instead of a raw state-diff log
python scripts/print_readable_play_by_play.py [AWAY] [HOME]

# Regression test: field position must move by exactly the recorded gain on
# any possession-unchanged normal play. Rerun after any change touching
# quarter transitions, clock handling, or possession-switching logic — this
# is what caught the Round 13 quarter-transition state-corruption bug.
python -c "from scripts.audit_play_continuity import audit; audit(n_games=1000, max_steps=250)"

# Rebuild clock_pace_v_0_1_0/pace_pools.json from real pbp data (only if the grid's
# bucket definitions change, e.g. quarter/time-window/margin-tier boundaries)
python scripts/eda/analyze_clock_pace_grid.py

# Start analytics API
python -m uvicorn src.api.app:app --host 0.0.0.0 --port 8000

# Quick endpoint smoke test
curl "http://localhost:8000/api/positional-evaluator?down=1&distance=10&yardline_100=75&clock=1800&score_differential=0&posteam_timeouts=3&defteam_timeouts=3&n_sims=100"
```

**Standard verification sequence for any `game_engine.py` change:** `py_compile` → `pytest tests/test_positional_evaluator.py` → smoke test (small N via `NFLGameEngine(team, team, year=2025, N=20-500)` directly, or `BatchSimulator`) → full batch audit if the change could plausibly affect play count, pace, or completion rate.

---

## 8. Fragile Areas / Gotchas

- **Bare `python` on PATH is NOT this project's environment — activate `venv/` first.** Confirmed 2026-07-22: a shell that hasn't activated `venv/` can resolve `python` to an unrelated system install (e.g. a Windows-Store Python 3.8 stub with numpy 1.24.4/joblib 1.4.2) instead of this repo's `venv/` (Python 3.12.13, numpy 2.5.1, joblib 1.5.3 — matches `requirements.txt` exactly). Symptom: `ModelRegistry()` / `ChaosModelV010.__init__` raises `ModuleNotFoundError: No module named 'numpy._core'` when `joblib.load()` hits a `.joblib` artifact pickled under numpy 2.x — this looks like a real numpy/joblib version-mismatch bug but isn't one; it's purely a wrong-interpreter problem and disappears once `venv\Scripts\python.exe` is used. There's also a `venv_py38_old/` in the repo root (retired 3.8 fallback, not yet deleted — see §0/§2) that some `py -3.8` invocations could land on and produce the same symptom. Always confirm the active interpreter is `venv/` before treating a model-load failure as a real bug. **Cost of getting this wrong (2026-07-22): an entire session's worth of `game_engine.py` changes (Phase 7) got verified via isolated logic-replication against real data instead of the actual engine, because this wasn't discovered until near the end — the isolated checks were all correct, but a real end-to-end batch run (only possible once `venv/` was used) immediately surfaced an unrelated real regression (see §11.14) that no amount of isolated-function testing could have caught. If a model-load error blocks verification, fix the interpreter, don't fall back to a weaker verification method and move on.**
- **A field silently merged into a DNA file by a one-time script (not the file's regular builder) will get wiped the next time that file is fully rebuilt, with no error.** `coach_dna.json`'s `"proe"` field is the concrete example (2026-07-22, see §0/§11.14): merged in from `coordinator_atlas.json` on 2026-07-16 by a script that ran once and was never re-run; `coach_dna.json`'s actual builder (`R/scripts/build_dna_registry_v_0_1_0.R`'s `build_coach_dna()`) never produced that field itself. Rebuilding `coach_dna.json` from scratch silently dropped it — no crash, no warning, `get_coach_proe()` just started returning `0.0` (a valid-looking default) for every team. **General lesson: before doing a full rebuild of any generated data file, check whether every field in the CURRENT file is actually reproduced by the script about to run it, not just assume "rebuild = safe, it's the same file."** `merge_coach_proe.py` is now a permanent, rerunnable fix for this specific case.
- **A 2-3-matchup manual spot-check is not a substitute for the full official audit, in either direction — it can both invent regressions that aren't real and hide the true magnitude of ones that are.** Round 17 (2026-07-22, §11.14) hit both failure modes in the same session: (1) `completion_rate_pct` looked elevated (~68%) from a KC/BUF-only spot-check (both real-world elite-accuracy QBs, not representative) — the full 64-game audit showed it was fine (63.89% vs. 64.40% target) all along, a phantom regression. (2) `sacks_per_game` is defined per-team everywhere in this codebase, but an early spot-check summed both teams before dividing by game count — a 2x measurement error that made a real +32.5% drift look like +165%, leading to a recalibration that badly overcorrected (confirmed only once the full official audit ran with the bad value in place and returned `sacks_per_game: 1.133` against a `2.41` target). **General lesson: run `scripts/simulation_runners/run_weeks_1_to_4_2025.py` (or equivalent full-scale audit) BEFORE concluding a regression exists or attempting any recalibration — not after, as a "let's confirm the fix" afterthought.** It's the only measurement in this codebase proven to match the real historical convention key-for-key (`docs/audit/v_0_2_0_audit/sim_post_cal_metrics.json` vs. `historical_eda_metrics.json`, same keys, direct comparison).
- **WP model inversion at kickoff (t=3600):** Known calibration quirk; KEP values at game start may be slightly off. Clamp is ±24. Does not affect relative concept ranking.
- **CORS:** `app.py` allows origins 5173 and 5174. If ports change, update `ALLOWED_ORIGINS` in app.py or set env var.
- **selectedPlayId race condition:** GameSummary.jsx initializes selectedPlayId=null; `loadAllGameDetails` is async. The concept useEffect uses `?? evals[evals.length-1]` fallback to handle null selectedPlayId when chess tab is opened before data loads.
- **Recharts programmatic clicks:** Native DOM MouseEvents dispatched to SVG elements do NOT trigger Recharts' synthetic event handler. Use React state changes (setSelectedPlayId) instead.
- **InDevelopment.jsx (DFS site):** Chess code was removed. There is no `id: 'chess'` page in pagesConfig.js — do not add one without explicit instruction.
- **HistoricalLab.jsx:** Marked "TESTING LAB — REMOVE BEFORE LAUNCH". Routed as `historical-lab` in pagesConfig.js. Remove from pagesConfig.js + App.jsx before going to production.
- **`receive_2h_ko` missing from nfl_data_py:** This field is not in the PBP dataset. `week1_2025.py` defaults it to 0.0 for all plays. Do not try to populate it from nfl_data_py.
- **`src/nfl_sim/data/` must not exist as a Python package:** The YAC model's path-walker traverses up from `__file__` looking for the first directory named `data/`. A `data/` subpackage under `src/nfl_sim/` intercepts the walk and breaks the YAC model. Keep `week1_2025.py` directly in `src/nfl_sim/`, never in a `src/nfl_sim/data/` subdir.
- **HistoricalLab chart click UX (unresolved):** Dot onClick handlers are attached directly to SVG circles in the `dot` render function AND via `activeDot onClick`. Programmatic SVG dispatch does not trigger these (Recharts limitation). Real user clicks should work but behavior was not fully verified. If clicks still feel unreliable, replace with a scrollable play list below the chart — each row is a clickable play showing clock, situation, and KEP. This was the recommendation in the original AGENTS.md note about Recharts.
- **week1_2025.py score fields:** `posteam_score` and `defteam_score` are the running scores per play. `home_score`/`away_score` from nfl_data_py PBP = FINAL game score stored on every row — do NOT use these for display. Derive running home/away score as: `home_score_now = posteam_score if posteam == home_team else defteam_score`.
- **HistoricalLab silent fetch failure:** If the frontend loads before the backend warms up (~10s on first request due to model + PBP loading), the plays fetch fails silently (`.catch(() => {})`), leaving the chart in "No data" state. Fix: hard-refresh the browser after backend is ready. Permanent fix: add error state + retry button to HistoricalLab.
- **Suggested lines direction:** Lines are converted from offense-reference KEP to home-team reference before display. Home possession → sort descending (optimizing for +). Away possession → sort ascending (optimizing for −). BEST badge = play most beneficial to the possessing team in home-KEP terms.
- **Quarter-transition logic (`game_engine.py`) — moved 2026-07-21, was `_run_clock`, now `_resolve_quarter_transitions`.** Was BROKEN for years — `end_game` was computed by re-reading `self.quarter` AFTER it had already been incremented for Q3→Q4 transitions, so those lanes immediately satisfied `quarter >= 4` and the game was marked over the instant Q4 *began*, never simulating any of the 4th quarter. Fixed by capturing `pre_quarter = self.quarter.copy()` before any mutation and using that for both the increment-check and the game-over check. **General lesson: any time a decision needs the PRE-mutation state of an array that gets mutated earlier in the same code path, capture a `.copy()` first — don't re-read the live array.** As of 2026-07-21, this whole block (including the OT-entry coinflip) lives in `_resolve_quarter_transitions(active)`, called exactly once per `simulate_play_step` (via `self.step_start_active`) instead of once per clock-consuming category inside `_run_clock` — the split was needed because the coinflip's own `np.random` draw was reordering relative to other randomness when the per-category `_run_clock` calls were consolidated for performance (see §11.13).
- **Quarter-transition code runs AFTER the current play's state has already been applied — it must not blindly overwrite that state (Round 13).** The Second Half Kickoff Logic used to unconditionally reset `yardline_100`/`down`/`distance`/`possession_is_away` for any lane crossing into Q3, silently discarding whatever the just-resolved play in `simulate_play_step` had actually produced (real gain, first down, everything) — on roughly half of all simulated games. This is the same root pattern as the entry above (state set earlier in a step getting stomped by code that runs later in the same step, called from a different method) — **third occurrence of this exact bug shape in this file.** Fixed by deferring to the existing `needs_kickoff`-triggered kickoff-resolution code (which already computes the correct post-kickoff state on its own, the same deferred pattern used for every touchdown/field-goal kickoff) instead of pre-empting it. Caught by `scripts/audit_play_continuity.py` — rerun this after any future change to quarter-transition, clock, or possession-switching logic. (This logic also now lives in `_resolve_quarter_transitions`, not `_run_clock` — see the entry above.)
- **Hoisting pattern for Phase-7-referenced masks:** `td_mask`, `turnover_down`, `is_oob`, `has_accepted_penalty` etc. must be initialized to `np.zeros(self.N, dtype=bool)` (or the array their branch would have produced) BEFORE the conditional block that normally sets them, not just inside it — Phase 7 (clock management) references them unconditionally later in the same `simulate_play_step` call, and if the originating branch had zero active lanes this step, an un-hoisted local would raise `NameError`.
- **`clock_stopped` must be reset to `False` BEFORE the (now single, combined) `_run_clock` call, not after:** `_run_clock` can internally set `clock_stopped=True` for lanes that cross the two-minute-warning threshold during that call. Code that unconditionally does `self.clock_stopped[mask] = False` *after* the call stomps on that. Caught as a real bug in the `regular_clock` and OOB `normal_oob` branches — both reset to `False` before the call.
- **Any XGBoost model wired in via the raw-booster `inplace_predict` pattern (bypassing sklearn's `predict_proba`) must pass `iteration_range` if the model was trained with early stopping** — otherwise it silently uses more (worse) trees than what was actually validated. Found 2026-07-21 affecting Gate 4 and Gate 2b (`src/nfl_sim/models/chaos_v_0_1_0/inference.py`) and the 4th-down conversion model (`src/nfl_sim/models/fourth_down_conversion_v_0_1_0/inference.py`) — all three now store `iteration_range = (0, model.best_iteration + 1)` at load time and pass it on every `inplace_predict` call. Gate 2 has no `best_iteration` (not trained with early stopping) and needs no equivalent — check `getattr(model, 'best_iteration', None)` before assuming a new gate needs this.
- **`fumble_rate` is not a field in any DNA file** (`rb_dna.json`/`wr_dna.json`/`te_dna.json` — confirmed via grep, 2026-07-21). A per-carrier fumble-probability multiplier keyed off this field existed in `game_engine.py` for an unknown amount of time and was mathematically always exactly 1.0 for every player, every play — removed. General fumble probability is now a flat, real-data-adjacent constant per play type (not player-differentiated); sack-fumbles use a separate flat real-data rate (`SACK_FUMBLE_RATE = 0.1253`). Don't reintroduce a per-player fumble-rate DNA lookup without first adding the underlying field to the DNA schema.
- **The completion-probability model's `b0` mathematically cancels out** in the open-field catch path (`game_engine.py`, `is_open` branch). `delta_wr` is built as `logit(zone_baseline) - logit(sigmoid(b0 + b1*adot_val))`, and since `logit(sigmoid(x)) = x`, the `b0` terms cancel completely when substituted into `logit_p = b0 + b1*ay_val + delta_wr + sep_bonus`. **Do not try to "calibrate completion rate by shifting b0" — it's a no-op.** Use a separate, explicitly-labeled probability-space offset instead (see `OPEN_FIELD_CALIBRATION_OFFSET` in the code, added clock_physics_v020 Round 7).
- **QB CPOE must be applied in probability space, not logit space:** CPOE is itself a probability-space quantity (percentage points of completion rate over expected). Adding it inside a logit sum before the sigmoid compresses its effect (~1/3 of intended magnitude at typical completion rates). Apply it as `sigmoid(logit_p) + cpoe/100` after the sigmoid, matching the contested-catch path's pattern, not `sigmoid(logit_p + cpoe/100)`.
- **`docs/boxscores/` is not git-tracked** (confirmed: 0 files under version control there) — local-only generated output, regenerated by `run_weeks_1_to_4_2025.py`. Don't assume it reflects what's on GitHub.
- **`player_df` (from `BatchSimulator.run_batch`) can have multiple rows sharing the literal name `"Defense"`** — both teams' DST unit uses this as a generic placeholder `Player` name. A naive `groupby(['game_id','Player'])` will treat different teams' defenses as duplicates. Always include `Team` in the groupby key when aggregating player stats, or use the existing `StatAggregator` rather than reimplementing aggregation ad hoc.
- **The pace grid (`analyze_clock_pace_grid.py`) must exclude out-of-bounds-terminated plays** from its general pool — OOB is modeled as its own explicit mechanism in `game_engine.py` (roll a probability, draw from the pace pool, subtract a reduction). If OOB plays are also blended into the general pool, the OOB timing effect gets double-counted. This was tried both ways in clock_physics_v020 (see Round 1 vs. Round 2 in the audit doc) — the current, correct state excludes OOB from the pool.
- **`train.py` in `air_yards_v_0_1_1/` and (as of Round 9, historical only) `yac_model_v_0_1_1/` do NOT reproduce the currently-deployed models — and this pattern extends to `rush_yards_v_0_1_0` too.** All three models' `metadata.json` were stamped `"version": "V.0.2.0"` and ship zone-specific `{primary,redzone,goalline}_*` artifacts using DNA-filter features (`cpoe_by_filter`/`target_share_by_filter`/`carry_share_by_filter`) — but the training scripts that originally produced those zone-split artifacts were never committed to this repo. Not lost history: `git ls-files src/nfl_sim/models/` returns zero tracked files, period — nothing under `models/` or `data/` (gitignored outright) has ever been in git, so there was nothing to find. `air_yards_v_0_1_1/train.py` and `yac_model_v_0_1_1/train.py` are leftovers from an earlier, single-model (non-zone-split) generation with a different feature set — their EDA outputs (`docs/eda_outputs/{air_yards,yac}/evaluation_v_0_1_1/`) confirm they really were run once, just superseded. `rush_yards_v_0_1_0/` doesn't even have a leftover script. **`yac_model_v_0_1_1/train_zone_split.py` (Round 9) is now the correct, working replacement for YAC** — it reproduces the deployed zone-split architecture and is safe to rerun. `air_yards_v_0_1_1/train.py` and `rush_yards_v_0_1_0` still have no working equivalent — don't run `air_yards_v_0_1_1/train.py` expecting it to refresh the live model, it would silently replace it with a differently-shaped one. See clock_physics_v020 Rounds 8-9 / AGENTS.md §11.7.
- **`game_engine.py`'s YAC computation bypasses `YACModelV011.predict()` entirely** — it calls `self.registry.yac_model._boosters[zone].inplace_predict(X_yac_zone)` directly with a manually-built feature matrix. The dead `receiver_dna`/`qb_dna` injection block that used to sit at init (comment: "Dynamically inject V.0.2.0 player DNA") was **removed in the repo-audit dead-code pass** — it set attributes nothing read (`YACModelV011.predict()` reads `self.skill_dna`/`self.qb_dna`, different names, and is never called from the hot path). The real DNA lookups happen via `self.precomputed_skill_target_share` etc.
- **`off_score`/`def_score` naming trap — RESOLVED 2026-07-21.** These field names used to mean away/home in `game_engine.py`'s `get_game_summaries` and `batch.py`'s legacy worker (both always away/home in practice, since `team_off`/`team_def` are always passed positionally as `away_team`/`home_team`) — genuinely mislabeled, since nothing there is real offense/defense (that flips every possession change). Renamed to **`away_score`/`home_score`** everywhere in the simulation path: `game_engine.py`, `batch.py`, `app.py`, and the 4 live `scripts/simulation_runners/*.py` + `diagnose_scoring_efficiency.py`. **`src/live/` (`espn_adapter.py`, `posting_policy.py`) deliberately keeps `off_score`/`def_score`** — there it correctly means real, dynamic possession-based offense/defense (`posteam_score`/`defteam_score`), a different and legitimate concept; confirmed zero import/data crossover between `src/live/` and the simulation path (verified via `app.py`, the one file that touches both — it does so through non-colliding field names, `off`/`def` for the live positional-eval endpoint vs. `away_score`/`home_score` for sim output). `legacy/` left untouched (archival, still uses the old names). **Known residual risk:** `data/interim/sim_results_2025_{games,players}.parquet` (generated by `run_full_season_sim_2025.py`, gitignored, not regenerated by this change) may still have OLD column names baked in on disk if it was last generated before this rename — regenerate it before assuming `app.py`'s `/api/week_projections` (or anything else reading that cache) sees the new names.

---

## 9. EFSD — Next Iteration of KEP (planned, not yet built)

**Problem with current KEP:** Inverts the WP model to find "what kickoff margin yields this same WP?" — it's a nonlinear rescaling of WP. Circular. WP defines WP.

**EFSD = Expected Final Score Differential.** Train an XGBoost regressor to predict `final_home_score - final_away_score` from game state features. The model output IS the metric — no WP in the chain, no inversion step.

**Training recipe (modeled on `train_positional_ep.py`):**
1. Pull multi-season PBP from nfl_data_py
2. Join each play to its game's final score margin (home − away)
3. Features: same as WP model (`score_differential`, `game_seconds_remaining`, `down`, `ydstogo`, `yardline_100`, timeouts, `receive_2h_ko`)
4. Target: `final_home_score - final_away_score` (home-team reference)
5. Train XGBoost regressor. Output file: `src/nfl_sim/models/efsd_v_0_1_0/`

**Key properties:**
- Continuous output despite discrete NFL margins (regression = weighted average, not mode)
- No artificial ±24 ceiling
- Time-invariant bands: EFSD already encodes clock; same EFSD in two different game states = genuinely equivalent expected outcome
- If output looks "stepped" around 3/7/10/14, apply Gaussian target smoothing (σ ≈ 1.5 pts) during training

**Preliminary band hypothesis (validate empirically on holdout set):**
| Band | EFSD | Football meaning | Target win rate |
|---|---|---|---|
| Even | ±3 | Within a field goal | ~50% |
| Advantage | ±3 to ±8 | One possession edge | 60–70% |
| Clear advantage | ±8 to ±16 | Two possessions | 75–85% |
| Decisive | ±16+ | Three+ possessions | 90%+ |

Validation: bucket holdout predictions by EFSD range, check actual win rates. Adjust band boundaries until calibrated.

---

## 10. Generated Artifacts (do not commit to source control)

**Note (2026-07-15):** all 11 trained model families under `src/nfl_sim/models/` (joblib + xgboost-native json) are now git-tracked as of the repo audit — this section is about *other* generated artifacts (data caches, EDA outputs, docs) that remain intentionally gitignored, not the models themselves. See §0.

**Note (2026-07-22):** `data/dna/qb_dna.json`/`rb_dna.json`/`wr_dna.json`/`te_dna.json`/`skill_dna.json`/`coach_dna.json` and `data/current_rosters/*_traits_2026.json` are now gitignored too — large and mechanically regeneratable via `scripts/roster_management/regenerate_dna_v_0_1_0.py`. This is a repo-size decision only, not a privacy one: the repo is public and stays public (site + 4th-down bot), so gitignoring data doesn't hide methodology — the code computing it is already visible regardless. `data/dna/trench_dna.json`, `rookie_projections_2026.json`, `team_to_coach_2026.json`, `trench_tiers_2026.json`, and the 2025 equivalents of all of these stay tracked (either small and judgment-bearing, or — `trench_dna.json` specifically — not fully regeneratable by a verified script yet, its composite z-score fields depend on an EDA CSV chain, see §0's Phase 1 bullet).

- `src/nfl_sim/models/positional_ep_v_0_1_0/positional_ep_model.json` — trained model (already committed, update only on retrain)
- `src/nfl_sim/models/clock_pace_v_0_1_0/pace_pools.json` + `metadata.json` — generated by `scripts/eda/analyze_clock_pace_grid.py`; large (~465KB), do not hand-edit
- `docs/audit/v_0_2_0_audit/sim_post_cal_metrics.json` — regenerated every time `run_weeks_1_to_4_2025.py` runs; treat as ephemeral output, not source of truth (the doc's narrative in `docs/audit/clock_physics_v020/README.md` captures the meaningful before/after deltas)
- `docs/audit/v_0_2_0_audit/historical_eda_metrics.json` — regenerated by `run_historical_eda.py`; only changes if the real-data pull or metric definitions change
- `docs/boxscores/week_1..week_18/` — generated per-game boxscores, confirmed NOT git-tracked
- `data/interim/sim_results_2025_{games,players}.parquet` — the full 18-week/272-game/N=1000 season dataset (Round 16), regenerated by `scripts/simulation_runners/run_full_season_sim_2025.py`. This is what `src/api/app.py` serves by default (`/api/week_projections`, `/api/games`, etc.) — regenerate after any `game_engine.py` change that should be reflected in the frontend tools. Also not git-tracked (`data/` is gitignored). `data/interim/week_N_full_projections.json` self-heals (recomputed automatically when the parquet's mtime is newer), don't hand-edit.
- `src/nfl_sim/models/{yac_model_v_0_1_1,air_yards_v_0_1_1}/_train_cache.parquet` — cached prepared training dataframes written by each folder's `train_zone_split.py` to speed up repeat runs (objective/feature experiments); delete freely, regenerated from a live `nfl_data_py` pull on next run without it
- `frontend_analysis/node_modules/`, `frontend/node_modules/` — npm installs
- `venv/` — Python virtual environment

---

## 11. Game Engine — Clock Physics & Passing Model (clock_physics_v020, active development)

**Full history:** [docs/audit/clock_physics_v020/README.md](docs/audit/clock_physics_v020/README.md) — 7 rounds, read before touching any of this. Triggered by a roadmap review noticing "we might be missing a couple plays a game."

### 11.1 What's been built (current state of `game_engine.py`'s clock model)

- **Pace model:** `_sample_pace_runoff()` bootstrap-samples snap-to-snap runoff from real empirical pools (`clock_pace_v_0_1_0`), keyed by (quarter/time-window × score-margin tier), replacing an old flat `randint(18,30)` that was measurably wrong (real running-clock mean is ~32-38s, not ~23.5s). Currently has a cumulative **+2s calibration nudge** baked into the return value (Rounds 3-4), justified by residual play-count overshoot, not a real-data measurement.
- **Out-of-bounds:** explicit per-play roll (situational rates by quarter/score-margin, not flat — see the doc's Round 4 table), with two regimes: outside the last 2:00 of Q2 / 5:00 of Q4, OOB shaves 1-5s off the normal pace draw (play-type-specific); inside that window, full stop (clock holds until snap, confirmed via web search against the actual NFL rule).
- **Two-minute warning:** automatic free stoppage, clamped exactly to 2:00 remaining the first time either quarter's clock would cross below it. `self.two_minute_warning_used` resets on quarter transition.
- **Squeeze plays: removed (Round 10).** Used to let teams rush the snap to deliberately fit an extra play in before the warning (Cam's original explicit spec — Q2 flat 95%, Q4 leading 0%/trailing 2+ scores 100%-50%/tied-or-trailing-1-score 30% baseline). Cam decided this was "greedy for an extra play" and had it removed — regular-clock plays in the warning zone now just use the normal `_sample_pace_runoff` pool, which already reflects real hurry-up pace via its own fine-grained time-window buckets. See §11.8.
- **Clock-stop bug fixes:** scoring plays, turnover-on-downs, punts, and field goals now correctly stop the clock (several of these were silent bugs before this session — see §8 fragile-areas entries).
- **The Q4 game_over bug** (see §8) meant the engine never simulated a 4th quarter before this session. This was the single largest fix in terms of impact.

### 11.2 What's been built (completion-rate / passing model, Round 6-7)

- **Screen catch rate recalibrated:** was hardcoded 80%(RB)/95%(WR-TE); real data showed 82.68%/80.01%. Now 83%/80%. The 95% figure was the dominant driver of the whole completion-rate overshoot.
- **QB CPOE fixed to apply in probability space**, not logit space (see §8 — this was silently compressing QB skill differentiation to ~1/3 of intended effect).
- **Baseline calibration offset** (`OPEN_FIELD_CALIBRATION_OFFSET = 0.075`) added to close the remaining gap to the confirmed real 2025 completion-rate target (**64.40%**, computed from real season QB stats, weighted CMP/ATT). Landed at 64.15% — within 0.25pp.
- **Deep-zone completion and zone distribution (screen/standard/deep %) were already accurate** — confirmed via real-data comparison, not touched.

### 11.3 Known interaction: fixing completion rate moved play count again

Fixing the completion-rate bugs (Round 7) increased offensive/total snap overshoot back up (+4.4%→+6.8%, +3.3%→+6.2%) after Rounds 1-6 had brought it down to +2-4%. Mechanism: fewer completions → more incomplete passes → more plays needed per drive to reach the same eventual outcome. **Not a bug in either fix** — both are independently correct — but it means the play-count tuning is not settled. Re-measure before assuming Round 4-6's pace calibration is still appropriate. (Squeeze-play, mentioned here in earlier drafts, was removed in Round 10 — see §11.8.)

### 11.4 Deferred to v0.4.0 (sacks)

Sacks are empirically fixed as of Round 15 (net sacks/game 2.410 vs. real 2.41, +0.2% — see §11.12) via a calibration multiplier, not an architectural fix. The deeper architectural work is still deferred to v0.4.0, but **the plan changed 2026-07-13, and it's important to be precise about which trench file it touches:**

- **Gate 2 (sacks) already uses `trench_dna.json`'s raw metrics, not `trench_tiers_2025.json`.** `sack_rate_allowed`, `def_pressure_rate`, `def_sack_rate` (Cam's stated priorities) are already live Gate 2 features today (`game_engine.py`, inside the per-play sack-feature block) — no migration needed there. The originally-planned "add explicit trench-tier (pass_block_tier vs. pass_rush_tier) matchup to chaos gate 2" is superseded; Gate 2 was never going to use the tier file anyway.
- **`trench_tiers_2025.json`'s 1-5 tier grades (unclear provenance — see `docs/sims/inputs/README.md` §4.1) feed a *different* part of the engine entirely**: the static run-block/pass-block multipliers computed once at engine `__init__` (`run_diff_*_off`/`pass_diff_*_off` from `run_block_tier`/`pass_block_tier`/`run_stuff_tier`/`pass_rush_tier`). **Correction (2026-07-16):** the previously-claimed `qb_cpoe_z` "box density" feature "re-read live by the rush-yards model" was never actually true — traced the real `X_run_zone` feature stack directly and found `box_density_z` (fed from `qb_cpoe_z`) was dead code, never reaching the model; removed in the 2026-07-16 dead-code pass along with 4 other dead rush-yards z-scores. **This is the part Cam wants moved onto raw `trench_dna.json` metrics instead** (or an equivalent continuous replacement) — separate work from the sacks model. **Scoping finding (2026-07-16): `trench_dna.json` has no run-blocking/run-stopping fields at all** (it's 100% pass-game data — sacks, pressure, air yards) — the pass-side of this multiplier has a plausible raw-data replacement, but the run-side needs genuinely new data (e.g. yards-before-contact/stuff-rate) that doesn't exist anywhere in the current DNA files yet. Not a simple file swap; see `docs/sims/inputs/README.md` §5 bug #1.
- **`avg_time_to_throw_sec`/air-yards variables** are accepted as a proxy for time-to-throw/time-to-pressure in the absence of real pressure-timing data (see the TTT→air-yards idea, `FUTURE_DEVELOPMENT.md`).
- A **new candidate Gate 2 feature, `blitz_rate`, is real and buildable** (via `nfl_data_py.import_ftn_data()`'s `n_blitzers` field, 2022+) but deliberately deferred until more seasons accumulate (~2027-2028) — see `FUTURE_DEVELOPMENT.md`'s "Trench Data Pipeline" section for the full investigation.

Bundled with the injury/administrative-stoppage work below since both are v0.4.0 scope.

### 11.5 Ruled out (don't re-investigate without new evidence)

- OOB reduction magnitude being wrong — confirmed real via play-type-controlled comparison (runs save ~2.18s, passes ~4.24s going OOB), not a confound.
- Yards-per-play / big-play rate being systematically off — checked directly (pre-Round-8), both matched real closely; the completion-rate problem was the actual issue at the time, not yardage. **Superseded by Round 8**: yards-per-*completion* (a different cut) is off — YAC under-prediction, see §11.7. This wasn't visible until completion rate was fixed in Round 7.
- Drive count being wrong — matched real almost exactly (21.31 vs 21.70/game); the play-count overshoot lives entirely in plays-per-drive, not drive frequency.
- "b0 shift" as a completion-rate calibration lever — mathematically a no-op, see §8.

### 11.6 Deferred, parameters agreed (throwaway mechanism)

Target: ~5% of all dropbacks should be throwaways, 75% pressure-related / 25% clean-pocket-no-good-look. Real-data EDA to pin down the exact rate hit a wall — nflfastR's play descriptions only inconsistently flag throwaways in text, undercounting badly with no reliable numeric heuristic to fill the gap. Current mechanism (`is_throwaway = real_sacks & rand<0.20`) only fires as a sack-escape — needs a clean-pocket path independent of `sack_prob` added.

### 11.7 Rounds 8-9: YAC model under-predicts — diagnosed and fixed

Full writeup: Rounds 8-9 in [docs/audit/clock_physics_v020/README.md](docs/audit/clock_physics_v020/README.md). Summary:

- Round 8 diagnosed a −14.4% overall YAC shortfall (worst on deep balls and screens) plus an opposite-direction goalline/redzone over-prediction, and found the deployed YAC/air-yards/rush-yards models all have the same problem: no training script in the repo reproduces them (`yac_model_v_0_1_1/train.py` and `air_yards_v_0_1_1/train.py` train a different, non-zone-split architecture; `rush_yards_v_0_1_0` has no training script at all). Turned out this isn't lost history — `git ls-files src/nfl_sim/models/` returns zero tracked files, ever; nothing under `models/` or `data/` was ever committed, so there was never a commit to find.
- Round 9 wrote a new script, `src/nfl_sim/models/yac_model_v_0_1_1/train_zone_split.py` (live `nfl_data_py` pull + `data/dna/*.json` joins, 2020+, `GroupShuffleSplit` by `game_id`, 70/15/15), and found **two compounding root causes, neither of them a feature gap**:
  1. **Objective function.** The model (and the orphaned original) used `reg:absoluteerror` (median-targeting). YAC is right-skewed, so a median-fit model systematically undershoots the mean — and the mean is what season-long aggregates depend on. Switching to `reg:squarederror` closed almost the entire gap on its own (primary zone: −30.9% → −0.9% on held-out test data).
  2. **Noise-floor inflation**, only visible once the model itself was fixed: `game_engine.py`'s post-model noise injection used a flat minimum scale (6.0 yards) regardless of context. Combined with flooring negative draws at zero, this mechanically inflates the mean wherever real YAC is small — goalline (~0.9-yd mean) got hit hardest (+237% over real, barely moved by the model fix alone), redzone less so, primary negligible. Fixed by scaling the noise to the play's own predicted YAC (`base_scale = max(0.75, yac * 1.25)`) instead of a flat constant.
  3. One experimental feature added and kept: `room_after_catch` (`yardline_100 − air_yards`, i.e. distance from the catch spot to the goal line) — meaningful feature importance (13-88% depending on zone), helps the goalline/redzone case specifically.
- Player-skill traits (elusiveness, broken_tackle_rate, top_speed_mph, avg_separation_yds) were deliberately left out this round — checked NGS coverage first (`avg_separation` has solid history back to 2016 for qualifying pass-catchers; `top_speed_mph` isn't in the standard NGS dataset at all and its `skill_dna.json` source is unclear) — situational-only features for now, `avg_separation_yds` flagged as worth revisiting given the coverage check came back better than expected.
- **Result:** overall YAC −14.4% → −1.6%; pass yards/game −13.0% → −6.2%. **But offensive/total snap-count overshoot did NOT improve** (+6.8%→+7.2%, +6.2%→+6.6%) — the hypothesis that fixing YAC would also fix snap-count was only half right; it's now confirmed a separate problem. See §11.8 (Round 10) for what was tried next.

### 11.8 Round 10: Squeeze-Play Removed — Partial Fix for Snap-Count

Full writeup: Round 10 in the audit doc. Squeeze-play mechanic removed entirely (Cam's call — realistic hurry-up pace stays via the empirical pace pools' own time-window buckets, but no more deliberate "guarantee an extra down" roll). Measured, isolated effect: offensive snaps +7.2%→+5.4%, total snaps +6.6%→+4.7% (~2.3-2.9 fewer combined plays/game — Cam's "a couple of plays" instinct was right, more accurate than Round 5's rougher estimate). **~6-7 combined plays/game still unexplained.** Ranked list of untried options: (1) re-measure plays-per-drive vs. drive count now that completion rate/YAC/squeeze have all changed since Round 5's measurement, (2) air_yards retrain (still −4.4% to −7.3% short, same template as `train_zone_split.py`), (3) sacks recalibration (deferred v0.4.0, currently −29% under real — more sacks could shorten drives), (4) revisit the Round 3-4 pace-pool "+2s calibration nudge" now that upstream models are far more accurate than when it was added, (5) turnover rate (interception gate has a flat `*0.80` scalar, not re-checked recently), (6) administrative stoppages (deferred v0.4.0 — consumes clock without being a play, a direct lever for this exact problem).

### 11.9 Rounds 11-12: Air Yards Retrain + Sacks Diagnosis

Full writeup: Rounds 11-12 in the audit doc. Air yards: same fix pattern as YAC (`air_yards_v_0_1_1/train_zone_split.py`, live pull + DNA joins + grouped split), but the orphaned original already used mean-targeting, so no big win — pass yards/game and snap count both stayed flat. Real bug found and fixed along the way: goalline's 3-way gate has no "deep" class in training data at all (physically can't legally gain 20+ air yards from inside your own 5), which crashes both explicit and auto-inferred `num_class`; fixed with a per-zone/per-level fallback-donor system (`fallback_donor` map in the script) — **the donor must be the adjacent zone by field position** (`redzone` for `goalline`), not a blanket `primary` fallback, which was tried first and made goalline air_yards measurably worse (primary's gate has never seen a play from inside the 5 and extrapolates badly on `yardline_100`).

Sacks: diagnosis only this round, no fix yet. Three findings: (1) `data/dna/trench_tiers_2025.json` already has the `pass_block_tier`/`pass_rush_tier` matchup data the v0.4.0 plan wanted, sitting unused; (2) gate 2's `off_sack_rate_l4`/`def_sack_rate_l4` features are fed literal duplicates of season-aggregate values at inference (`game_engine.py`'s Gate 2 feature construction), not real recent-form data; (3) most promising lever — after gate 2's raw sack probability rolls, ~24% of resulting "sacks" get converted to QB scrambles/throwaways before being counted (`(1-0.05)×(1-0.20)≈0.76`), closely matching the observed −29-31% shortfall. Recommended order: test the diversion hypothesis first (cheap, no retrain), then the L4 fix, then the trench-tier retrain.
- New additive instrumentation in `game_engine.py` (same pattern as the existing Positional Evaluator Hook, §8): `last_play_pre_yardline_100`, `last_play_is_complete_pass`, `last_play_yac`, `last_play_gain`. Diagnostic script: `scripts/eda/analyze_pass_yardage_breakdown.py` (drives `NFLGameEngine` directly, not `BatchSimulator`, since the latter doesn't expose per-play internals) — rerun this after any future YAC/air-yards change to confirm the fix held.
- `yac_model_v_0_1_1/train.py` (the orphaned single-model script) is still in the folder alongside the new `train_zone_split.py` for historical reference — don't confuse the two; `train_zone_split.py` is what produces the currently-deployed `{primary,redzone,goalline}_yac_reg.joblib` files.

### 11.10 Round 13: Quarter-Transition State Corruption (found and fixed)

Full writeup: Round 13 in the audit doc. Triggered by Cam spotting repeated identical down/distance/yardline lines (different players/gains) in a readable single-game play-by-play and asking for a real audit rather than a clock-physics explanation — it was a real bug.

`game_engine.py`'s Second Half Kickoff Logic (inside `_run_clock`, called after the current play's real state has already been applied for the step) used to unconditionally overwrite `yardline_100`/`down`/`distance`/`possession_is_away` for any lane crossing into Q3 — discarding the just-resolved play's real result on roughly half of all simulated games (every game hits the Q2→Q3 boundary exactly once; the bug fires whenever that specific step is a real snap rather than a kickoff/kneel). It was also redundant: the kickoff-resolution code already computes the correct post-kickoff state on its own next call, the same deferred pattern used for every touchdown/field-goal kickoff — the fix was to stop pre-empting that and just set `needs_kickoff=True`, same as everywhere else. A second, smaller fix was needed for possession specifically: a score or turnover-on-downs landing on that same boundary play already correctly flips possession earlier in the step, so the forced "home team receives" default now only applies when possession is unchanged from the start of the step (tracked via a new `self.step_start_possession_is_away` snapshot, since `_run_clock` is a separate method and needs an instance attribute to see it).

New permanent regression test: `scripts/audit_play_continuity.py` — checks that field position always moves by exactly the recorded gain on a possession-unchanged, non-sack/interception/lost-fumble/penalty play. Went from 95 violations (200 games) to 1 (1000 games, and that 1 is a diagnostic-only artifact of stale hooks on an early-return code path, not a real bug). **Season-level snap counts barely moved** (offensive +5.2%→+5.2%, total +4.6%→+4.5%) — the bug replaced "rest of a truncated drive" with "one phantom fresh drive," which nets out close to a wash. Doesn't explain the still-open overshoot (§0/§11.8's options list stands), but was a real correctness bug worth fixing on its own terms.

New additive hooks from this round's investigation (also used by the new readable play-by-play script): `last_play_target_name`, `last_play_rusher_name`, `last_play_is_normal_play`, plus `self.step_start_possession_is_away` (not a `last_play_*` hook — a per-step snapshot consumed by `_run_clock`). New script: `scripts/print_readable_play_by_play.py` — football-narrative play-by-play for a single game (N=1), unlike `print_play_by_play_v020.py`'s raw state-diff log. Known limitation: its 4th-down-decision labeling can show a hypothetical "run/pass for N yards" when the real outcome was a kick — the decision engine appears to evaluate a hypothetical go-for-it option internally even when it decides to kick, and the narrator picks up that hypothetical. Not fixed this round (cosmetic, script-only).

### 11.11 Round 14: Sacks Diversion Hypothesis — Tested

Full writeup: Round 14 in the audit doc. New diagnostic counters `self.gross_sacks` (gate 2's raw `has_sack` roll) and `self.net_sacks` (`actual_sacks`, post-diversion) — whole-game, both teams combined, divide by 2 for a per-team-game figure comparable to real. New script: `scripts/eda/test_sacks_diversion_hypothesis.py`.

Result: gross (pre-diversion) 2.094/game vs. real 2.41 (−13.1%); net (post-diversion) 1.661/game vs. real 2.41 (−31.1%). The diversion pipeline removes 20.7% of gross — closely matching Round 12's theoretical ~24% estimate, so that part of the hypothesis holds. But the raw model is *also* independently under-calibrated (−13.1% before any diversion at all) — the "raw model is already correct, diversion is pure redundant double-counting" framing from Round 12 was too clean. Two compounding gaps, not one: removing/reducing the diversion would only close about half the shortfall; the raw model needs calibrating up too (probability-space offset, or fixing the L4 duplicate-feature bug / adding trench-tier matchup data, both still on the table from Round 12). Nothing changed in the engine this round beyond the two diagnostic counters — decision point for Cam on which lever(s) to pull next.

### 11.12 Round 15: Sacks Fixed

Full writeup: Round 15 in the audit doc. Two changes in `game_engine.py`'s Gate 2 block: throwaway-diversion rate `0.20 → 0.06` (the flat arbitrary constant; `scramble_rate`, real per-QB roster data, left untouched), plus `SACK_PROB_CALIBRATION_MULT = 1.24` multiplicatively applied to gate 2's raw `sack_prob` (same pattern as gate 4's existing `* 0.80`). Tuned empirically via `scripts/eda/test_sacks_diversion_hypothesis.py` — a first pass at 1.30 overshot (net 2.528, +4.9%), scaled down to 1.24 landed at 2.410 vs. real 2.41 (+0.2%).

Season-level cascading effects (full batch audit): sacks/game −29.1%→+0.2%; offensive snaps +5.2%→+4.2%; total snaps +4.6%→+3.8% (a bonus improvement, not the target of this round); completion rate 64.37%→64.81% (fewer zero-yard throwaway "attempts" now that more convert to sacks — minor, not chased further); pass yards/game −8.1%→−10.2% (sacks now correctly subtract real negative yardage instead of some being zero-yard throwaways — an expected trade-off tied to the already-open air-yards gap from Round 11, not a new bug).

**Deliberately deferred to v0.4.0, not fixed this round:** the L4 duplicate-feature bug (Round 12 Finding 2) and the trench-tier matchup addition (Round 12 Finding 1, the original plan's ask) — both need real per-week trench data that doesn't exist in the current pipeline (`trench_dna.json` is season-level only, no week granularity), so fixing the L4 issue properly is the same data-build as the tier-matchup work. Also still separately deferred: the throwaway-mechanism rework itself (§11.6/Round 6, target ~5% of all dropbacks with a clean-pocket path) — cutting the flat throwaway rate today moves further from that eventual target; reconcile the two when that work happens.

### 11.13 2026-07-21: `game_engine.py` optimization pass + `_run_clock` architecture split

Task brief: [docs/audit/game_engine_cleanup/README.md](docs/audit/game_engine_cleanup/README.md)'s "Optimization opportunities" section (all 8 items addressed). Full detail: [WORKLOG.md](WORKLOG.md) 2026-07-21 entry, §0 above.

- **Precompute hoisting.** New `_precompute_matchup_tables()` (called once from `__init__`, re-callable — designed so a future season-sim mode that rolls last-4-games DNA week-to-week can call it again after mutating `self.dna`/`self.rosters`, without needing new constructor plumbing): per-team/per-zone CPOE/target-share/carry-share tables (`self.zone_cpoe`/`self.zone_target_share`/`self.zone_carry_share`), team-level trench/sack-gate features (`self.team_sack_features`), and per-receiver DNA trait dicts (`self.receiver_traits`, with `self.default_receiver_traits` as the fallback matching the old inline defaults exactly). Two new module-level helpers, `_zone_scalar_vector`/`_zone_split_vector`, collapsed a by-zone lookup loop that was independently duplicated across 5 call sites (Phase 3 play-selection, sack-gate, air-yards, YAC, run-resolution) — the audit only flagged 2 of these (air-yards/YAC) as duplicated; there were 3 more. `get_safe_float` moved to module level (was redefined as a closure inside the per-receiver loop). `receivers_cache`/`rusher_cache` now store real `np.array`s instead of converting fresh (`np.array(recs)[idx]`) every play.
- **Fumble mechanics reworked** — see the §8 entry on `fumble_rate` above for the DNA-field finding. Sack-fumble rate is now `SACK_FUMBLE_RATE = 0.1253` (2020-2025 real PBP, 963/7687 sacks), replacing a mislabeled `qb_fumble_rate` variable (it read the QB's `sack_rate` DNA field, not a fumble-rate field) that was scaling a base ~5.79% rate per-QB. Cam's explicit call: use the raw real rate with no calibration dampener, unlike every other rate in this file.
- **4th-down model pandas bypass + the iteration_range bug** — see the §8 entry on early-stopped XGBoost models above.
- **`_run_clock` call-count reduction, then an architecture fix.** The audit flagged `_run_clock` as called "8-10x/step" — traced to 15 call sites total: 6 early "special play" branches (kneel/spike/kickoff/punt/FG-made/FG-missed) plus 9 Phase-7 clock-management categories, all disjoint by construction but each independently paying `_run_clock`'s fixed full-N overhead. Consolidated the 9 Phase-7 calls into 1 (`combined_seconds` array written per-category, one call at the end) — cut the redundant work, but this **broke exact seeded reproducibility**: `_run_clock`'s internal OT-entry coinflip (`np.random.rand` for who receives the new OT period's kickoff) used to fire interleaved between the 9 calls and now fired once at the end, reordering the random stream on any step touching a quarter/OT boundary (measured: ~500 of ~1178 `_run_clock` calls in a 3000-game batch). Verified statistically instead (large-N tie-rate/OT-rate A/B, consistent with noise) rather than via exact parity for this one change. **Root-caused and fixed properly**: split `_run_clock` into pure runoff + two-minute-warning mechanics (unchanged), and moved all quarter/OT-period-transition handling (including the coinflip) into a new `_resolve_quarter_transitions(active)` method called exactly once per `simulate_play_step`, after every clock call for that step, via a new `self.step_start_active` snapshot (mirrors the existing `self.step_start_possession_is_away` pattern — same "separate method needs pre-narrowing state" reason). Restores full bit-for-bit determinism (verified: same seed run twice → identical output) — the coinflip now fires from exactly one fixed point per step regardless of which of the 7 remaining `_run_clock` call sites actually caused a lane's clock to hit zero.
- **Performance verdict: modest, and don't oversell it.** Items above (precompute + Phase 7 consolidation) measured ~3.5% faster on a 3000-game batch. Directly instrumented `_run_clock`'s own cumulative cost post-consolidation: ~1% of total game time. It was never the dominant cost despite being called 8-10x/step — the call-count reduction and the architecture split were worth doing for correctness/clarity, not because they were expected to move the needle on speed. If a real performance win is wanted next, profile `simulate_play_step` itself rather than assuming `_run_clock` is still the lever.

### 11.14 2026-07-22: Model inputs rewired to `self.rosters` (Phase 7) + sacks recalibration Round 17

Full detail: [WORKLOG.md](WORKLOG.md) 2026-07-22 entry, §0 above.

- **`_precompute_matchup_tables()` and `__init__`'s `precomputed_qb_cpoe`/`precomputed_skill_target_share`/`precomputed_skill_carry_share` now source from `self.rosters` (live, per-game, weekly-blended) instead of `self.dna['qb']`/`self.dna['skill']` (static career average) or — for the zone-split `_by_filter` features specifically — the `ModelRegistry` singleton (process-level, shared/stale across every concurrent game).** `self.dna['qb']`/`self.dna['skill']` kept as a defensive fallback only if a player/field is genuinely missing from `self.rosters`. Confirmed via isolated logic-replication against real 2025 data (couldn't run the full engine at the time — didn't yet know about the `venv/` interpreter gotcha, see §8): Mahomes's live roster CPOE (3.40) vs. static career average (2.96); `adot` (roster) vs. `avg_target_depth_yds` (DNA/nested-splits name) resolved correctly, not silently falling through to the wrong source.
- **Sacks recalibration, Round 17 — including a real measurement mistake made and caught mid-round, documented here rather than quietly fixed.** First real end-to-end batch run this session (after discovering the `venv/` interpreter fix, §8) found `sacks_taken` summed across both teams at ~6.3-6.4/game. **`sacks_per_game` is a per-TEAM-per-game metric everywhere else in this codebase** (`run_historical_eda.py` computes it as `game_stats['sacks'].mean()`, explicitly labeled "Sacks per Team-Game"; `run_weeks_1_to_4_2025.py` divides by `32 teams × 4 weeks`, not `4 weeks` alone) — comparing a **combined-both-teams** figure against the per-team `2.41` target, as the first pass here did, is a 2x measurement error. Correctly halved, the real starting point was ~3.19/team (**+32.5% high, not +165%**) — a real but much smaller problem than first thought. **First recalibration attempt (`1.24` → `0.494`) was computed from the wrong (combined) baseline and badly overcorrected** — confirmed by a full official 64-game audit run *with* `0.494` in place: `sacks_per_game: 1.133` (**-53% low**, not the ~2.4 the 8-matchup spot-check using the wrong convention suggested). **Final: `SACK_PROB_CALIBRATION_MULT = 1.134`**, reached over 3 successive full official 64-game audit runs (each one used to solve for the next candidate, not just spot-checks): `0.494` → `1.133` (-53%), `1.051` → `2.235` (-7.3%), `1.134` → **`2.384` vs. real `2.4106` (-1.1%, matching Round 15's own original precision)**. Confirmed final — no more open questions on this specific number.
- **Root cause of the *original* (smaller, +32.5%) drift still not fully decomposed** — traced to this session's own Phase 1 (DNA rebuild): `trench_dna.json`'s `def_pressure_rate`/`def_sack_rate`/`sack_rate_allowed` used to silently default to a flat `0.15`/`0.06`/`0.06` for every 2025 team-game (the bug Phase 1 fixed), meaning Round 15's `SACK_PROB_CALIBRATION_MULT=1.24` was calibrated against a world with **zero cross-team variance** in those 3 features. Forcing the flat defaults back on only explained a modest fraction of the raw-model-output shift; `qb_cpoe` checked and ruled out as a further contributor. Not chased further given the actual drift turned out much smaller than first measured — an empirical recalibration was the right call here, same as Round 15's own approach.
- **Retracted: completion % is NOT elevated — the "regression" was a sample-selection artifact, not a real issue.** A small 2-3-matchup manual spot-check (repeatedly using KC/BUF, i.e. Mahomes/Allen — both real-world elite-accuracy QBs) showed ~68% and was initially treated as a second open regression alongside sacks. **The full official 64-game audit shows `completion_rate_pct: 63.89%` — within 0.5pp of the Round 6/7-calibrated target (64.40%), i.e. fine.** Don't trust a 2-3-matchup hand-picked sample as representative of league-wide calibration — this and the sacks measurement mistake above are the same underlying lesson, see the new §8 entry.


# Shared local-port policy

Before adding or changing a local development service, read the shared
[`LOCALHOST_PORT_REGISTRY.md`](../LOCALHOST_PORT_REGISTRY.md), reserve the port
there, and update the registry in the same change. Run
`powershell -ExecutionPolicy Bypass -File ../tools/check-localhost-ports.ps1`
before finishing port-related work.
