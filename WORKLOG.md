# WORKLOG.md
<!-- Reverse-chronological session ledger. Newest entry at top. -->
<!-- Format: ### [YYYY-MM-DD] Handoff from [Model] -->

---

### [2026-09-23] Handoff from Claude Opus 5.5 (Game Explorer: sticky toggles, score explorer, cross-filter, auto-refresh)

- **Active Task:** Cam's Game Explorer (ex-"DFS Simulator") UI requests, 2026-09-22/23: rename; fix + make
  Active/Inactive toggles sticky (and allow overriding IR); catch-all buckets on the score charts;
  clickable/multi-select score exploration with raw game counts, per-chart resets, cross-filtering; and
  pages auto-refreshing whenever a new 10K sim run lands. All done and verified live.
- **Files Modified:**
  - [frontend/src/pagesConfig.js](frontend/src/pagesConfig.js) + Home/DfsSummary/Optimizer/ShowdownOptimizer
    copy: "DFS Simulator" -> "Game Explorer" (route id `simulator` kept for bookmarks).
  - [frontend/src/pages/Simulator.jsx](frontend/src/pages/Simulator.jsx): `toggleActive` flips the *shown*
    (pending) state and un-queues when back at saved state (was re-queuing the same action forever);
    Score Distribution reads live `/api/game_distribution` for baseline results and refetches on `simVersion`.
  - [scripts/roster_management/dfs_status_ledger.py](scripts/roster_management/dfs_status_ledger.py) (new):
    sticky toggle ledger `data/overrides/2026/dfs_status_ledger.json`; latest entry at/before a week wins.
  - [src/data_pipeline/week_roster_v_0_1_0.py](src/data_pipeline/week_roster_v_0_1_0.py): `dfs_status=force_active`
    overrides a reserve slot (never cut/left_team); `effective_dfs_status` / `reserve_signature` (override only
    applies to the same IR stint). [apply_sheet_helpers_v_0_1_0.py](scripts/roster_management/apply_sheet_helpers_v_0_1_0.py)
    honors it; [build_week_overrides_v_0_1_0.py](scripts/roster_management/build_week_overrides_v_0_1_0.py)
    applies the ledger (`ledger=`, `team=`).
  - [src/api/app.py](src/api/app.py): roster-toggle POST records to the ledger + rebuilds later existing week
    sheets; toggle job wrapped in the run marker; `_build_game_distribution` fixed layout (`layout: 2`:
    total "<10"/2-pt/"80+", margin "≤−28"/1-pt/"≥+28", joint 5x5); new `GET /api/sim_status`.
  - [scripts/simulation_runners/sim_run_status.py](scripts/simulation_runners/sim_run_status.py) (new):
    `run_marker` ("sims running" file) + `atomic_to_parquet`; used by
    [run_week_sim_2026.py](scripts/simulation_runners/run_week_sim_2026.py) and
    [resim_games_2026.py](scripts/simulation_runners/resim_games_2026.py).
  - [frontend/src/components/GameDistribution.jsx](frontend/src/components/GameDistribution.jsx): rewrite --
    `normalizeDist` rebins old payloads from `raw`; bar click/drag/Ctrl/Shift multi-select; selections
    combine; per-chart reset + reset all; "Most common final scores" + "When X scores N" cards; raw game
    counts everywhere; cross-filtering (each chart drawn from the OTHER charts' selections, ghost outline).
  - [frontend/src/App.jsx](frontend/src/App.jsx) + [frontend/src/api.js](frontend/src/api.js): 20s
    `sim_status` poll -> refetch + REPLACE week results/projections (clears saved per-game results) when the
    version changes and no run is in progress; `SimStatusBanner`; `simVersion` to Simulator/Showdown.
  - [frontend/src/pages/ShowdownOptimizer.jsx](frontend/src/pages/ShowdownOptimizer.jsx): Game Read refetches on `simVersion`.
  - Tests (new): [tests/test_dfs_status_ledger.py](tests/test_dfs_status_ledger.py) (13),
    [tests/test_sim_run_status.py](tests/test_sim_run_status.py) (5). AGENTS.md §1 entries added.
- **Verification Performed:** `pytest tests/ -q` -> 139 passed. `vite build` clean. Every score-explorer number
  (bins, top scores, conditional, combined/cross filters) cross-checked against the dfs_week_3 parquet in
  Python -- all exact. Auto-refresh verified end to end in the browser: rebuilding banner -> quiet line + game
  cards fill; hand-made marker -> "in progress" banner; parquet mtime bump + marker removed -> "finished"
  banner in ~6s, distribution/games/projections/week results refetched, back to quiet at rebuild end, no
  console errors. Toggle checkbox round-trip verified (nothing applied to real data).
- **Current System Status:** Tests green. Backend 8002 (`--reload`) and frontend 5173 started from this
  session -- they die with it; restart with `start_backend_api.bat`. dfs_week_3 parquets had their mtime
  bumped for the refresh test (contents unchanged).
- **Immediate Next Steps for the Next Agent:**
  1. Ask Cam whether to run `python scripts/roster_management/dfs_status_ledger.py seed 2` -- week 2's 16
     gameday outs predate the ledger and did NOT carry into week 3.
  2. Blowout rate looks high: ~8.7% of week-3 sims end with |margin| >= 28 (ATL@GB "GB by 28+" 12%);
     check vs real history (`historical_eda_metrics.json`) before trusting tails.
  3. Week-results rebuild at 10K sims took ~12 min once (2 min warm) -- profile `_compute_week_sim_results`
     if it stays slow; page shows old data + banner meanwhile.
  4. Minor: App loads `week_sim_results?week=1` on mount before defaulting to the latest week (wasted call).

### [2026-09-23] Handoff from Claude Opus 5.5 (exposure drill-down: "Most paired with" + showdown port)

- **Active Task:** Follow-ups to the previous entry's exposure drill-down, per Cam: drop Avg Salary on
  classic (every classic lineup sits near the cap -- uninformative), add "top players selected with him"
  (by position + overall), and port the whole drill-down to showdown *with* Avg Salary kept. Done and
  live-tested; frontend-only.
- **Files Modified:**
  - [frontend/src/pages/Optimizer.jsx](frontend/src/pages/Optimizer.jsx): removed Avg Salary from
    `summarizeLineups` / the stats strip. New `pairedPlayers` memo -- for the filtered lineups, every
    non-selected player with `count`, `pairPct` (% of filtered lineups), `exposure` (whole-portfolio %)
    and `lift` (pairPct - exposure, pp). UI under the stats strip: one leader chip per position + a
    top-8 list with ALL/QB/RB/WR/TE/DST tabs (`pairPosFilter`); bar = pairPct, white tick = exposure.
    Clicking a pairing adds it to the filter. Live check (150-lineup build): Gibbs -> 60/150; top
    pairings Jones/McLaurin 27%, LaPorta 23%; QB tab Prescott +8; clicking Prescott -> 9/150.
  - [frontend/src/pages/ShowdownOptimizer.jsx](frontend/src/pages/ShowdownOptimizer.jsx): full port.
    Showdown-specific: selection keys are `name_team` (any slot) or `name_team|CPT` (captain only) --
    click an Exposure row for any-slot, click its **CPT%** cell for captain-only (the two are mutually
    exclusive per player, `toggleDrill`). `displayedLineups` feeds the table (exports still use
    `sortedLineups`); stats strip adds 1st%, Own%, Proj and **Avg Salary**; pairings carry a `cptCount`
    ("CPT n") and include K. `exposures` rows gained a `key` field. Live check (ATL@GB, fresh 20-lineup
    optimize): Love 60% Tot -> 12/20, CPT 10% -> 2/20 (both Love-CPT); Kraft paired 8/12, 6 as CPT, +22.
- **Observations (not changed):** showdown Port.Score per lineup spans roughly -477..+518 on that build,
  so subset averages swing hard (Love subset 185.65 vs portfolio 1.72 -- real, verified row-by-row).
  Showdown EV% reads >+1000% on that build -- pre-existing EV math, not from this work; may merit a look.
- **Commit note:** same split as last entry -- both .jsx files and WORKLOG.md also carry another
  session's uncommitted work; only this session's hunks were staged, the rest left untouched.
- **Immediate Next Steps:** Cam to sanity-check the classic threshold-while-editing behavior with a real
  threshold set (still outstanding from the previous entry).

---

### [2026-09-22] Handoff from Claude Opus 5.5 (DFS optimizer UX: pool team/game filter, edit-safe projection threshold, exposure drill-down)

- **Active Task:** Three DFS Optimizer UI requests from Cam, all frontend-only, all in
  [frontend/src/pages/Optimizer.jsx](frontend/src/pages/Optimizer.jsx). Done and live-tested
  against the running DFS site (week's existing 20-lineup build).
- **Files Modified:**
  - [frontend/src/pages/Optimizer.jsx](frontend/src/pages/Optimizer.jsx):
    1. **Player Pool team/game filter** -- `teamGameFilter` state (`'ALL' | 'G:<AWAY@HOME>' | 'T:<TEAM>'`)
       + a dropdown beside the search box, options from `poolGameOptions` (derived from the pool itself,
       not `games`). View-only -- does NOT exclude from the optimizer (SettingsPanel / GameBar still own that).
       Live check: 338 -> 24 players on ARI@SF, all rows that game.
    2. **Projection threshold waits for blur** -- `editingProj` (`{id, projection, gppProjection}` snapshot
       taken onFocus, cleared onBlur / Enter). While set, that player skips the "Hide & Exclude below" filter
       in `visiblePool`, sorts by the snapshot (default sort is by projection, so a half-typed 12->1 would
       otherwise throw the row down the table), and isn't dimmed. `isPlayerExcluded` itself is unchanged, so
       the optimize payload is unaffected. Live check: typed 0.1 into Gibbs, row held position + focus;
       restored to 25.4 (no override left behind). **Not live-tested with a threshold > 0** -- Cam's saved
       threshold was 0 and settings autosave, so didn't change it.
    3. **Exposure drill-down** -- clicking a Portfolio Exposure card toggles it into `selectedExposureKeys`
       (key = `name_team_pos`, now also stored as `key` on each `playerExposures` row). `displayedLineups`
       filters the lineups table to lineups with ALL / ANY (`exposureMatchMode`, toggle shown at 2+ players)
       of them; a stats strip shows avg EV% / Port.Score / ITM% / Top1% / Top.1% / P95 / salary for the subset
       vs. the whole portfolio (`summarizeLineups`). Selected players get an underline in lineup rows.
       **Exports (DK upload + summary CSV) still use the full `sortedLineups`**, deliberately. Stale selections
       are pruned when `playerExposures` changes; expanded row collapses when the filter changes (index-based).
       Live check: Flowers 40% -> 8/20; + Aaron Jones (35%) -> 3 ALL / 12 ANY (8+7-3, consistent).
- **Commit note:** Optimizer.jsx also carried another session's uncommitted work (DST `dk_name` export fix,
  ContestPicker, FLEX-mix line, etc.) and WORKLOG.md that session's uncommitted entry. Only this session's
  hunks were staged; the other work is still uncommitted in the working tree, untouched.
- **Immediate Next Steps:** Cam to sanity-check #2 with a real threshold set. Possible follow-ups (not
  requested): show original portfolio rank (`#`) in a filtered lineup view; a "lock/exclude from here"
  action on the drill-down chips.

---

### [2026-09-22] Handoff from Claude Sonnet 5 (Week-3 rollover + DFS ownership model v2, built largely autonomously)

- **Active Task:** Two linked pieces of work, same session. (1) Get the DFS dev site
  defaulting to week 3 with real matchups showing (Cam explicitly scoped out
  projections/ownership for that piece — "we can run all of that tomorrow"). (2) The
  ownership model v2 build from [docs/implementation_plans/ownership_model_v2_plan.md](docs/implementation_plans/ownership_model_v2_plan.md)
  (segmentation features + prior-week signal) — Cam stepped out for several hours and
  asked how much of it could be built without him; built essentially all of it, with
  two deliberate frontend-UI stubs flagged below rather than guessed at.
- **Files Modified:**
  - [src/api/app.py](src/api/app.py): `/api/weeks` was hardcoded to `[1, 2]` — the actual
    reason week 3 never showed up anywhere in the frontend (it picks
    `Math.max(...weeks)` as the default on load). Now `[1, 2, 3]`. Added
    `OptimizeRequest.max_entries` / `ShowdownOptimizeRequest.max_entries` (both
    `Optional[float] = None` — no frontend field sets these yet, added so the backend
    is ready). Added `ShowdownPrepRequest.entry_fee/field_size/max_entries` (same
    story, see Immediate Next Steps #1). Wired a `contest={field_size, entry_fee,
    max_entries}` dict into all three `predict_classic_ownership`/
    `predict_showdown_ownership` call sites that have real per-request values
    (`/optimize`, `/api/showdown_optimize`) — `/showdown_prep`'s call passes the new
    (currently-always-None) fields through for when the frontend catches up.
  - [src/ownership/model_inference.py](src/ownership/model_inference.py): added an
    optional `contest` param to both predict functions; `_stakes_tier()` (duplicated
    from `standings_parser.py`'s function on purpose — this module is always-loaded by
    the live API, and importing from `scripts/` would mean a broken script-tree import
    could take the whole server down with it) and `_contest_fields()` turn `contest`
    into the three raw feature values, defaulting to None/no-signal when omitted so
    every existing caller's behavior is unchanged. Wired `prior_week_score` into both
    row-building loops. Updated the module's stale "1 classic slate, 2-3 showdown"
    docstring note.
  - [src/ownership/prior_week.py](src/ownership/prior_week.py) (new): the prior-week
    sentinel feature. Encoding, per Cam (confirmed mid-session): `-99` genuinely never
    seen before (rookie debut, whether that's week 1 or later), `-76` returning from a
    3+ consecutive-week absence while salaried each of those weeks (a real injury —
    "even injured players have salaries"), `-51` team was on a bye the prior week, `0`
    no salary listing the prior week but seen at some earlier week (practice-squad
    elevation / re-signed veteran — "most representative of their previous weeks"),
    else the real score. Caught and fixed a real bug in this **before** it shipped:
    the roster-eligibility check (`_salary_keys`) originally only scanned the classic
    main slate, but DK's Main Slate genuinely excludes Thursday/SNF/MNF games (verified
    by cross-referencing which teams were missing from a real main-slate snapshot
    against the schedule — exactly the Thu/SNF/MNF matchups, nothing else, and this
    matches `/api/games`'s own existing weekday/gametime heuristic). Un-fixed, every
    player on a primetime team would've been wrongly coded `-99`/`0` every single
    week. Fixed by unioning every slate folder (main + all showdowns) for the week,
    not just main_slate — verified against a real player (Josh Allen/BUF) before and
    after: `0.0` (wrong) → `40.82` (his real week-2 score, correct).
  - [src/scrapers/dk_scraper.py](src/scrapers/dk_scraper.py): fixed a real,
    **pre-existing** bug (present since week 1, not introduced this session) —
    `_refresh_slate()` read `position` off each DK draftable purely to route
    DST-vs-everyone-else, then discarded it; `get_dk_salaries()`'s `players` dict had
    nowhere to put a position at all. This is why every classic main-slate salary
    snapshot has had `pos=""` for every non-DST player since the archive started.
    Added a `player_pos` dict alongside `players`, threaded through `get_dk_salaries()`'s
    return shape (+ its `empty` fallback) and `load_prelock_salary_snapshot()`'s
    CSV-reading counterpart, for parity.
  - [scripts/dfs_ownership/snapshot_slate_salaries.py](scripts/dfs_ownership/snapshot_slate_salaries.py):
    `snapshot_main()` now writes real `pos` from `dk["player_pos"]` instead of a
    hardcoded `""`.
  - [scripts/dfs_ownership/train_ownership_model.py](scripts/dfs_ownership/train_ownership_model.py):
    fixed a second real bug found while re-grounding the plan in real numbers —
    `leave_one_slate_out_cv`/`_heuristic_baseline` grouped by bare `slate_id` (just
    the archive folder's basename, e.g. `"main_slate"`), which is *identical* across
    different weeks by construction, so week 1 and week 2's classic data were silently
    merging into one CV fold even after week 2 was recovered. Now groups by
    `(year, week, slate_id)`. Added `field_size`/`max_entries`/`prior_week_score` to
    `CLASSIC_FEATURES`/`SHOWDOWN_FEATURES`, `stakes_tier` to `CATEGORICAL_COLS` (NOT
    also in the feature lists — same convention `pos` already follows, doubling it up
    silently double-selects the column), `prior_week_score` computed in `_enrich()`.
  - `data/dfs_ownership/2026/week_01/main_slate/salaries_prelock.csv`: position
    backfilled in place (week 1's original draft-group id, 151307, is documented
    elsewhere in this file as confirmed repointed/poisoned by DK since capture — could
    not safely re-fetch). 568 resolved from week 2/3's now-correct data (cross-team,
    same player usually keeps position), 128 from `data/current_rosters/dfs/*_traits_2026.json`,
    26 left blank (all deep-bench/min-salary, acceptable gap).
  - `data/dfs_ownership/2026/week_02/main_slate/` + `showdown_{BUF_DET,IND_KC,LAR_NYG}/`:
    recovered from scratch — week 2's pre-lock salary snapshot had never been taken at
    all (no `manifest.json` existed for any of these). Classic recovered via the sticky
    main-slate pin (`data/dk_main_slate_pins.json`, draft group 153428); showdowns
    recovered by probing DK's sequential draft-group-id block for that week and
    verifying each fetch's returned team names against the schedule before trusting
    it (confirmed DK runs several product variants — Showdown, Snake Showdown, etc. —
    per matchup under different ids; checked they were byte-identical player pools
    before picking one arbitrarily). **Independently cross-verified**, not just
    assumed correct, against data captured live during week 2 itself and unrelated to
    this session: 76/76 real skill-player salaries matched
    `data/optimizer/2026/week_02/builds/*.json`'s `players_used`; Josh Allen's captain
    salary (17100 = 11400 × 1.5) matched the showdown workspace's own saved lineup.
    Re-fetched a second time after the position fix landed, re-verified again, still
    76/76 clean.
  - `data/dfs_ownership/2026/week_02/main_slate/{firstdown_1_20max,minimax_.5_150max,pocket_.01_150max}.csv`:
    renamed from typo'd originals (`firstdown_1_20max .csv` — trailing space;
    `minimax_,5_150max.csv` / `pocket_,01_150max.csv` — comma instead of period) that
    the standings parser was silently skipping even after the salary snapshot came back.
  - `data/dfs_ownership/2026/week_03/main_slate/` + `showdown_ATL_GB/`: new pre-lock
    snapshots for the current week (main slate + the one showdown posted so far —
    DK only posts each single-game showdown near its own kickoff, not all at once;
    this needs re-running a few more times before Thursday/Sunday/Monday locks).
  - `data/dfs_ownership/_processed/{ownership_actuals,features}.parquet`: rebuilt
    several times as the above landed.
  - `data/dfs_ownership/_processed/models/*.json`: retrained + shipped (not
    `--dry-run`) three times this session as each fix landed; final numbers below are
    the ones actually on disk.
  - [docs/implementation_plans/ownership_model_v2_plan.md](docs/implementation_plans/ownership_model_v2_plan.md):
    living design doc, updated throughout the session as things were discovered
    (not written after the fact) — real CV numbers, the data-recovery process, all
    resolved design questions. Left as the design record; this WORKLOG entry is the
    source of truth for what actually shipped.
- **Verification Performed:**
  - Week-3 rollover: live in the browser (Optimizer + Showdown Optimizer both load on
    Week 3 by default, real matchups/Vegas lines render, `read_console_messages`/
    `read_network_requests` clean).
  - `python -c "import ast; ast.parse(...)"` on every touched `.py` file; separately
    `import src.api.app` in-process (Pydantic model validation + FastAPI construction)
    with no errors.
  - Direct function tests of `predict_classic_ownership`/`predict_showdown_ownership`
    (with and without `contest`): `ownership_source` stays `"model"` (not falling back
    to heuristic), renormalization lands on the real structural targets (classic
    QB≈100/DST≈100/RB+WR+TE≈700; showdown FLEX≈500/CPT≈100), and supplying contest
    info measurably shifts predictions vs. omitting it.
  - Full HTTP-level FastAPI `TestClient` smoke tests (in-process — deliberately did
    NOT touch the live server already running on port 8002, which belongs to another
    chat session and is running pre-session code): `/api/optimize` against real week-2
    data → 200, real lineups, new `max_entries` field accepted; `/api/showdown_prep`
    against real week-2 BUF/DET data → 200, sensible CPT/FLEX split, new contest
    fields accepted.
  - Final leave-one-slate-out CV (the numbers the shipped models were judged against
    the plan's documented acceptance bar — "doesn't get worse" than pre-session — and
    passed): classic 2 folds, 0.6pp model MAE vs. 2.7pp heuristic, wins 2/2; showdown
    FLEX 6 folds, 7.2pp vs. 30.2pp, wins 6/6; showdown CPT 6 folds, 2.2pp vs. 21.1pp,
    wins 6/6.
  - Random 15-player spot-check of `prior_week_feature` against real week-3 data
    post-fix — all real, sensible scores (Mahomes 31.98, bench players 0.0), no
    spurious sentinels; separately confirmed the `-99` (never-seen) and week-1
    (no-prior-week-exists) paths return correctly for a fabricated name.
- **Current System Status:** Everything verified as above; nothing committed (Cam's
  standing rule: never commit without being explicitly asked — this session's changes
  sit on top of whatever was already uncommitted). The live `backend-api-dfs` server on
  port 8002 (another chat's, left untouched all session) is running **pre-session
  code** — it needs a restart before any of tonight's fixes are live for actual use.
  `week_03/`'s showdown coverage is 1 of ~13 games (only the Thursday one posted by
  DK so far).
- **Immediate Next Steps for the Next Agent:**
  1. Two things deliberately built as backend-only stubs, not wired to any UI, because
     they're product/UX decisions Cam should make, not ones to guess at solo: (a)
     `max_entries` (max entries per user, "Nmax") has no frontend field anywhere in
     this app yet — nothing will ever populate it until one's built. (b)
     `/showdown_prep` structurally runs *before* a contest is chosen (right after
     picking a game), so even though it now accepts `entry_fee`/`field_size`/
     `max_entries`, the current frontend flow has nothing to send there yet — would
     need the "target contest chosen up front" flow from
     [docs/implementation_plans/optimizer_persistence_plan.md](docs/implementation_plans/optimizer_persistence_plan.md)
     to land first, or a smaller reordering just for showdown.
  2. Restart `backend-api-dfs` (port 8002) before relying on any of tonight's fixes
     live — it's currently serving stale pre-session code.
  3. Re-run `snapshot_slate_salaries.py --week 3` (no `--main`, already have it) a few
     more times before Sunday/Monday lock to catch the rest of week 3's showdown
     slates as DK posts them — same gap that caused week 2's data loss if skipped.
  4. `-76` (injury) and `-51` (bye) sentinels are built and load-bearing in the shipped
     model, but literally cannot have fired on real data yet — no bye weeks and no
     3+-week absences exist this early in the season. Worth a manual sanity check the
     first time either fires for real.
  5. Week 1's 26 unresolved (still-blank) positions are all deep-bench/min-salary
     players — low-impact, but worth knowing if anyone later audits week-1 data quality.
  6. Per the plan's §3.1, revisit per-bucket model splitting (vs. today's pooled +
     segmentation-features approach) once coverage reaches the README's ~4-6-slates-
     per-bucket target — not yet, deliberately.
  7. Ask Cam whether/how he wants tonight's changes committed — likely several logical
     commits (dk_scraper position fix; ownership v2 segmentation+prior-week; week 1-3
     data recovery; week-3 rollover), not one giant one, matching this repo's usual
     pattern.

---

### [2026-09-22] Handoff from Claude Sonnet 5 (2026-09 audit revisit — Batches A/C/H of the fix-pass plan)

- **Active Task:** Cam asked to revisit the 2026-09 full-repo audit (run just before Week 1, see [docs/audit/2026_09_audit/](docs/audit/2026_09_audit/)) — 13 days and one large DFS-feature commit later, nothing from its [fix_pass_plan.md](docs/audit/2026_09_audit/fix_pass_plan.md) had been done except E1 (already marked done pre-existing). Scoped this pass to **Batches A (safety), C (tooling), H (docs) only** — B (dead-code removal), D/F (engine tests + validation benchmark), G (publish-prep), and both Cam-gated decisions (filename strip, live-bot canonical entry point) are deliberately deferred to later in the season, not skipped. `fix_pass_plan.md` itself now carries `✅ DONE`/`🔄 PARTIAL` markers per item — check there before re-doing anything below.
- **Files Modified:**
  - **A1 (OneDrive) — turned out mostly already done:** the actual repo move off OneDrive had already happened before this session (confirmed: `C:\Users\txcwa\Dev\NFL_Exploration` isn't under the OneDrive tree, and the `.gitignore` tripwire was already committed). Deleted the remaining 10 stale `*-Cams-Desktop` conflict-copy files (`.obsidian/workspace-Cams-Desktop*.json` ×8, `data/interim/sim_results_2025_{games,players}-Cams-Desktop.parquet` ×2) — all dated back to June, untracked, harmless. Left `venv_py38_old/`'s copies alone (that whole dir is B3, deferred).
  - **A2 (backup):** [scripts/data_utils/backup_critical_assets.py](scripts/data_utils/backup_critical_assets.py) (new) — pushes the 13 gitignored, un-regeneratable files (`data/processed/hardened_pass_training_master_v2_5.csv`, 308MB, + the win_probability/field_goal/fourth_down R `.rds` train/val/test splits) to a Backblaze B2 bucket (Cam's choice), SHA1-skip-if-unchanged so a repeat run is cheap. Reads `B2_APPLICATION_KEY_ID`/`B2_APPLICATION_KEY`/`B2_BUCKET_NAME` from env, refuses to run with a clear error if unset — never silently no-ops. Added `b2sdk==2.13.0` to [requirements.txt](requirements.txt). **Verified via `--dry-run` only** (finds all 13 files, ~295MB) — Cam still needs to create the actual B2 account/bucket and set the env vars before the first real upload; nobody should assume a backup has actually run yet.
  - **A3 (fail loud on missing inputs):** [src/nfl_sim/game_engine.py](src/nfl_sim/game_engine.py), [src/nfl_sim/batch.py](src/nfl_sim/batch.py), [src/nfl_sim/model_registry.py](src/nfl_sim/model_registry.py) — added `_require_json`/`_require_dir` helpers (raise `FileNotFoundError` naming the missing path) and wired them into every non-optional load at `__init__`/`load_all` time: both rosters, all DNA files (qb/coach/trench/rb/wr/te), `team_to_coach`, `trench_tiers`, `rush_gate_calib`, and all 9 `ModelRegistry` model dirs. Left the soft `_load_json` in place only where a missing *value* (not file) is genuinely expected (e.g. a player absent from a DNA dict → fallback is correct). Since every one of these files currently exists, this is a pure failure-path change — zero effect on today's successful runs, only changes what happens the day one of these is actually missing (previously: silent `{}`/`None` and a wrong-looking-normal sim; now: an immediate, named `FileNotFoundError`).
  - **C1 (pyproject.toml):** [pyproject.toml](pyproject.toml) (new) — `[tool.pytest.ini_options] testpaths = ["tests"]` (bare `pytest` no longer wanders into `scratch/`/`legacy/`), dependencies dynamically sourced from `requirements.txt` (no duplication), `[tool.setuptools.packages.find]` for the `src*` tree. Added empty `__init__.py` to `src/`, `src/nfl_sim/`, `src/api/`, `src/data_pipeline/`, `src/live/`, `src/scrapers/`, `src/nfl_sim/models/`, and all 10 `src/nfl_sim/models/*/` subdirs (were implicit namespace packages before — needed explicit `__init__.py` for `packages.find` to discover them; zero import-behavior change, verified). `pip install -e . --no-deps` now works.
  - **C2 (CI):** [.github/workflows/ci.yml](.github/workflows/ci.yml) (new) — first workflow that runs the pytest suite (`pytest tests/ -q`) and both frontend builds (`frontend`, `frontend_analysis` — `npm ci && npm run build`, matrix job) on push/PR to main. Kept fully separate from the existing `nfl_live.yml`/`nfl_manuel.yml` R-bot workflows.
  - **C3 (renv.lock):** [R/renv.lock](R/renv.lock) (new, committed) — real, accurate 109-package manifest snapshotted from the actual global R library (`renv::snapshot()` pointed explicitly at the global lib paths, not persisted into settings so no absolute path leaked into the committed file). **Deliberately did NOT activate renv's isolated-library workflow** — `R/.Rprofile`'s `source("renv/activate.R")` is commented out with an explanation, because (a) `nfl_live.yml`'s live-bot CI step runs `Rscript --vanilla` which skips `.Rprofile` anyway, and (b) renv's default sandboxing would hide the normal user/system R library from any interactive session started with `R/` as the working directory (real risk to Cam's local RStudio use if left active with an empty isolated project library). Instead fixed the concrete bug this closes: [.github/workflows/nfl_live.yml](.github/workflows/nfl_live.yml)'s hand-written `extra-packages:` list was missing `xgboost` (needed by the win_probability/fourth_down/field_goal `.rds` models!) plus 10 others actually used (`nflfastR`, `nflreadr`, `rtoot`, `atrrr`, `dotenv`, `rlang`, `scales`, `slider`, `tidyr`, `pROC`, `caret`, `ggplot2`) — cross-checked against the new lockfile. Verified locally: packages still resolve normally with `R/` as cwd (renv stays inert, confirmed via a real `Rscript` call).
  - **H1 (README):** [README.md](README.md) — full rewrite. Dropped the broken `file:///c:/Users/txcwa/OneDrive/Desktop/Antigravity%20Projects/...` absolute links (that path doesn't even exist anymore), fixed the port claim (there are two APIs — Analytics on 8000, DFS on 8002 — not one), added the DNA/override system, R live-bot, paper-trading/evaluation-tab pieces that didn't exist when it was last touched in July.
  - **H2 (false completes + stale claims):** [GOAL_TRACKER.md](GOAL_TRACKER.md) — reverted 3 false-✅ rows to honest status (Tier 1 Production Release → not published; full system validation → no benchmark has ever actually run, Batch F still pending; per-sub-project roadmaps → 1 of ~4 exist). [AGENTS.md](AGENTS.md) — trimmed §0 from ~68 lines of full session narrative (every bullet already ended "Full detail: WORKLOG.md" — 100% duplicated) down to a short pointer + a distilled "known open threads, not re-verified" list; fixed the test count (92→121). [DEVELOPMENT.md](DEVELOPMENT.md) — fixed the wrong claim that `docs/boxscores/week_*/` isn't git-tracked (82 files are).
  - **H3 (doc consolidation) — partial:** `git mv`'d the 5 stale root planning docs into `docs/planning/` (`DETAILED_GOALS.md`, `FRONTEND_GOALS.md`, `FANTASY_BETTING_SITE.md`, `GAME_ANALYSIS_SITE.md`, `FUTURE_DEVELOPMENT.md`) — fixed the one broken link this created (README.md's own reference). While sweeping absolute paths for H1, found the *same* `file:///...OneDrive...` broken-link bug in 9 more files beyond the one the audit named (`src/live/README.md`) — fixed all of them via a small one-off relative-path-rewriting script (verified every rewritten link resolves to a real file): `src/README.md`, `src/api/README.md`, `src/data_pipeline/README.md`, `src/nfl_sim/README.md`, `scripts/README.md`, `docs/models/fg_v_0_1_0.md`, `docs/implementation_plans/chess_implementation_plan.md`, `docs/implementation_plans/EFSD_implementation_plan.md`, and 1 link in `DETAILED_GOALS.md`. **Left alone, deliberately:** `docs/eda_outputs/chaos/chaos_eda_report.md` and `docs/v_0_2_0_implementation_plan.md` — these point at an even-older `NFLSims - Copy` path that likely no longer exists anywhere; treated as frozen historical snapshots, not live docs worth chasing. **Still open, needs Cam's sign-off before doing:** gitignoring + deleting the 82 tracked `docs/boxscores/week_*/` files, and folding the `docs/todo`/`docs/prompts`/`docs/study`/`docs/metrics`/`docs/optimizations`/`docs/frontend` grab-bag dirs into `docs/notes/` or `implementation_plans/`.
  - **H4 (inert coach levers):** [data/dna/coach_coordinator_levers_2026_notes.md](data/dna/coach_coordinator_levers_2026_notes.md) (new — a sibling notes file, not a CSV header comment, since `build_2026_ooc_proxies.py` reads the CSV with a plain `pd.read_csv(dtype=str)` that would choke on a comment row) documents which of the 6 coach-tendency lever families are live (`proe`, `deep_shot_rate`) vs. staged-but-unconsumed (`no_huddle_rate`, `sec_per_play`, `air_yards_tendency`, `screen_rate`, `play_action_rate`, `rpo_rate`, `conservative_score_bias`). Cross-referenced from a new line in [AGENTS.md](AGENTS.md) §8 (Fragile Areas) and a new note at the top of [docs/models/play_selection_v_0_1_0.md](docs/models/play_selection_v_0_1_0.md).
  - [docs/audit/2026_09_audit/fix_pass_plan.md](docs/audit/2026_09_audit/fix_pass_plan.md): marked every completed/partial item this session touched (A1-A3, C1-C3, H1-H4) with a `✅ DONE`/`🔄 PARTIAL` header tag + a top-of-file summary paragraph, matching the pre-existing E1 convention.
- **Verification Performed:**
  - `venv/Scripts/python.exe -m pytest -q` — **121/121 passing**, re-run after every batch (A3's engine-loading change, C1's packaging change, and the final state) — no regressions at any point.
  - Live smoke tests (not covered by the existing suite — confirmed zero direct `game_engine.py.__init__` tests exist, that's D1's whole finding, deferred): constructed a real `NFLGameEngine('KC','BUF',year=2026,N=5)` and `BatchSimulator('KC','BUF',year=2026)` successfully post-A3; separately confirmed `NFLGameEngine('KC','ZZZ',...)` (bad team code) now raises a clear, named `FileNotFoundError` instead of silently defaulting.
  - Bare `pytest` (no path arg) confirmed scoped to `tests/` only post-C1 (was previously untested/assumed-broken per the audit).
  - `scripts/data_utils/backup_critical_assets.py --dry-run` confirmed it finds all 13 target files (~295MB) with no credentials set; confirmed it fails loud (`exit 1`, clear message) when run for real without B2 env vars.
  - `R/renv.lock` sanity-checked: 109 packages, `xgboost` present, zero absolute paths leaked into the committed file (`grep -c "C:/Users" R/renv.lock` → 0). Confirmed R packages still resolve normally with `R/` as cwd after the `.Rprofile` change (renv stays inert as intended).
  - Every rewritten relative link (the H1/H3 absolute-path sweep) spot-checked to resolve to a real on-disk file, not just syntactically plausible.
- **Current System Status:** All changes verified, no regressions. Nothing from this session has been committed yet (Cam's global rule: never commit without being explicitly asked) — the working tree has these changes on top of the pre-existing large uncommitted backlog noted in earlier entries. `scripts/data_utils/backup_critical_assets.py` has never actually been run for real (no B2 credentials in this environment) — do not assume a backup exists yet.
- **Immediate Next Steps for the Next Agent:**
  1. Ask Cam whether/how to commit this session's work before doing anything else with it — likely several logical commits mirroring the batch structure (A/C/H), not one giant commit.
  2. Cam needs to create a Backblaze B2 account + bucket and set `B2_APPLICATION_KEY_ID`/`B2_APPLICATION_KEY`/`B2_BUCKET_NAME`, then run `scripts/data_utils/backup_critical_assets.py` for real at least once. Re-run it after any future change to the pass-training master or a retrain of the win_probability/field_goal/fourth_down R models.
  3. H3's two deferred pieces (`docs/boxscores` gitignore+delete of 82 tracked files, `docs/` grab-bag folding) need Cam's explicit go-ahead before touching — both are real deletions/reorganizations, not mechanical.
  4. Batches B (dead-code removal), D (engine invariant tests), F (the actual validation benchmark — `GOAL_TRACKER.md`'s "full system validation" row is honestly `🔄 In Progress` right now, not done), and G (publish-prep) plus both Cam-gated decisions (filename `_v_0_1_0` strip, canonical live-bot entry point) are the rest of `fix_pass_plan.md` — explicitly deferred this pass, not forgotten. Cam said he wants to publish during the season "soon probably," at which point the full remaining audit (especially F, the validation gate, and G) becomes relevant again.
  5. The pre-existing large uncommitted backlog flagged in several earlier WORKLOG entries is still sitting there, now with this session's changes layered on top — still needs sorting/committing at some point, unrelated to this session's specific work.

---

### [2026-09-17] Handoff from Claude Sonnet 5 (CPOE-blend investigation -> volume-weighted DNA blend, all 3 phases + a real scramble_rate bug fix)

- **Active Task:** Cam noticed Jordan Love's CPOE dropped >2.5 points after one real game and asked to investigate -- confirmed the fixed 100/80/60/40/20% taper (dna_blender_v_0_1_0) was working exactly as coded, but the design itself was too swingy for a small-sample game. Became a full redesign of the blend mechanism. Full design rationale, Cam's exact direction, and a phase-by-phase build log with verification numbers: [docs/implementation_plans/volume_weighted_dna_blend_plan.md](docs/implementation_plans/volume_weighted_dna_blend_plan.md) -- this entry summarizes.
- **Files Modified:**
  - [src/data_pipeline/rolling_stats_v_0_1_0.py](src/data_pipeline/rolling_stats_v_0_1_0.py): (1) fixed a real, previously-invisible bug found while investigating -- `scramble_rate` was computed only from `play_type=="pass"` rows, but nflverse codes a scramble as `play_type=="run"`, so it silently returned `0.0` for every QB, every week (`game_engine.py` reads this field directly to drive in-sim scramble frequency -- not cosmetic). (2) Phase 1: `build_player_game_log()`/`build_player_ngs_game_log()` now return `(log, volume)` tuples -- volume is each field's real weekly sample size (attempts/targets/carries/dropbacks/completions). New `rolling_sum()`/`compute_season_to_date_volume()`/`compute_l4_volume()`/`rolling_volume_for_fields()` primitives. `rolling_stats_for_player()` now returns a 4-tuple `(season, l4, season_volume, l4_volume)`.
  - [scripts/roster_management/rebuild_veteran_baseline_v_0_1_0.py](scripts/roster_management/rebuild_veteran_baseline_v_0_1_0.py): Phase 2 -- writes a new `preseason_projection_volume` sibling block per player. Added a `--volume-only` mode (computes volume, never touches rate fields) after finding a normal run would silently overwrite Cam's hand-tuned preseason values (`apply_team_season_overrides` writes hand-tuned CSV values into `preseason_projection` too) with no git safety net (`data/current_rosters/*_traits_2026.json` is gitignored).
  - [src/data_pipeline/dna_blender_v_0_1_0.py](src/data_pipeline/dna_blender_v_0_1_0.py): Phase 3, full rewrite. `blend_player_dna()`/`blend_team_dna()` gained optional `projection_volume`/`season_volume` kwargs. Both given: new pooled-volume mechanism (`(hist_volume*hist_rate + season_volume*season_rate)/(hist_volume+season_volume)` through game 4, then trailing-4-game-only -- no historical anchor -- from game 5 on, WINDOW=4 per Cam's call). Either omitted: falls back to the original fixed-taper/steady-state code unchanged (renamed `_taper_blend`, byte-identical body) -- deliberate, not a shim: rookies (curve-track, no real historical volume -- zero-volume pooling would let one game instantly override their usage ramp), zone-split fields (`MIN_ZONE_SAMPLES` already solves this differently), and team defense all still need it.
  - [scripts/roster_management/refresh_weekly_dna_v_0_1_0.py](scripts/roster_management/refresh_weekly_dna_v_0_1_0.py): wired Phase 3 in -- veterans' flat-field blend now passes real `projection_volume`/`season_volume`; rookies explicitly pass `projection_volume=None` (forces the taper fallback, preserves their curve-ramp behavior); the per-zone blend call and `refresh_team_defense()`'s call are untouched (no volume args -> automatic taper fallback). Also fixed 3 existing callers of `rolling_stats_for_player()` (here + `rebuild_veteran_baseline_v_0_1_0.py` + `build_prior_season_actuals_v_0_1_0.py`) to unpack its new 4-tuple.
  - [scripts/roster_management/build_prior_season_actuals_v_0_1_0.py](scripts/roster_management/build_prior_season_actuals_v_0_1_0.py): mechanical 4-tuple unpacking fix only, no behavior change.
  - [tests/test_dna_blender.py](tests/test_dna_blender.py): +33 tests (volume primitives, `build_player_game_log`'s volume output, pooled-blend mechanics/cutover/fallback, the scramble_rate regression) -- all prior tests kept and still pass unmodified (they exercise the no-volume fallback path, confirmed identical to the old code).
  - [docs/implementation_plans/volume_weighted_dna_blend_plan.md](docs/implementation_plans/volume_weighted_dna_blend_plan.md) (new): design doc + phase-by-phase build log.
- **Data regenerated** (not hand-edited, produced by running the now-fixed pipeline against real 2026 PBP/NGS): `data/current_rosters/*_traits_2026.json` (season-long, gitignored -- no git diff to show) + `data/current_rosters/dfs/*_traits_2026.json` (tracked) via `refresh_weekly_dna_v_0_1_0.py 2026 1` (rerun twice: once after the scramble_rate fix, once after Phase 3) + `rebuild_veteran_baseline_v_0_1_0.py 2026 2024 2025 --volume-only` (once, backfills `preseason_projection_volume`) + `apply_team_week_overrides_v_0_1_0.py 2` (recompiles the DFS tree, rerun twice); `data/dna/trench_dna.json` (team-defense side of the same refresh); `data/interim/dfs_week_2_{games,players}.parquet` via `run_week_sim_2026.py 2` (rerun twice, so live DFS projections reflect both fixes). **Note for the next agent:** this session's `git status` at start already showed ~205 modified/untracked files repo-wide (a large pre-existing uncommitted backlog, same one flagged in earlier entries) -- none of that is this session's work; the file list above is the real diff.
- **Verification Performed:**
  - `venv/Scripts/python.exe -m pytest tests/` -- 121/121 passing (99 -> 109 -> 121 across the session's checkpoints).
  - `ast.parse()` on every edited script, clean.
  - Scramble_rate fix: spot-checked Love/Allen/Hurts/Herbert against real 2026 week-1 data post-fix -- all now show real, distinct nonzero rates (previously every playing QB showed exactly `0.8x preseason`, a fake zero).
  - Phase 2 volume backfill: verified **0 rate fields changed**, 5,029 volume fields written across 631 players; spot-checked Love (QB, all non-volume fields byte-identical before/after) and Ja'Marr Chase (WR, sane target/completion counts: 360 targets, 252 completions over the 2-season window).
  - Phase 3 pooled blend: reran the real weekly refresh and confirmed the motivating cases are fixed -- Love's swing off one bad game dropped from -2.52 to -0.61 CPOE points, Kenny Pickett (small historical sample, 98 attempts) from -13.06 to -0.66, Sam Darnold (large sample, 1,095 attempts) from -5.92 to -0.08 -- swings now scale with real sample size instead of a flat 20%.
- **Current System Status:** All changes verified live against real 2026 data, no regressions found. Test suite green. DFS week-2 sim outputs are current as of this session's last run.
- **Immediate Next Steps for the Next Agent:**
  1. The volume-weighted blend plan (all 4 phases) is functionally complete -- no further planned work on it, but Cam hasn't yet reviewed the live week-2 numbers himself; be ready to adjust if he flags something.
  2. Open assumption flagged in the plan doc, not confirmed: Phase 2's historical volume was computed from a `2024, 2025` source-year window (the script's own documented example) since no prior run of `rebuild_veteran_baseline_v_0_1_0.py` is logged anywhere in `AGENTS.md`/`WORKLOG.md` -- worth confirming this actually matches whatever window built the current `preseason_projection` rates.
  3. `rebuild_veteran_baseline_v_0_1_0.py`'s normal (non-`--volume-only`) mode still overwrites hand-tuned preseason values on every run, unchanged from before this session -- if a future full baseline refresh is ever wanted, back up `data/current_rosters/*_traits_2026.json` first (gitignored, no recovery otherwise).
  4. The large pre-existing uncommitted backlog noted above (~205 files at session start, unrelated to this work) is still sitting there -- someone should sort/commit it separately at some point.

---

### [2026-09-12] Handoff from Claude Sonnet 5 (large synthetic field, K=5,000 -- ranking algorithm + two real perf/memory bugs it surfaced, all fixed and live-confirmed)

- **Active Task:** Same-session follow-up (3rd pass on the EV/field-simulation work today — see the two entries below this one for the histogram feature and the field-self-heal fix). Cam's ask: "up the sample [field size] until it is unsustainable and then base off of percentile" instead of comparing against a literal 50k+-entry field — i.e., push `FIELD_SAMPLE_K` as large as practically affordable, then keep extrapolating percentile-against-that-sample to `total_entries` (already the mechanism from the last entry). Archetype tuning explicitly deferred by Cam to later.
- **Three separate problems found and fixed, in the order they surfaced:**
  1. **Ranking didn't scale.** `_compute_lineup_field_stats`'s per-lineup brute-force compare (`field_matrix <= lineup_draws`) is O(n_field × n_sims), repeated once per generated lineup. Benchmarked on synthetic data: n_field=5,000 → ~17s for 150 lineups; n_field=20,000 untestable (~72s+). **Fixed** with `_compute_lineup_field_stats_batch` — stacks every generated lineup's draws with the field into one `(n_field + n_lineups, n_sims)` matrix and rank-transforms it (argsort-of-argsort: every entry's rank within its own iteration/column) in two vectorized numpy calls total, regardless of lineup count. Synthetic benchmark: ~6s at n_field=5,000 for 150 lineups (vs ~17s brute force), ~12.5s at n_field=10,000, a real cliff at n_field=20,000 (~387s). Correctness verified against brute force at each size (percentile agreement >99.9%). **Chose `FIELD_SAMPLE_K = 5,000`** (new module constant, [src/api/app.py](src/api/app.py) near `FIELD_SAMPLE_CACHE`) — comfortably inside that cliff, 5x the prior K=1,000's tail resolution. Both `/api/optimize` and `/api/optimize_showdown`'s per-lineup loops now collect all lineups' draws first, call the batch function once, then zip results back in — same output shape/keys per lineup, no frontend changes needed.
  2. **A real perf bug live-testing surfaced:** a single `/api/optimize` call hung for minutes at K=5,000. Root cause in `get_player_trial_scores` (classic's field-builder closure, called once per field-lineup × slot — tens of thousands of calls at K=5,000): (a) recomputed `np.percentile(arr, 50)` every call even for the same player, and (b) far worse, its "no real parquet data for this player" fallback branch constructed a **brand-new `np.random.default_rng` once per iteration inside a Python loop over all 10,000 `n_stat_sims` values, every single call** — any field-lineup player hitting that fallback (bye-week DST, hand-added player, anyone missing from the cached parquet) cost 10,000 fresh RNG constructions per field lineup that rostered them. **Fixed**: both the per-player median and the fallback's full deterministic 1000-length synthetic trial array are now memoized in request-scoped dicts (`_median_cache`, `_fallback_cache`) — computed once per unique player, same seed formula as before (`hash(name)%10000 + idx`) so scores are bit-for-bit identical, just not recomputed. Showdown's equivalent (`player_draws`) did NOT have this bug — already called once per unique player via `draw_cache`, not once per field-lineup slot.
  3. **A real memory bug found on the NEXT live attempt** (after fix #2, with the machine's competing CPU load having eased): `/api/optimize` returned a genuine 500 — `numpy._core._exceptions._ArrayMemoryError: Unable to allocate 385. MiB for an array with shape (5050, 10000) and data type int64`, thrown inside `_compute_lineup_field_stats_batch`'s `ranks = np.empty_like(order)`. The batch function was holding THREE full `(n_field+n_lineups, n_sims)` arrays at once (`combined` float64, `order` int64, `ranks` int64 — ~400MiB each, ~1.2GB total) on a machine that, between this and other running processes, didn't have that much contiguous RAM free at that moment. **Fixed**: `combined`/`order`/`ranks` now use `float32`/`int32` instead of the numpy defaults (`int32` comfortably covers `n_field+n_lineups` up to ~2.1B; DK fantasy scores don't need float64 precision), roughly halving peak memory (~600MB), plus explicit `del combined` / `del order` as soon as each is no longer needed to cut peak further. Re-ran the standalone correctness test after this change — identical numeric output to before, and noticeably faster too (5.79s vs 11.17s for the same synthetic case, smaller arrays sort faster).
- **Verification Performed (all live, this session, real backend + frontend):**
  - `_compute_lineup_field_stats_batch` extracted standalone and tested against synthetic correlated field+lineup data (`<scratchpad>/test_batch_stats.py`) both before and after the dtype fix — monotonic, sane EV/ITM progression from -100% through realistic mid-range to extreme-but-correct values in both passes, numerically identical.
  - `ast.parse` on `app.py` clean throughout.
  - **Real `/api/optimize` call on the actual Week 1 pool, with precise server-side timing added temporarily (`DIAGPERF` prints, since removed) and then confirmed clean on a final re-run after removing them:**
    - Field build (K=5,000 archetype construction via `build_field_sample`, one-time per week then cached in `FIELD_SAMPLE_CACHE`): **12.34s**
    - Field-matrix scoring (scoring all 5,000 field lineups × 10,000 iterations): **2.33s**
    - Our 50 generated lineups' own draws: **0.02s**
    - Batched ranking (the new double-argsort, all 50 lineups against the 5,000-lineup field in one pass): **4.39s**
    - Total for a cold (post-restart) call: ~19s. First attempt at this hit the ArrayMemoryError above (bug #3); after the dtype fix, re-ran clean end to end (`POST /api/optimize` → `200 OK`), then ran once more after stripping the diagnostic prints entirely — also clean.
  - Aside: this session also hit a genuinely unrelated environmental wall mid-investigation — a different long-running process (`run_full_season_sim_2026.py`, 36,500+ accumulated CPU-seconds) was saturating this shared machine, making even a bare `python -c "import ast"` take minutes. That eased on its own partway through; noting it here only because it explains why some intermediate timing numbers in this session's history looked erratic — it was never the code.
- **Current System Status:** All three fixes are live-confirmed working (200 OK, correct-looking output, reasonable timing) on the real backend against the real Week 1 pool. Diagnostic instrumentation was added temporarily to pin down exact timings and fully removed afterward — `grep -n DIAGPERF src/api/app.py` returns nothing.
- **Immediate Next Steps for the Next Agent:**
  1. This item from earlier is now answered rather than open: EV%/ITM% for the optimizer's top lineups are still elevated at K=5,000 (this session didn't re-inspect the exact displayed numbers after the final clean run, but the mechanism -- rank against a properly-sized, properly-scored field, extrapolated to `total_entries` -- is now correct and fast). If Cam still sees EV% that looks too high after trying this himself, the next lever is archetype tuning (`data/dna/field_archetype_params.json` / `field_simulator.py`), which he's explicitly said to defer, not the field size or ranking math -- both of those are now settled for this pass.
  2. If `FIELD_SAMPLE_K=5,000` ever needs to move (e.g. real player-pool sizes larger than tested here push the ~19s cold-start time too high, or a smaller/faster ceiling is wanted), it's one constant to change, no algorithm work needed -- see the memory-cliff data in point 1 above for where the next danger zone starts (~20,000).
  3. Past/settled-lineup histograms remain deferred (see the first entry below) -- unchanged.

---

### [2026-09-12] Handoff from Claude Sonnet 5 (EV/Top1% fix — rank against the FULL simulated field, not one opponent)

- **Active Task:** Same-session follow-up. Cam looked at the "Range of Outcomes" feature from the prior entry and said the EV was *still* way too high, and named the actual missing piece: "we need to simulate the contest as well... in run 1 our lineup scores x points but the Y amount of lineups we are playing against are also scoring points based on the sims... so we need to rank our created lineups against those as well." That's a precise description of a real bug, not just a formula quirk.
- **Root cause, this time for real:** the prior entry's `_compute_lineup_histogram`/`_compute_lineup_stats_direct` both ranked our lineup against `field_scores` -- a 1-D array holding **one randomly-picked field opponent's score per iteration** (classic: one random lineup from the cached archetype field sample; showdown: one random lineup from `_build_showdown_field`'s 1,200 builds). Comparing against a single competitor, or worse, a cutoff computed from that array's *marginal* distribution (blended across every game environment), throws away the fact that our lineup and the WHOLE field are scored from the SAME correlated game-environment draw each iteration -- when the environment runs hot, both our lineup and every field entry score high together, and a fixed/marginal/single-opponent reference doesn't see that. This is exactly what was inflating EV/Top1%.
- **Files Modified:**
  - [src/api/app.py](src/api/app.py): replaced `_compute_lineup_stats_direct` + `_compute_lineup_histogram` with one consolidated `_compute_lineup_field_stats(lineup_draws, field_matrix, ...)`. `field_matrix` is now `(n_field, n_sims)` -- **every** field-sample lineup's score in **every** aligned iteration, not one draw. Per iteration, our lineup's rank is `count_nonzero(field_matrix <= lineup_draws) / n_field` -- a real percentile against the real (correlated, same-environment) field. `itm_pct`/`top1_pct`/`top01_pct`/`ev_pct` are now all computed directly from that per-iteration implied rank (rank -> real payout-tier lookup, averaged) -- no more tier-proportion formula, no more marginal cutoffs. This **replaces** the table's existing `ev_pct`/`itm_pct`/`top1_pct`/`top01_pct` values in place (not a parallel "empirical" field this time -- Cam was clear the old numbers are simply wrong) and folds the histogram in for free.
    - **Classic `/api/optimize`** (~line 4295): previously scored exactly one randomly-chosen field lineup per iteration via a 10,000-iteration Python loop. Now scores **every** cached archetype-field lineup (`FIELD_SAMPLE_CACHE`, ~1,000 builds, reusing the existing `get_player_trial_scores` helper) across all iterations, vectorized, into `field_matrix`. Same treatment for the no-week-given fallback path (now builds and scores 300 random field lineups instead of one-per-iteration).
    - **Showdown `/api/optimize_showdown`** (~line 5285): didn't need new field-building work -- `field_vecs` (every one of the 1,200 field builds' full score vector) was already being computed for `field_max_scores`/`first_pct`, just never used for ranking. Now passed straight through as `field_matrix`.
  - [frontend/src/components/LineupHistogramModal.jsx](frontend/src/components/LineupHistogramModal.jsx): "Sim ROI" tile now reads `lineup.ev_pct` (the corrected value) instead of the now-removed `ev_pct_empirical`; updated the docstring/tooltip to describe full-field ranking instead of the old single-opponent method.
- **Verification Performed:**
  - `ast.parse` on `app.py` (clean) and `npx vite build` on the frontend (clean) after every edit.
  - **Could not re-verify live in the browser this pass** -- both dev servers (`backend-api-dfs` :8002, `dfs-frontend` :5173) are currently held by another chat session on this machine; this session's `preview_start` was refused ("port in use by another chat's dev server"). Did not attempt to kill that process since it isn't this session's to stop.
  - Instead verified the new `_compute_lineup_field_stats` function directly: extracted it from `app.py` (regex-isolated, no FastAPI import, so no side effects from the app's startup caches/DK scraper) and ran it against synthetic data in `<scratchpad>/test_field_stats.py`. Three cases: (1) a lineup drawn from the *same* strength distribution as a correlated 1,000-lineup field → `itm_pct 58.0`, `ev_pct +1.17%` (this is the number that matters -- realistic, break-even-ish, not the +7,000%+ the old code produced for ordinary lineups); (2) a lineup engineered to always beat the entire sampled field → `itm/top1/top01_pct = 100%`, `ev_pct 99,900%` (mathematically correct for guaranteed-1st against this test's payout table, not a bug); (3) a lineup engineered to always lose → `ev_pct -100%`. Also benchmarked cost at realistic scale: ranking 150 lineups against a 1,000×10,000 field matrix took ~3.1s total (~21ms/lineup); building that field matrix from scratch (1,000 field lineups × 9 players × 10,000 sims) took ~0.3s -- both cheap relative to the optimizer's existing multi-second generation time.
- **Current System Status:** Backend/frontend changes are saved and pass static checks + the standalone functional test, but have **not been exercised through a live `/api/optimize` or `/api/optimize_showdown` request** this pass -- do that first thing next session (or once the other chat's dev servers are free) before telling Cam it's fully confirmed. Watch specifically for: real generated lineups now showing much more moderate EV%/Top1% than before (expected and desired), and total request latency staying in the same few-seconds ballpark it was before (the benchmark above says it should, but that used synthetic trial-score arrays, not the real per-player parquet-backed ones).
- **Immediate Next Steps for the Next Agent:**
  1. Restart/reclaim `backend-api-dfs` + `dfs-frontend`, run a real ⚡ Optimize on both the classic and Showdown Optimizer, and confirm the table's `ev_pct`/`itm_pct`/`top1_pct` now read sane (not +1000s%) and the "Range of Outcomes" modal still renders correctly off the new `field_matrix`-derived histogram.
  2. If EV still reads too high after this, the next suspect is the field SIZE/composition itself (K=1000 for classic, 1,200 for showdown) relative to real `total_entries` (up to 50,000) -- worth sanity-checking whether the archetype-sampled field is actually as sharp/competitive as a real large-field GPP, not just whether the ranking math against it is correct (that part is now fixed).
  3. Past/settled-lineup histograms are still explicitly deferred (see prior entry) -- unchanged, still needs a decision from Cam.

---

### [2026-09-12] Handoff from Claude Sonnet 5 (per-lineup "Range of Outcomes" histogram — both optimizers)

- **Active Task:** Cam saw ETR/The Solver's per-lineup range-of-outcomes histogram (screenshot: click a lineup in a post-lock sim dashboard, see how often it finishes in each percentile) and wants the same for our own generated-lineup tables — both for lineups yet to be played and, later, for settled ones. He also flagged that the headline EV%/Top1% numbers have looked "gaudy" lately and suspects we've drifted from scoring lineups directly off the sims. Confirmed scope with him mid-session: this is about the existing Generated-Lineups table (ordered as the export CSV would be), click a row → histogram, ETR-style. The "past/settled lineup" half of the ask is explicitly deferred — see below.
- **Root cause of the "gaudy" numbers, found while investigating:** both `/api/optimize` and `/api/optimize_showdown` already draw 10,000 aligned per-iteration lineup-score draws (`lineup_score_draws` / `lineup_draws`) and a matching simulated field (`field_scores`) to compute the table's stats — but immediately reduce them to 8 aggregate scalars via `_compute_lineup_stats_direct` and discard the raw draws. That function's `ev_pct` is a **tier-proportion formula** (assumes payout tiers split proportionally across itm/top1/top01 cutoff bands) rather than an actual per-iteration rank → real-payout lookup, so it can diverge from what the sims actually show, especially for lopsided or multi-tier payout structures.
- **Files Modified:**
  - [src/api/app.py](src/api/app.py): new `_compute_lineup_histogram()` (added right after `_compute_lineup_stats_direct`) — takes the same already-computed `lineup_draws`/`field_scores` arrays, computes each iteration's finish percentile (`searchsorted` against the sorted field), an implied contest rank (`total_entries * (1 - percentile/100)`), and a **true empirical EV%** by looking up the real payout tier at that implied rank per iteration and averaging (`ev_pct_empirical`, distinct from — not replacing — the table's existing `ev_pct`, so nothing currently displayed changed). Bins the percentile distribution into 40 buckets for the chart. Also returns `cash_percentile`, `score_min/mean/max`, `rank1_count`, `rank_top10_count`. Wired into both the classic `/api/optimize` lineup loop (~line 4372) and the showdown `/api/optimize_showdown` loop (~line 5306) via `**hist` spread into each lineup result dict, so every lineup returned by either endpoint now carries a `histogram` block for free — no new endpoint, no persistence change (rides along in `builds/*.json` same as everything else in that dict).
  - [frontend/src/components/LineupHistogramModal.jsx](frontend/src/components/LineupHistogramModal.jsx) (new): the ETR-style modal — pure inline SVG bar chart, same no-chart-lib/theme-aware pattern as `components/GameDistribution.jsx` (`Bars` component). Stat tiles (Sim ROI/Cash Rate/Ceiling/Average/Floor/Top1%/1st Place/1-10th Place) + the histogram, bars colored green at/above the cash-line percentile (gold dashed marker) and cyan below, hover shows the exact sim-run count per bucket.
  - [frontend/src/pages/ShowdownOptimizer.jsx](frontend/src/pages/ShowdownOptimizer.jsx) and [frontend/src/pages/Optimizer.jsx](frontend/src/pages/Optimizer.jsx): added `histLineup` state + a "📊 Range of Outcomes" button in each lineup row's existing expanded-detail strip (next to Port.Score / Proj Median), opening `<LineupHistogramModal>`. Did not touch the primary row-click behavior (still toggles the player-detail expansion, unchanged).
- **Verification Performed:** `python -c "ast.parse(...)"` on `app.py` after each edit (clean). `npx vite build` on the frontend (clean, no new warnings). Live end-to-end in the browser against both dev servers (`backend-api-dfs` :8002, `dfs-frontend` :5173): ran a fresh ⚡ Optimize on both the Showdown Optimizer (NE@SEA) and classic Optimizer (main slate), expanded a row, clicked "Range of Outcomes" on each, confirmed the chart renders, hover tooltip updates correctly ("0-3 percentile: 2585 of 10,000 sim runs (25.9%)"), and cash-line marker positions correctly. **Concretely reproduced the "gaudy EV" complaint and the fix's value on a real lineup**: the classic Optimizer's table showed `ev_pct` (old formula) of +7566.43% for one lineup; the new modal's empirical Sim ROI for the same lineup read +10,309.16% — a materially different number computed directly from the sim draws, with the histogram itself showing why (this build's cash rate really is 98.47% against the current simulated field — the field/entry assumptions may be worth a separate look, but the two metrics no longer have to trust each other blindly).
- **Current System Status:** Both dev servers (`backend-api-dfs`, `dfs-frontend`) were left running in the Browser pane for Cam to poke at when he's back. No regressions observed; existing `ev_pct`/`itm_pct`/etc. columns are untouched, `ev_pct_empirical` is purely additive.
- **Immediate Next Steps for the Next Agent:**
  1. Cam may want to discuss **replacing** the table's `ev_pct` column with `ev_pct_empirical` (or showing both) now that the empirical version exists — hold off doing this silently; it's a visible number he's used to and the two can legitimately disagree (see the "root cause" note above for why).
  2. **Past/settled lineups are explicitly out of scope for this pass.** Cam's original ask also wanted this for already-played slates. `scripts/dfs_ownership/score_paper_entries.py` already computes one *realized* percentile finish per settled paper entry (real DK standings, not simulated) — that's a single dot, not a range. Showing a genuine *range* for a played slate needs a simulated distribution built from that week's stored player-level projections (`data/interim/sim_results_2026_players.parquet`), and it's unconfirmed whether that parquet is retained long enough after later weeks overwrite the cache, or would need archiving per-week first. Needs a decision from Cam on data source/approach before building.
  3. Consider whether `_compute_lineup_histogram`'s O(n_bins) + O(n_sims) per lineup cost is worth trimming if lineup counts grow much past the ~20 tested here (untested at higher counts).

---

### [2026-09-12] Handoff from Claude Sonnet 5 (Phases 3+4 — paper trading + evaluation tab)

- **Active Task:** Cam paused Phase 2's classic-optimizer rollout ("game script sniper" — the Game Read conditional-scoring UI; backend `iteration_filter` support stays built but unwired for classic) and asked to work out Phases 3 (paper-trading spine) and 4 (evaluation tab) together. Built both, end-to-end, verified on real settled NE@SEA/SF@LA contests.
- **Refactor first:** factored the standings-CSV parsing guts out of `build_ownership_dataset.py` into `scripts/dfs_ownership/standings_parser.py` (DST_NICKNAMES, filename/lineup-string regex, `read_standings`, `ownership_from_entries`, plus new `player_key` — a canonical (name,team,pos)→join-key helper used consistently across salary snapshot / standings / a paper lineup / the eval scripts — and `actual_scores_from_summary`, pulling each player's real result straight off DK's published summary block). `read_standings` now also captures the `Points` column (needed for field-score distributions; wasn't kept before). `build_ownership_dataset.py` re-verified byte-for-byte equivalent output after the refactor.
- **Phase 3 — paper trading:**
  - `src/api/paper_store.py` (new) + `POST/GET/DELETE /api/paper/entries` — a lineup flagged "I'm actually entering this" is written to `data/dfs_ownership/<year>/week_<NN>/<slate_id>/paper_entries.json`, alongside that slate's salaries/standings (not the workspace-slot system's separate `slate_key`).
  - `frontend/src/pages/ShowdownOptimizer.jsx`: a **📝** column on every result row (Optimize or Lab) + a small "Paper trade into [contest name] [fee] [max]" strip in the Portfolio Summary card. Saved rows get a ✓; resets on a new Optimize/Lab run or game switch.
  - `scripts/dfs_ownership/score_paper_entries.py` (new, offline) — once a standings CSV matching an entry's `contest_name` shows up, computes the lineup's real score (CPT ×1.5, missing-player-in-summary → 0 and flagged), real rank/percentile (`beat_field_pct` = % beaten, `finish_percentile` = rank as % of field), and — joined from `ownership_actuals.parquet` — actual vs predicted total ownership. Writes `_processed/paper_results.parquet`.
  - **Bug caught and fixed mid-build**: the ownership backcheck join used raw `normalize_player_name(name)` as the key, but a DST in a saved lineup is the generic sim name "Defense" (disambiguated only by `team`) while the ownership archive uses DK's real nickname ("Patriots") — silently produced `actual_total_ownership: null` for any lineup with a defense. Fixed by switching the join to the new `player_key()` helper (by-team for DST), matching how the score lookup already worked. Verified with a real Optimize-generated lineup end to end: predicted 53.75 → actual 53.35 (rank 941/1173, beat 19.9%); ownership 24.2% predicted → **168.6% actual** — another concrete, now-quantified data point for the known ownership-model under-prediction problem.
- **Phase 4a — field analysis** (independent of paper trading — needs only a settled contest, not our own picks): `scripts/dfs_ownership/eval_field.py` (new, offline). Per settled contest: winner + lineup, score percentile cutoffs (top 1% / 0.1%, mean/median/p90/p99), the combined-ownership profile of the top ~20 finishers (chalky or contrarian winners?), and the **hindsight-optimal lineup** — an exact PuLP ILP solve (two solvers written, showdown CPT+5FLEX and classic QB/RB/RB/WR/WR/WR/TE/FLEX/DST) over real per-player results, merged **across every settled contest for that slate** (a small field's summary can be missing a player a bigger field on the same slate captured — first cut used one contest's own summary and got inconsistent hindsight scores across contests of the same slate; fixed by merging). Writes `_processed/field_eval.parquet`. Verified on all 12 real settled contests (6 NE@SEA + 6 SF@LA): e.g. NE@SEA's biggest field (`minimax`, 237k entries) had a winner who found the literal 106.0-point optimal lineup exactly; smaller/sharper fields (`hard_count`, `wildcat`) left 8-9 points on the table even for their own winner.
  - **Known, documented gap:** no cash line / cashers count — the standings export has no payout-structure column, only entrant scores. Reported as percentile cutoffs instead; a real cash line would need the payout table snapshotted pre-lock.
- **Backend reads:** `GET /api/eval/field` / `GET /api/eval/paper` (optionally `?slate_id=`) — thin JSON views over the two parquet files (`_read_parquet_rows` helper, NaN→null since bare `NaN` isn't valid JSON and some strict parsers reject it).
- **Frontend — new page `frontend/src/pages/EvaluationTab.jsx`** (🧪 in the nav, wired into `pagesConfig.js`/`App.jsx`, wide-container like the optimizers): Week/Game picker (same pattern as the other DFS pages) → **Field Analysis** table (one row per settled contest: winner, hindsight-optimal + Δ, percentile cutoffs, top-N ownership%) and **Paper Trades** table (predicted vs actual score/ownership, rank, beat-field%, finish percentile), plus a small "N pending" note for paper entries saved but not yet settled.
- **Verified live, full round trip:** ran ⚡ Optimize on the Showdown Optimizer → 📝 saved a real generated lineup into "hard_count" → ran `score_paper_entries.py` → reloaded the Evaluation page → both the Field Analysis (all 6 NE@SEA contests) and the just-settled Paper Trade row rendered correctly, including the ownership-backcheck fix. eslint clean on every touched/new frontend file (same 5 pre-existing findings in `api.js`, untouched). Test data cleaned up afterward; `field_eval.parquet` / `ownership_actuals.parquet` / `features.parquet` left in place (real, not test data).
- **README** (`data/dfs_ownership/README.md`, now v0.3) updated: new scripts, paper-trading + field-eval workflow steps, `paper_entries.json` / `paper_results.parquet` / `field_eval.parquet` schemas, corrected the git section to match the actual `.gitignore` rule (was documenting a stale pattern).
- **Not done / explicitly deferred:** classic optimizer's own 📝 paper-trade button and Game Read UI (backend ready for both, no frontend yet) — same "showdown first, classic follows once proven" sequencing as the save slots. Phase 5 (season-long field/user construction analytics) untouched.

---

### [2026-09-12] Handoff from Claude Sonnet 5 (workspace save slots — wired into the classic optimizer too)

- **Active Task:** Cam asked to wire the 3-save-slot switcher (built last handoff for the showdown optimizer) into the classic `Optimizer.jsx` too.
- **`frontend/src/pages/Optimizer.jsx`:** `classicSlateKey = classic_${platform}_${draftGroupId ?? 'default'}`, snapshot = `{settings, overlay, lineups: optimizerLineups, portfolio: portfolioStats, labRows, resultsMode, view}`, rendered via `<SlotSwitcher>` right under the DK Slate picker. **Gated on a new `weekHydrated` flag** (set once the pre-existing per-week `state.json` hydration completes) so the two persistence layers can't race to set `settings`/`overlay` on mount — the slot system, being more specific (per-slate, not just per-week), intentionally hydrates second and wins. Additive to, not a replacement for, the existing per-week state.json/builds autosave (a different concept: that's an ever-growing history, this is 3 deliberate "save files") — both keep running; the legacy file just ends up mirroring whichever slot was last active, which is harmless.
- **Bug found and fixed (in both `Optimizer.jsx` and last handoff's `ShowdownOptimizer.jsx`):** the hydrate callbacks did `if (data.settings) setSettings(s => ({...s, ...data.settings}))` — a merge that **silently no-ops when a slot has never been saved** (`data.settings` is `undefined`), so switching to an empty slot kept whatever the previous slot had in memory instead of actually looking empty. Caught it live: excluded a player on Slot 1, switched to Slot 2, the exclusion was still showing. Fixed by resetting to defaults (`{...defaultSettings, ...(data.settings||{})}` / `data.overlay || emptyOverlay()`) instead of conditionally skipping. One wrinkle for classic specifically: `platform` is embedded in `classicSlateKey` itself, so the reset pins `platform` to its current value rather than resetting it too — resetting it would flip the slate key mid-hydration.
- **Verified live, full round-trip, on a genuine reload (not just an in-app nav — a same-hash `navigate` call doesn't remount and was a false pass the first time around, worth remembering for next time):** excluded Jordan Love on Slot 1 → autosaved to disk → switched to Slot 2 → pool clean → switched back to Slot 1 → exclusion restored. Confirmed ⚡ Optimize (20 lineups) still works normally afterward. eslint clean (same 4 pre-existing findings in this file, unchanged; `ShowdownOptimizer.jsx`/hook/component fully clean).
- **Noticed in passing, spawned as a separate task, not fixed here:** `Optimizer.jsx`'s Generated-Lineups table renders `{slotted.map((p, pi) => renderSlot(p))}` with no `key` — pre-existing React console warning, unrelated to this work.
- **Test data cleanup:** wiped `data/workspace/` (all test slots created across both sessions) and reverted the one line of test pollution that leaked into the tracked `data/optimizer/2026/week_01/state.json` via `git checkout --`.
- Both sites restarted/confirmed healthy per the prior handoff; no backend changes this turn (frontend-only).

---

### [2026-09-12] Handoff from Claude Sonnet 5 (workspace save slots — 3 per slate, showdown optimizer wired)

- **Active Task:** Cam confirmed the save-slot scope from the last handoff's proposal (3 slots **per slate**, not global) and asked to build it, plus put the sites back up.
- **Backend — `src/api/workspace_store.py`** (new): file-backed store for 3 switchable, autosaved workspace snapshots per `(season, week, slate_key)`, same atomic-write pattern as `optimizer_store.py`. `slate_key` is an opaque, path-sanitized string the frontend derives (`showdown_<game_id>` / `classic_<platform>_<draft_group_id>`), so different games/slates sharing a week each get their own 3 slots. Layout: `data/workspace/<season>/week_<NN>/<slate_key>/{active.json, slot_1.json, slot_2.json, slot_3.json}`. Each slot file is `{label, updated_at, data}` — `data` is an opaque frontend-shaped blob, untouched here. Additive to, not a replacement for, `optimizer_store`'s per-week state/builds (that's a different concept — an ever-growing autosave history vs. 3 deliberately-swapped "save files").
  - `src/api/app.py`: 6 new endpoints — `GET /api/workspace/slots` (lightweight metadata: label/updated_at/has_data for all 3 + which is active, no payloads), `GET /api/workspace/slot` (one slot's full blob), `PUT /api/workspace/slot` (autosave target), `POST /api/workspace/active` (switch), `PATCH /api/workspace/slot` (rename), `DELETE /api/workspace/slot` ("start fresh" — wipes data, keeps the label).
  - Verified via curl: write → list (has_data flips true, label sticks) → read → switch-active → clear (data empties, label survives).
- **Frontend:**
  - `frontend/src/hooks/useWorkspaceSlots.js` (new) — state-shape-agnostic hook: caller hands a `snapshot` (useMemo'd) to autosave (1.2s debounce) and an `onHydrate(data)` callback to apply a loaded blob back onto local state; the hook only fetches/debounces/switches, never interprets the blob. Handles the initial-load race (suppresses autosave while a slot is loading, so hydrating doesn't immediately re-save itself). Written to satisfy this project's strict eslint (`react-hooks/refs`, `set-state-in-effect`) — ref writes moved into their own effects, the initial-load effect's setState calls all run inside a `Promise.resolve().then()` continuation rather than the synchronous effect body.
  - `frontend/src/components/SlotSwitcher.jsx` (new) — 3-tab presentational switcher: click to switch, click-again/double-click to rename inline, a ↺ with a confirm step to clear the active slot. Reused as-is by any page that adopts slots.
  - `frontend/src/pages/ShowdownOptimizer.jsx` — wired in: `slateKey = showdown_${gameId}`, snapshot = `{edits, frozenOwn, settings, labRows, lineups, portfolio, notes, resultsMode, view, scenario}`, rendered next to the Game selector in the control bar. Known v1 gap: `GameDistribution` doesn't accept a controlled selection, so restoring `scenario` correctly re-conditions ⚡ Optimize/Lab and shows the "conditioned" badge, but the heatmap itself won't re-draw the box after a reload — cosmetic only.
  - **Verified live, full round-trip**: excluded a player on Slot 1 → autosaved to disk (confirmed the exact JSON) → switched to Slot 2 → pool reset (exclusion gone) → switched back to Slot 1 → exclusion restored → **navigated away to the Simulator page and back** → Slot 1 still active, exclusion still restored. Confirmed ⚡ Optimize (20 lineups) still works normally afterward — no regression. eslint clean on all new/changed files.
- **Not done yet:** classic `Optimizer.jsx` doesn't have the slot switcher wired — the backend/hook/component are fully reusable, it's just not plugged in yet (that page already has its own week-level autosave + "builds" history to coexist with, worth doing carefully rather than rushed). Queued as the next slice of this feature.
- **Sites restarted** (backend `:8002`, frontend `:5173`) at Cam's request, both healthy.

---

### [2026-09-11] Handoff from Claude Sonnet 5 (Phase 2 "cheap" conditional lineup scoring — Game Read on the showdown optimizer)

- **Active Task:** Phase 2 of the 5-phase workspace plan — box-select on the game distribution → re-score lineups conditional on that slice of games. Cam confirmed: build the cheap mode now (re-score, no re-solve), defer the exact/in-depth mode; also asked to think through extending this to the classic optimizer and raised two bigger design items (Explore/Build/Evaluate persistence as 3 save-file-style slots; the evaluation tab) that are proposed but not yet built — see the reply for the design writeup and open question on the save-slot scope.
- **Backend — `src/api/app.py`:**
  - Factored the `game_distribution` builder (binned total/margin histograms + sparse joint grid + raw per-iteration arrays) out of `run_simulation` into a standalone `_build_game_distribution(game_df, away_team, home_team, ref_total, ref_spread)`, shared with a new lightweight `GET /api/game_distribution?away_team=&home_team=&week=`. That endpoint does ONLY this — no player-projection simulation — so a page that just needs the game's outcome read (the showdown optimizer) gets it fast and **always with the exact `raw` iteration arrays** (unlike `week_sim_results`' binned-only version), no "run a fresh sim first" step.
  - **The actual conditional mechanism is a one-line change, applied to both optimizers**: `ShowdownOptimizeRequest.iteration_filter` / `OptimizeRequest.iteration_filter` (list of raw 0-999 iteration ids, straight from `game_distribution.raw.iteration` for a box-selected region) replaces the uniform `aligned = rng.integers(0, 1000, ...)` draw with `rng.choice(iteration_filter, ..., replace=True)`. Since our lineup(s) AND the synthetic field both read off the same `aligned` indices, this reweights the whole joint game-environment draw for everyone at once — "given this game lands in this total/margin box" — with no re-solve needed. Classic works too: every game in a slate shares one full-slate iteration index, so conditioning on one game's box conditions the whole field + lineups. Works with both generated and `manual_lineups` (Lineup Lab). Response `portfolio` gains `iteration_filter_frac` / `iteration_filter_n` so the UI can show what fraction of games the numbers are conditioned on.
  - **Verified via curl (before/after, both optimizers):** showdown NE@SEA, conditioning a lineup on total<35 (204/1000 sims): EV% 1193.66% → 1373.62%, ITM% 33.87% → 37.15%. Classic, same condition: EV% 1728.28% → 1104.64%. `GET /api/game_distribution` returns in ~50ms with real `raw` arrays (11.7KB).
- **Frontend — `frontend/src/pages/ShowdownOptimizer.jsx`:** new **🎯 Game Read** toggle (next to ✍️ Lineup Lab) → embeds `<GameDistribution>` (fetched via new `ApiService.getGameDistribution`, no mock fallback) right on the optimizer, keyed to the active game. Box-select sets a `scenario` state; both `handleOptimize` and `scoreLab` include `iteration_filter: scenario?.idx`. The toggle button itself shows the conditioning %, and the results header gets a `🎯 conditioned · N% of games` badge (from `portfolio.iteration_filter_frac`) so conditional numbers are never mistaken for the baseline. `pickGame` resets `gameDist`/`scenario`.
- **Not done yet — flagged, not blocking:** live drag-to-select re-verification in browser this session — the Browser pane reported a 0×0 viewport (hidden pane), so `getBoundingClientRect`-based drag math had nothing to compute against. The mechanism itself was verified live in the Phase 1 session (real drag → correct conditional readout) and the backend is curl-verified end-to-end; only the exact UI click-path in *this* build is unverified by eye. Cam should give it a try once the pane's visible.
- **Classic optimizer's own Game Read panel**: backend ready (`iteration_filter` works identically), frontend not wired — a classic slate spans many games, so "which game do you condition on" is a real UI decision (a per-game drill-down/slide-over, discussed but not designed in detail) rather than a straight port of the showdown embed. Queued behind the save-state work.

---

### [2026-09-11] Handoff from Claude Sonnet 5 (Lineup Lab ported to the classic optimizer)

- **Active Task:** Cam asked for the showdown Lineup Lab (hand-build a lineup, score it through the tournament field sim) on the classic `/api/optimize` optimizer too.
- **Backend — `src/api/app.py`:** new `ManualLineup` model (`{qb, rb[2], wr[3], te, flex, dst, label}`) + `OptimizeRequest.manual_lineups`. `optimize_lineups` keeps ALL players when `manual_lineups` is set (an excluded flag is a generation constraint, not "can't score" — mirrors showdown), resolves each named slot to a pool player (case/punctuation-insensitive, `Name|TEAM` to disambiguate a shared name), and **validates the declared slot matches the player's actual position** (QB/RB/RB/WR/WR/WR/TE/DST must match exactly; FLEX must be RB/WR/TE) — classic scoring has no CPT-style multiplier to visually catch a mis-slotted player the way showdown does, so this is a real correctness gate, not just showdown-parity. Skips the ILP generation loop entirely; scores the resolved lineups through the same trial-map / field-scores / payout-structure path as generated lineups. Response gains `mode: 'manual'|'optimize'` and each lineup gains `label` + `over_salary_cap`.
- **Frontend — `frontend/src/pages/Optimizer.jsx`:** extracted `buildOptimizerPlayersPayload()` (shared by ⚡ Optimize and the Lab). New **✍️ Lineup Lab** toggle next to ⚙️ Settings → panel with a label input + 9 position-filtered `<select>`s per row (QB only lists QBs, FLEX lists RB/WR/TE, already-picked players disabled), "+ add lineup", "Score lineup(s) →". Results render in the same `Generated Lineups` table via `resultsMode` ('optimize' | 'lab') — banner text, `#` column shows the label, salary cell gets a `⚠` + over-by tooltip when over cap. Lab state resets on week change (in the existing week-hydration effect).
  - **Guarded against `ApiService.optimizeLineups`'s mock fallback**: unlike showdown's `optimizeShowdown` (no mock, returns `null` on failure), the classic endpoint falls back to `generateMockLineups(payload)` on any fetch failure/retry-exhaustion — which would otherwise let a bad Lab lineup (typo, backend hiccup) silently render as if it were real scoring. Fix: the Lab treats a response missing `mode: 'manual'` as a failure (mock responses never carry `mode`), not just a missing `lineups` array.
- **Verified:** curl — valid lineup scores (`mode: 'manual'`, label + salary + EV/ITM/Top% passthrough); a WR placed in the QB slot correctly 400s (`"Ja'Marr Chase is WR, not eligible for QB"`) once the backend was force-restarted to pick up the validation edit (see note below). Browser — classic Optimizer: Lab opened, position-filtered selects populated from the live 282-player pool, built a 9-man lineup, scored → "✍️ Lineup Lab · 1 hand-built lineup scored", full stat row, $63,000 ⚠ over-cap flag, expandable player cards. Confirmed ⚡ Optimize still generates 20 lineups normally afterward (`resultsMode` resets correctly). eslint clean (only the 4 pre-existing findings in this file, unchanged).
- **Note — `--reload` flakiness:** `.claude/launch.json`'s `backend-api-dfs` was changed outside this session back to `uvicorn ... --reload` (no `--reload-dir`, dropped since the 09-09 fix for orphaned multiprocessing workers). Observed it **not** picking up an app.py edit until a manual stop/start — don't trust it mid-session; force-restart after backend changes.

---

### [2026-09-10] Handoff from Claude Sonnet 5 (Lineup Lab — score hand-built showdown lineups)

- **Active Task:** Quick side-tool Cam asked for before the distribution-panel presentation work + optimizer integration: enter a hand-built showdown lineup, run it through the same tournament field sim as the optimizer, see EV% / ITM% / Top 1% / Top 0.1% / 1st% / dupe est.
- **Backend — `src/api/app.py`:** `ShowdownOptimizeRequest` gains `manual_lineups: Optional[List[ManualShowdownLineup]]` (`{cpt, flex[5], label}`, names matched to the pool case/punctuation-insensitively, `Name|TEAM` to disambiguate). When set, `optimize_showdown` **skips generation entirely** — resolves the hand lineups to pool indices and runs them straight into the existing field build + correlated-draw scoring + payout math. Same code path, same `field_source` (prefers `dfs_week_N` parquet). Excluded players stay in the pool for manual mode (an exclude is a generation constraint, not "can't score"). Per-lineup response adds `label` + `over_salary_cap` (a stud CPT at 1.5× blows past $50k easily — flagged, not rejected). Response gains `mode: 'manual'|'optimize'`. Added `import re` (was missing).
- **Frontend — `frontend/src/pages/ShowdownOptimizer.jsx`:** extracted `buildPlayersPayload(all)` (shared by ⚡ Optimize and the Lab). New **✍️ Lineup Lab** toggle in the pool header → collapsible panel: per row a label + CPT + 5 FLEX `<select>`s (pool sorted by salary, already-picked players disabled), "+ add lineup", "Score lineup(s) →". Results render in the **same table** as the optimizer with a `resultsMode` banner ("✍️ Lineup Lab · N hand-built lineups scored · field: …"), the row `#` column shows the label, salary cell shows `⚠` + over-by tooltip when over cap. `pickGame` resets the Lab.
- **Verified:** curl (3 lineups incl. a DST, over-cap flagging, name resolution) + in-browser (NE@SEA: open Lab → pick JSN CPT + 5 FLEX → Score → "1 hand-built lineup scored · field: dfs-week_1 · 10k sims", full stat row, expandable player cards). Confirmed ⚡ Optimize still works after the `buildPlayersPayload` refactor. eslint + `ast.parse` clean.
- **Note:** on NE@SEA the numbers are inflated (EV +1276%, total own 181%) — dead slate so salaries are estimated, plus the known ownership-model over-concentration + showdown-EV calibration gap. On SF@LA tonight with real DK salaries loaded + a fresh sim the Lab output is meaningful.
- **Next:** back to the distribution-panel presentation + how to fold the heatmap/box-select into the optimizing workflow; then the ownership rebuild after SF@LA.

---

### [2026-09-10] Handoff from Claude Sonnet 5 (game-outcome distribution panel — Phase 1)

- **Active Task:** Phase 1 of a 5-phase plan Cam scoped for the DFS workspace (game distribution graphs → conditional lineup re-scoring → paper-trading spine → evaluation tab → field/user analytics). Phase 1 = total-points + score-differential histograms + a joint total-vs-differential heatmap on the Simulator's "Sim Game Summary" view. Ownership modelling deliberately deferred to after tonight's SF@LA game.
- **Backend — `src/api/app.py`:**
  - `run_simulation` response gains `game_distribution`: pre-binned `total` histogram `{lo,step,nb,p[]}`, `margin` histogram (margin = away − home, positive = away ahead), sparse `joint` grid `{t_lo,t_step,t_nb,m_lo,m_step,m_nb,cells:[[mi,ti,p]]}`, scalars (`n`, `mean_total`, `mean_margin_away`, `ref_total`, `ref_spread_home` from the schedule / overrides), plus `raw:{iteration[],total[],margin[],weight}` — the per-iteration arrays for exact conditional filtering. `_nice_step` picks bin widths (~15 bins). ~2 KB/game binned, ~200 KB with raw.
  - `_compute_one_game` (the `week_sim_results` per-game builder) includes `game_distribution` **with `raw` stripped** — 16 games × 1000 iters of raw arrays would bloat that cached payload. So the pre-loaded week view gets the binned panel; a fresh single-game `/api/simulate` additionally unlocks exact per-iteration conditional analysis.
- **Frontend — `frontend/src/components/GameDistribution.jsx` (new, ~240 lines), rendered in `Simulator.jsx`** inside the summary panel (after "Simulated Team Offensive Averages"), guarded by `simResults.game_distribution`:
  - Hand-rolled inline SVG, no chart lib (project has none). Theme-aware via the app's CSS vars.
  - Total-points histogram + score-differential histogram, each with a solid mean line and a dashed Vegas reference line (total line / spread), hover shows bin probability.
  - Joint heatmap: X = total, Y = differential, cell opacity ∝ √(density). Vegas crosshair + zero-margin line drawn. **Drag a box → conditional readout**: % of all games in the box, conditional mean total / mean margin, away-win %, over %. Exact (per-iteration, + iteration ids for Phase 2 lineup re-scoring) when `raw` is present; otherwise approximated from the binned joint grid (bin-centre weighted) and labelled "approx from binned grid — run a fresh sim for exact". `onSelect(sel)` prop is wired but not yet consumed — Phase 2 hooks the optimizer to it.
- **Verified** (DOM inspection — the preview pane wouldn't paint for screenshots, window-focus issue): backend `game_distribution` present & sane on both `/api/simulate` (raw, NE@SEA mean_total 45.5 vs Vegas 44.5, mean_margin −4.0 vs spread −3.5) and `week_sim_results` (binned, ~2 KB/game, raw stripped); frontend panel renders all three charts, drag-select produces the conditional readout, clear resets, eslint clean, no console errors. `week_1_sim_results.json` disk cache was regenerated (17:21) so it now carries `game_distribution`.
- **`.claude/launch.json`:** `backend-api-dfs` reverted to plain `uvicorn ... --port 8002` (no `--reload` — the reloader keeps orphaning multiprocessing workers on the port). `src/` changes need a manual `:8002` restart.
- **Immediate next steps:** (1) Ownership model rebuild after SF@LA settles — diagnosis already done (formula too concentrated, CPT collapses to one name, projections don't match market tiers; plan = salary-implied-proj blend + role gate + widen softmax + calibrate curve to NE@SEA realized). (2) Phase 2: box-select → conditional lineup re-score, embedded in ShowdownOptimizer, consuming `GameDistribution`'s `onSelect`. (3) Minor: after slate-wide "RUN ENGINE" the Simulator resets `simResults` from the (raw-less) week cache, so the panel falls back to approx even post-sim until a per-game re-sim — worth making the fresh single-game `raw` stick for Phase 2.

---

### [2026-09-10] Handoff from Claude Sonnet 5 (DFS ownership data pipeline — snapshot + parser)

- **Active Task:** Build the projected-ownership model's data pipeline — the two scripts that turn "contests Cam played" into a training set. Cam's call: target the contests he actually enters, spread across field-size / stakes; don't chase 50 CSVs.
- **`scripts/dfs_ownership/snapshot_slate_salaries.py`** (new) — pre-lock feature snapshot. For every week-N game with a live DK "Showdown Captain Mode" slate, writes `data/dfs_ownership/2026/week_NN/showdown_<away>_<home>/salaries_prelock.csv` (`name,team,pos,salary,cpt_salary,dk_flex_id,dk_cpt_id,snapshot_ts`) + merges `manifest.json` (draft group, `gameday/gametime`, Vegas block: `total_line`, home-favored `spread_line`, `home_implied`/`away_implied` from `data/external/schedule_2026.csv`). Appends every run to `_processed/salary_history.parquet` (1 row/player/snapshot — keeps intra-week salary movement). `--game SF_LA` limits to one; `--main` also snapshots the classic Main Slate; **`--backfill`** refetches slates already gone from the DK *lobby* via the `dk_draft_group_id` in each `manifest.json` — **DK's draftables endpoint keeps serving a locked slate's pool for a while after the lobby listing vanishes at kickoff** (confirmed: pulled all 68 NE@SEA players a day after the game). Late pulls flagged `late_snapshot: true`.
- **`scripts/dfs_ownership/build_ownership_dataset.py`** (new) — post-settle parser. Walks every slate folder, pairs each DK "Export Full Standings" CSV with that slate's `salaries_prelock.csv`, writes `_processed/ownership_actuals.parquet` (target) + `_processed/features.parquet` (v1 matrix: value + Vegas), one row per (contest × player), prints a `field_bucket × stakes_tier` coverage table.
  - **Standings CSV naming: `<name>_<price>_<xmax>max.csv`** (e.g. `hard_count_20_5max`, `minimax_.5_150max`). name = contest identity; price = entry fee in dollars, fractional ok (`.5` = $0.50) or `c` suffix for cents; xmax = max entries/user. **Field size is NOT in the name** — parsed as `max(Rank)` off the CSV. Entry fee + max-entries are in the name because the export doesn't contain them.
  - **DK's showdown export publishes the CPT/FLEX ownership split directly** — the summary block lists every player twice with `Roster Position` = CPT / FLEX and its own `%Drafted`. Parser uses that as the target when present (`own_source="dk_published"`), and always *also* recomputes FLEX/CPT counts from the `Lineup` strings (`CPT <name> FLEX <name>…`, regex) as a cross-check + for `top_lineup_dupe_count` (DK never publishes duplication). On week-1 NE@SEA the two agreed within 0.3%. Older exports w/o `Roster Position` fall back to the lineup-string split.
  - DST in `Lineup` strings comes as a city nickname ("Seahawks") — `DST_NICKNAMES` map → abbrev. Reads with `utf-8-sig` (DK ships a BOM). Kickers flow through as normal priced players.
  - **Buckets (README'd):** `field_bucket` = large ≥50k / mid 1k–50k / small <1k — **the primary model axis** (chalk QB was 58% in a 237k milly). `stakes_tier` = casual ≤$5 / mid / sharp ≥$20 is a **feature, not a bucket**. Two separate targets: `logit(flex_own)` and `logit(cpt_own)`. GBM once ~4–6 slates/bucket; `_compute_showdown_ownership` is the baseline to beat.
- **`data/dfs_ownership/README.md`** rewritten to the simplified workflow (snapshot → drop standings CSVs → rebuild), bucket/selection-rule definitions, `manifest.json` + parquet schemas, v1/v2/v3 feature roadmap.
- **`.gitignore`** — now `data/dfs_ownership/**/*.csv` + `!…/salaries_prelock.csv` (was only `contest_standings.csv`; Cam's files are named per-contest and a 150-max milly export is 40 MB / 240k rows). Manifests + parquets stay tracked.
- **Answers to Cam's two open questions:**
  1. **Date/matchup in the filename?** No — the path already carries `2026/week_01/showdown_NE_SEA/`, so the filename only needs contest identity (`name_price_xmax`). Adding the date/matchup is redundant clutter.
  2. **Save salaries in a "database" as we scrape pre-contest?** Yes, and it's built: `salaries_prelock.csv` per slate folder is the canonical snapshot, and `_processed/salary_history.parquet` accumulates every snapshot with `snapshot_ts` so salary movement across the week is queryable. Consistent with the project's file-based / no-DB convention (parquet, not sqlite).
- **Verified end-to-end on real data Cam dropped in:** week-1 NE@SEA — `--backfill` pulled 68 players; parser joined 3 real contest exports (`minimax_.5_150max` 237k field / casual, `bubble_screen_15_1max` 2.7k / mid, `hard_count_20_5max` 1.2k / sharp) → 204 rows, DK-published vs recomputed ownership within 0.27%, kickers + DSTs resolved, Vegas features joined. 8s total runtime.
- **Also this session:** removed the `SIM_TIMING`-gated `[sim-timing]` / `[sd-opt]` debug prints from `run_simulation` and `optimize_showdown` in `src/api/app.py` (item 5 from the 09-09 next-steps). `app.py` compiles clean.
- **Immediate Next Steps:** (1) Snapshot SF@LA salaries were captured pre-lock (Thu 9/10 game) — drop its standings CSVs after it settles. (2) Wire v2 features (projection/ceiling/value ranks, sim optimal-CPT/FLEX%, usage shares) into `features.parquet` via a join in a model-training script, not the parser. (3) Still open from 09-09: calibrate showdown EV; point showdown EV field-scoring at `dfs_week_NN_players.parquet`; hard-cap-on-total-ownership lever; showdown build persistence.

---

### [2026-09-09] Handoff from Claude Sonnet 5 (Showdown single-game optimizer — v1)

- **Active Task:** Cam asked for a Showdown (single-game, CPT + 5 FLEX) optimizer in the same style as the classic slate optimizer, built quickly for testing on opening night, with **total lineup ownership** as a first-class lever ("sacrifice a few points of ceiling for being relatively unique"). Design decisions taken up front: full field-sim EV parity with `/api/optimize` (not a lean v1), **penalty-only** ownership lever (a continuous λ, no hard cap yet), and the pool **requires a sim run** for the chosen game (no mock fallback).
- **Backend — `src/api/app.py`** (new section after `/api/optimize`, ~260 lines):
  - `POST /api/optimize_showdown` — mirrors `optimize_lineups`'s structure: correlated Cholesky draws off `_build_correlation_matrix` (reused as-is — one game, so QB↔pass-catcher stacking dominates), contest-type width multiplier, ILP-per-draw, `_compute_lineup_stats_direct` for EV/ITM/Top%, portfolio metrics.
  - `_solve_showdown_ilp` — PuLP: captain var `c_i` + flex var `f_i`, `Σc=1`, `Σf=5`, `c_i+f_i≤1`, salary `Σ(1.5·sal·c + sal·f) ≤ cap`, lock / lock-as-captain / exclude, **separate per-player and per-captain exposure caps**, min-unique of 6 vs prior lineups. Objective: `Σ (1.5·draw − λ_eff·cpt_own)·c + (draw − λ_eff·flex_own)·f`.
  - `_compute_showdown_ownership` — CPT and FLEX modelled separately (FLEX ≈ classic value-chalk softmax; CPT ≈ `proj**1.35`, concentrated, near salary-blind, hotter softmax, lower cap). Deterministic seed from pool content, same rationale as `_compute_ownership`. Respects manual `ownership_pct` / `cpt_ownership_pct` overrides.
  - `_build_showdown_field` — ownership-weighted CPT+5FLEX field lineups (2500), scored iteration-aligned off `PLAYERS_BY_GAME_ID[game_id]` (this game's season-parquet slice) so a shared game environment lifts our lineup and the field together; falls back to independent `dk_pcts_all` draws when the parquet slice is absent.
  - **λ is normalised** (`own_scale = mean(ilp_scores) / mean(flex_own)`, `eff_lambda = req.leverage_lambda * own_scale`) so the knob means the same thing on any slate regardless of point/ownership magnitudes. Validated sweep on the NE@SEA week-1 sim: λ 0.0/0.15/0.25/0.5/1.0 → avg total ownership 138/100/84/57/37 %, avg score 73.8/70.9/69.1/65.6/57.9.
  - Response carries per-lineup `total_ownership`, `dupe_est`, `cpt_name`; portfolio `avg_total_ownership`, `unique_captains`, plus a `notes` array (currently: kickers not modelled by the sim engine).
- **Frontend — `frontend/src/pages/ShowdownOptimizer.jsx`** (new page, ~560 lines) + wiring: `pagesConfig.js` (new `showdown_optimizer` nav entry), `App.jsx` (import + route + wide-container), `api.js` (`optimizeShowdown`, no mock), and the classic Optimizer's dead "Showdown Single-Game" slate button now navigates here.
  - Game picker lists only games with a sim result carrying projections. Base pool is `useMemo`-derived from that game's sim; user edits (salary/proj/ownership/lock/exclude) live in a separate keyed `edits` map merged on render — no setState-in-effect.
  - Editable **salary** (Showdown pricing ≠ Classic — sim only has Classic/main-slate salary, so null-priced players get a rough estimate flagged with `*` and a warning banner; Cam edits to real DK numbers). Editable proj (yellow when bumped above sim median), editable FLEX Own% / CPT Own% (blank → model). Read-only CPT Proj (1.5×) and the sim's optimal-CPT% as context.
  - Settings sidebar: contest type, entry $ / field size / paying spots, # lineups, min-unique, max exposure, CPT max exposure, **λ slider** (0–1.5, default 0.25, "0 = pure ceiling · 0.15–0.4 = lean unique · 1+ = hard fade").
  - Results: portfolio card (EV%, **Avg Total Own%**, Unique Captains, ELC, Coverage), lineup table default-sorted by total ownership ascending, expandable rows (6 player cards w/ slot, proj, own, salary), exposure panel (total% + CPT%), DK-ish CSV export.
- **Verification:**
  - `scratch/test_showdown_optimizer.py` — synthetic 13-player pool, asserts every lineup is a valid CPT+5FLEX under cap and shows the λ→ownership tradeoff.
  - **Full browser E2E** against an isolated backend (8004) + frontend (5199): page loads, pool builds from the week-1 NE@SEA sim (25 players), Optimize generates 20 valid lineups, results view renders, row expansion works, moving the λ slider 0.25→1.0 and re-running dropped Avg Total Own 67.5%→35.2% and raised Unique Captains 5→7 / Coverage 0.76→0.88. Frontend `vite build` + `eslint` clean (pre-existing `api.js`/`Optimizer.jsx` lint errors untouched).
- **KNOWN LIMITATION — EV/ITM/Top% are not trustworthy yet.** The season players parquet does not exist in the repo right now (`players_parquet_exists: false`), so both this tool *and* the classic `/api/optimize` fall back to a degenerate independent-draw field and EV comes out bimodal (+2900% / −100%). Ownership / leverage / score / salary / exposure / ELC are all sound. Cam confirmed the parquet **will** be generated by the weekly sims (`run_week_sim_2026.py` → `data/interim/dfs_week_NN_*.parquet`; roster source `data/overrides/2026/week_01`). Once present, re-check showdown EV — showdown EV (small field, top-heavy, captain leverage) may still need its own calibration pass separate from the classic tuning.
- **Follow-up same session — DK Showdown salary scraping + full E2E:**
  - **Restarted all four servers** (8000/8002 backends, 5173/5174 frontends). The old `:8002`/`:8000` uvicorns (two each — venv + anaconda, from different chats) plus an orphaned multiprocessing-fork child holding port 8000 were killed; relaunched clean. Cam's background sims (`run_full_season_sim_2026.py`, `regenerate_2026_reports.py`) left running.
  - **`src/scrapers/dk_scraper.py`** — new `get_dk_showdown_slates()` and `get_dk_showdown_salaries(draft_group_id | away_team+home_team)`. A DK Showdown Captain Mode draftables feed lists every player twice: `rosterSlotId 511` = Captain (salary already ×1.5), `512` = FLEX (base). Confirmed 2026-09-09 vs dg 153072 (Puka 16800/11200 = 1.5×). Folded into one record per player carrying base salary + captain salary + both `draftableId`s. `DK_TEAM_ALIASES` maps LAR→LA (+ defensive JAC/LVR/WSH). Kickers kept (DK prices them; sim doesn't project them). One draftables fetch per showdown slate, cached 30 min. Reuses `normalize_player_name` + the existing alias file.
  - **`src/api/app.py`** — `GET /api/dk/showdown_slates` and `GET /api/dk/showdown_salaries?away=&home=`. Also **fixed a bug in `optimize_showdown`'s `player_draws`**: `trial_map.get(a) or trial_map.get(b)` raised "truth value ambiguous" once the parquet was actually present (values are numpy arrays) — now explicit None checks. Added `try/except pulp.PulpSolverError` around the showdown ILP solve so a transient CBC subprocess failure (happens under heavy machine load) drops one lineup attempt instead of 500ing the request.
  - **`frontend/src/pages/ShowdownOptimizer.jsx`** — "⬇ Load DK Salaries" button: fetches the live DK Showdown pool for the picked game, matches on normalised name+team (mirror of `normalize_player_name` in JS), fills real base salary + `dkFlexId`/`dkCptId` onto matched players, clears the estimate flag. Status line ("DK slate 151820: matched 25/25 · N DK-only"). DK-only players (kickers, deep bench ≥$3k) surfaced as a note. `dk_id`/`dk_cpt_id` now flow into the optimize payload; **CSV export** emits DK's `Name (draftableId)` bulk-upload format when every lineup player has a slot id, else the readable name+stats fallback. `api.js` gains `getDkShowdownSalaries`.
  - **Full browser E2E passed** (5173 → 8002, real week-1 parquet loaded): NE@SEA — Load DK Salaries matched 25/25, salaries populated ($10.6k JSN etc., no `*`), Optimize generated 20 lineups now spending $46–50k of $50k (vs $22–36k with estimates), **EV numbers now sane** (portfolio +661%, per-lineup +111%→+1200%, ITM 6–33%) instead of the pre-parquet ±garbage — though still optimistically high, so **showdown EV still wants a calibration pass**. λ=0.25 → avg total ownership 31.6%.
  - **Known rough edge:** under the current extreme machine load (2× season sims + 2× regen + 2 backends) CBC intermittently fails to spawn, giving an occasional 500 on both `/optimize_showdown` and `get_week_sim_results`'s cash-consensus solve. Environmental — clears when the background sims finish. Also `week_1_sim_results.json` was recomputing every call for a while because the season parquet's simulated starters (Jadarian Price, Brock Purdy…) don't match current `data/current_rosters` (Charbonnet, Mac Jones…) — Cam needs `run_week_sim_2026.py 1` (or a season resim on current rosters) to stop the per-game live-sim bypass.
- **Follow-up same session — DK contest picker + week-1 refresh + site relaunch:**
  - **DK contest picker for Showdown** (no new scraper — `get_dk_contests(draft_group_id)` / `get_dk_contest_payout(contest_id)` and their `/api/dk/contests` + `/api/dk/contest_payout` endpoints are already draft-group-agnostic and work for a showdown dg). `ShowdownOptimizer.jsx`: "Load DK Salaries" now also calls `getDkContests(res.draft_group_id)`; a "Live DK Contest" `<select>` in the settings sidebar → `applyContest()` fills entry fee / field size (`max_entries`) / paying spots and fetches the real rank-by-rank payout table, which flows into the optimize payload as `payout_structure` (backend `ShowdownOptimizeRequest` already accepts it). Verified in-browser: NE@SEA showdown → 1106 live contests listed → picked the $2.25M Millionaire Maker → auto-filled $20 / 132,352 / 26,495 + 23 real payout tiers → optimize EV went from +661% (type-shaped estimate, smaller assumed field) to a realistic **−45% portfolio** (a $20 MM showdown is a lottery; the low-owned / high-ceiling lineups get closest to break-even). Working as intended.
  - **Ran `apply_team_week_overrides_v_0_1_0.py 1` then `run_week_sim_2026.py 1`** (Cam had edited week-1 usage for the two openers — NE/SEA/SF/LA sheets — but not the other 15 games). Compiled 777 players into `data/current_rosters/dfs/`, sim wrote fresh `dfs_week_1_{games,players}.parquet` (~4 min), deleted the stale baked `week_1_full_projections.json` / `week_1_sim_results.json`. `/api/week_sim_results?week=1` now returns in ~1s (was recomputing 16 games live every call). NOTE: the showdown optimizer's own EV path reads `PLAYERS_BY_GAME_ID` (the *season* parquet, `sim_results_2026_players.parquet`), NOT the DFS week parquet — so showdown EV still runs against the season sim's rosters until a season resim, or until that path is pointed at the DFS week parquet.
  - **Relaunched all four servers via nohup** (persistent, not session-bound) — the preview-manager-run DFS pair (`:8002`/`:5173`) had been reaped mid-session. Site is up; DFS frontend fronted in the browser.
- **Follow-up same session — pre-populated ownership + kickers:**
  - **Ownership, classic-optimizer style.** Enhanced `_compute_showdown_ownership` to blend the sim's own `optimal_cpt_pct` / `optimal_flex_pct` (the showdown analogue of "this slate's cash consensus" -- captains that are optimal most often are chalk) and a Vegas/sim implied-total nudge on top of the value/salary core. New `POST /api/showdown_prep` (`ShowdownPrepRequest`) returns modelled FLEX + CPT ownership for the whole pool + (see kickers below). Frontend: "Load DK Salaries" now also calls `showdownPrep`; the FLEX Own% / CPT Own% columns show the model value greyed, a hand-edit turns blue and wins, and a **"Freeze own"** toggle snapshots the model into `frozenOwn` so EV/portfolio math is stable across optimize runs (mirrors the classic Freeze). Resolved ownership (`ownOf`: hand override → frozen → model) is sent in the optimize payload so the backend keeps exactly what's shown. NOTE: `optimal_cpt_pct`/`optimal_flex_pct` come back 0 from the *week-sim* path (`get_week_sim_results` doesn't run the per-iteration showdown solve) — they only light up if the user runs a dedicated game sim on the Simulator page (`optimizer_sample_cap=50`). The model still works on value + Vegas without them.
  - **Kickers, derived from game script (Cam's call).** New `_synthesize_kicker_scores(game_id, team)`: per sim iteration, `team_tds` = Σ that team's players' (rTD+recTD+def_td), `xp_made ≈ team_tds`, `fg_made = max(0, round((team_score − 7·team_tds)/3))`, `dk_score = 3.5·fg + 1·xp` (3.5 ≈ blended FG incl. DK's 40-49/50+ distance bonuses). Returns an iteration-aligned array that slots into the showdown `trial_map` / `player_draws` / field sim exactly like a real player. `/showdown_prep` returns each kicker's projection / ceiling / 101-pt `dk_pcts_all`; `/optimize_showdown` synthesises their trial scores when `game_id` is set. Frontend: "Load DK Salaries" adds the DK-priced kickers as real pool rows (they were display-only before). Rough edges: no distance bonuses, assumes all XPs made, integer FG estimate — acceptable v1, correlates with game environment for free. Verified in-browser: NE@SEA → "matched 25/25 · +2 kickers", Jason Myers K proj 7.5 / ceil 15, ownership populated for all, optimize produced 20 lineups with kickers appearing in FLEX.
  - **8002 backend now runs WITHOUT `--reload`** — the reloader + heavy startup + multiprocessing pool + machine load kept leaving orphaned `multiprocessing-fork` workers holding the port (had to hunt the PID and kill it twice). Plain uvicorn is stable; **code changes to `src/` now need a manual `:8002` restart.**
  - **`/api/week_sim_results` in-flight lock (real fix for the recurring thrash).** The endpoint has no concurrency guard and is minutes-expensive when the season parquet's simulated starters don't match `data/current_rosters` (which they don't, and `run_week_sim_2026.py` deletes the on-disk JSON cache besides). The frontend + its `safeFetch` retries hammer it on every page load, so a handful of concurrent cold hits each spawned their own 16-game live re-sim, saturated every CPU, and **none ever finished** (a prime call timed out at 6.5 min). Split into `get_week_sim_results` (thin: cache check + per-`(week,year)` `threading.Lock`) → `_compute_week_sim_results` (the body). Concurrent callers now block on the lock and read the cache the first one populates. After the fix: first serialised compute 113s, every call after ~1-3s. Site restored. **Still worth doing:** point this endpoint at `dfs_week_NN_players.parquet` so it doesn't live-re-sim at all.
- **Follow-up same session — perf rewrite + auto-load + contest search:**
  - **`/api/optimize_showdown` was spawning a CBC subprocess per lineup-attempt** (`_solve_showdown_ilp`), which hung under machine load and — as an `async def` doing sync CPU work — froze the event loop, so retry storms from the frontend piled up and GIL-thrashed the whole backend to a standstill (saw it stuck at 5700+ CPU-s). Replaced with **`_solve_showdown_fast`**: pure-Python per-captain branch-and-bound over the 5 FLEX slots (suffix-sum prune, same shape as `solve_showdown_iteration`), min-unique checked once per captain not per leaf (a per-leaf check gutted the prune → full enumeration), tiny `+0.0005*salary` objective nudge so it spends the cap, falls back to a min-unique-violating lineup rather than returning None. **~0.9s for 20 lineups, ~7s for 150** (was 60s+/hang). Endpoint is now a thin `def` wrapper holding `_SHOWDOWN_OPT_LOCK` so duplicate/retry requests serialise instead of thrash. `_compute_one_game`'s per-game showdown solve sample dropped 8→2.
  - **`data/interim/week_1_sim_results.json` staleness check** now also watches `dfs_week_{N}_*.parquet` mtime + the recursive `current_rosters/**` glob, so re-running `run_week_sim_2026.py` invalidates it.
  - **Frontend: DK salaries + contests + ownership now auto-load** when a game comes into view (`useEffect` on `gameId` → `loadDkSalaries()`, once per game; "Load DK Salaries" button stays as a manual re-run). Matches the classic optimizer being pre-populated. **Contest search box** added above the contest picker (name substring filter, mirrors the classic `ContestPicker`). Verified on SF@LA: auto-matched 26/26 + 2 kickers with real salaries, "millionaire" filter → 124 matches. NOTE: a slate vanishes from DK's feed once its game kicks off (NE@SEA was gone by ~9pm), so the auto-load correctly finds nothing for a started game.
- **Immediate Next Steps:** (1) Calibrate showdown EV + the ownership model. (2) The salary nudge is weak with flat estimated salaries — real DK salaries fix lineup shape; a projected-score floor might still help. (3) Point showdown EV field-scoring at `dfs_week_NN_players.parquet`. (4) Follow-ups Cam floated: hard cap on total ownership, showdown build persistence, CPT/FLEX-aware dupe modelling. (5) Remove the `SIM_TIMING`-gated `[sd-opt]`/`[sim-timing]` debug prints in `run_simulation` / `optimize_showdown`.

---

### [2026-09-03] Handoff from Claude Sonnet 5 (roster source -> nfl.com scrape; red-zone / goal-line share columns)

- **Active Task:** Cam flagged roster errors in the per-team sheets and asked to source rosters from the official nfl.com team pages (`nfl.com/teams/{slug}/roster`, all 32) instead of nfl_data_py, and to add red-zone + end-zone target/rushing share columns.
- **Part 1 - nfl.com roster source:**
  - New [`scripts/roster_management/scrape_nfl_rosters_v_0_1_0.py`](scripts/roster_management/scrape_nfl_rosters_v_0_1_0.py) - stdlib only (urllib + regex + `html.unescape`), fetches the 32 nfl.com roster pages (server-rendered `<table>`, no JS/deps needed), 1s apart, caches to `data/overrides/2026/_nfl_com_roster.csv` (team, player_name, jersey, pos, nfl_status, height, weight, experience, college). Keeps prior scrape for teams not re-requested; non-fatal per-team failures.
  - nfl.com status codes are clean and accurate: **exactly 53 ACT per team**, `DEV`=practice squad, `RSR`/`RES`=IR, `PUP`, `RSN`=NFI, `SUS`=suspended, `EXE`=exempt, `CUT`=off roster. Correctly nails Josh Jacobs (EXE), Jeshaun Jones + Brock Rechsteiner (SUS) - all of which the nfl_data_py feed got wrong or vague.
  - [`roster_feed_v_0_1_0.py`](scripts/roster_management/roster_feed_v_0_1_0.py) rewritten: `build_feed_index()` reads the cache; `NFL_STATUS_TO_SLOT` maps codes; nfl_data_py is now called only (best-effort, wrapped) for a `player_id` bridge by name. New `resolve_status()` - exact `match_key` then a **skill-position-only** `(team, lastname, first-initial)` fuzzy fallback (bridges Kenny/Kenneth, Cam/Cameron; position-scoped so "Charlie Jones" WR can't match "Christian Jones" OT). `by_name` indexes ALL positions (so a sheet's "Travis Hunter" matches even though nfl.com lists him at CB); `team_feed` (add-new list) + the fuzzy index are skill-only. FB folded to RB.
  - `export_team_season_overrides` now prints 3 review lists: **roster_slot=cut but had a role** (18 - reassign share + delete), **matched by lastname+initial** (4 - sanity check), **projected role, no nfl.com match** (~20 - real cut/trade or a spelling the fuzzy missed; kept `active` + flagged). Bug fixed mid-task: nfl.com names arrive HTML-entity-encoded (`Ja&#x27;Marr Chase`) - added `html.unescape`, which had been silently breaking every apostrophe/accent name (Chase, Achane, Swift, Estimé...).
- **Part 2 - red-zone / goal-line shares:** sheet schema grew 22 -> 26 columns: `rz_target_share, rz_carry_share` (yardline 6-20), `gl_target_share, gl_carry_share` (yardline <=5). Seeded from `data/dna/zone_usage_overrides_2026.csv` (`rz_*` / `five_*` columns; currently placeholder copies of the flat share per that file's own status). `src/data_pipeline/week_roster_v_0_1_0.py` `SHARE_FIELDS` extended to all 6 - `resolve_week_rows` redistributes zone shares the same pro-rata way. `apply_team_season_overrides_v_0_1_0.py` now also regenerates `zone_usage_overrides_2026.csv`'s 4 tunable columns (**preserving that file's `hist_*`/`median_*` research columns** by joining on team+match_key) and runs `apply_zone_usage_overrides(2026)` -> `traits.splits.redzone/goalline`.
- **Encoding hardening:** `roster_feed.read_rows()` tries utf-8-sig then latin-1, so Cam can save the sheets from Excel as either "CSV" or "CSV UTF-8" and they round-trip. `write_rows()` writes latin-1 (matches the existing preseason/zone pipeline).
- **Verification:** `scrape` (all 32, 53 ACT nearly everywhere), `export` (810 rows, 32 sheets), `build_week_overrides 1`, `build_roster_md` all ran clean against the live files. Zone-CSV regen dry-run confirmed hist columns survive (707 matched / 85 new-blank). `pytest tests/` -> **92 passed** (+1 zone redistribution test). GB Week 1 numbers unchanged (Lloyd 0.806).
- **NOT done:** `apply_team_season_overrides` was NOT run - Cam should resolve the review flags first (below), then run it.
- **Review flags for Cam (from `export_team_season_overrides` output):**
  - **roster_slot=cut, had a role - reassign + delete:** BAL Cornelius Johnson / Dayton Wade / Lucas Scott, CLE Gage Larvadain / Jack Stoll, DAL Joe Milton / Marquez Valdes-Scantling, DET Cedrick Wilson, HOU Justin Watson, IND Ulysses Bentley, KC Jason Brownlee, NE Terrell Jennings, NO Bub Means, NYG JuJu Smith-Schuster, PHI Quez Watkins, SF Malik Turner, WAS Jerome Ford / Van Jefferson.
  - **fuzzy (lastname+initial) - sanity check:** DEN Cam Ross==Cameron Ross ✓, HOU Josh Pitsenberger==Joshua Pitsenberger ✓, NYJ Quentin Skinner==Quincy Skinner Jr. (?), TB Kenneth Gainwell==Kenny Gainwell ✓.
  - **projected role, no nfl.com match - real cut/trade or spelling miss:** ATL Casey Washington / Nate Carter / Trevor Siemian, CAR Ainias Smith, CIN Charlie Jones, CLE Cedric Tillman, DAL Jaydon Blue, DEN Jaleel McLaughlin, DET Greg Dortch / Teddy Bridgewater, JAX DeeJay Dallas, KC Emari Demercado, MIA Quinn Ewers / Tahj Washington / Theo Wease / Tutu Atwell, NE Kayshon Boutte, NO Ja'Lynn Polk, PIT Kaleb Johnson (traded to GB - already added there), SEA Harrison Bryant.
- **Follow-up same session — Cam's first-4-teams review + `Totals` row support:**
  - Cam is reviewing the season sheets in Excel and adding a trailing `player_name=Totals` row (blank pos) per team as a running column-sum sanity check. Added `is_player_row()` to `week_roster_v_0_1_0.py`; `apply_team_season_overrides` + `build_roster_md` skip non-player rows, `resolve_week_rows` passes them through and `recompute_totals_rows()` re-sums them for the week, `export_team_season_overrides` re-adds a computed Totals row on reseed. Confirmed for Cam that totals >1 are cosmetic: `game_engine.py:479-497` renormalises target/carry shares per position per zone (`shares /= total_shares`), and only the starting QB's carry_share enters the rush pool (backups excluded).
  - Applied Cam's calls: **ARI/BUF** no change; **ATL** removed Trevor Siemian (not in NFL) + Nate Carter (cut) + Casey Washington (on CAR now — already present there as practice_squad); **BAL** removed the 3 `roster_slot=cut` rows (Cornelius Johnson / Dayton Wade / Lucas Scott). All 6 were 0-share so no redistribution. Regenerated week_01 + roster MDs. 804 player rows across the 32 sheets (+ 4 Totals rows). Suite: 92 passed.
  - **Cam's team-by-team review (ongoing, ARI→NE done as of 2026-09-04):** applied his roster calls sheet-by-sheet — mostly removing players nfl.com/Cam confirm were cut or traded (the export's "no nfl.com match" + "role-cut" flags), plus IR/season-ending changes (`return_week` 99). Traded players are almost always already on their new team's sheet as `new to team` from the nfl.com scrape — the review is mostly deleting the stale origin-team row. Every removed player's share was ~0 so no redistribution. Full per-team log + open discussion items: [`data/overrides/2026/_review_notes.md`](data/overrides/2026/_review_notes.md). Generated on request: [`_team_proe.md`](data/overrides/2026/_team_proe.md) (32-team PROE by coach history) and [`_qb_air_yards.md`](data/overrides/2026/_qb_air_yards.md) (starting QBs by aypa — flagged Lawrence 9.14 as high vs career, and 3 uncertain starters: ARI/CLE/MIA).
  - **Full 32-team review pass complete as of 2026-09-04** (SF/TB/TEN/WAS closed out the pass, including WAS's Jerome Ford cut-vs-IR call after a file-lock retry). 92 tests green after every batch.
- **Season-long vs. DFS-weekly split into two separate JSON trees, per Cam's explicit design call.** Confirmed the engine reads rosters exclusively from a hardcoded `data/current_rosters/{team}_traits_{year}.json` path -- fine for season-long (that IS the file `apply_team_season_overrides` writes), but a DFS week's injury-adjusted usage must never land in that same file. Added a small additive `rosters_dir="data/current_rosters"` kwarg to `NFLGameEngine.__init__` (`src/nfl_sim/game_engine.py`) and `BatchSimulator.__init__` (`src/nfl_sim/batch.py`) -- default reproduces the exact prior hardcoded path, zero behavior change for every existing caller; a DFS run passes `rosters_dir="data/current_rosters/dfs"`. New [`scripts/roster_management/apply_team_week_overrides_v_0_1_0.py`](scripts/roster_management/apply_team_week_overrides_v_0_1_0.py) `<week>` compiles `week_NN/{TEAM}.csv` into `current_rosters/dfs/{TEAM}_traits_2026.json`, starting from a fresh copy of the CURRENT season-long JSON (so every static career-DNA field the flat sheet doesn't carry comes along for free) and overlaying that week's fields. Only the current week's JSON is kept on disk (no per-week history, per Cam) -- the `week_NN/*.csv` sheets are the historical record. Factored the create-missing/zero-removed/overlay-values logic into a shared [`apply_sheet_helpers_v_0_1_0.py`](scripts/roster_management/apply_sheet_helpers_v_0_1_0.py) (`apply_sheet_to_traits`) rather than duplicate it inline; `apply_team_season_overrides`'s own `sync_roster_membership()` was left as-is (already verified working) rather than risk a refactor. Verified: ran week 1 for all 32 teams -- 777 updated / 0 created / 0 zeroed (season-long JSON was already fully synced going in); `rosters_dir` sanity-checked directly against `NFLGameEngine` (loads the DFS tree correctly, default unchanged); full suite still 92 passed.
  - **Open question raised for Cam, not resolved:** GB Josh Jacobs' `season_long/GB.csv` row currently shows `target_share=0`/`carry_share=0` (not his real 8.5%/63% healthy-role numbers) -- unclear whether this was an intentional "treat him as a zero for the whole season" edit or a data-entry slip from earlier in the review. Flagged rather than silently changed either way.
  - **Follow-up same session (Cam: "build the per-week sim runner and repoint the endpoint... then run each sim"; Jacobs-at-zero confirmed intentional, no fix needed):**
    - New [`scripts/simulation_runners/run_week_sim_2026.py <week>`](scripts/simulation_runners/run_week_sim_2026.py) -- mirrors `run_full_season_sim_2026.py`'s per-game `BatchSimulator` loop exactly, but only for that week's games and with `rosters_dir="data/current_rosters/dfs"` (the new engine kwarg), writing to `data/interim/dfs_week_{N}_{games,players}.parquet` (separate from the season-long parquet; overwritten each run, no per-week history kept, matching the roster JSON policy). Also deletes the stale baked `week_{N}_full_projections.json`/`week_{N}_sim_results.json` if present so the API can't keep serving pre-refresh numbers.
    - `src/api/app.py`'s `get_week_projections()` (the one endpoint doing the season-slice-by-game_id pattern) now prefers `dfs_week_{N}_players.parquet` via a new `_get_dfs_week_players(week)` loader (own mtime-based reload cache, `DFS_WEEK_PLAYERS_CACHE`) and falls back to the old season-slice behavior when no DFS-specific sim exists for that week yet -- zero-risk to any week without one.
    - **Scoped deliberately narrow:** confirmed via `grep` that `get_week_projections` is the only endpoint doing the `ALL_PLAYERS_CACHED["game_id"].isin(...)` pattern, but `/api/week_sim_results` and `/api/optimize` pull rosters through a separate `get_rosters()`/`run_simulation()` path that was not investigated or touched this session -- **the DFS site is not yet fully DFS-roster-aware end to end**, only `/api/week_projections` is. Flagged for Cam, not silently claimed as done.
    - Ran the real pipeline: `apply_team_week_overrides_v_0_1_0.py 1` (fresh), `run_week_sim_2026.py 1` (16 games x 1000 iters, ~201s) -- spot-checked GB: MarShawn Lloyd now the clear lead back (14.9 rush att/g, 65 rush yds/g), Kaleb Johnson getting real work (7.2 att/g), Jacobs absent entirely, confirming the whole CSV -> JSON -> sim chain works end to end. Also launched `run_full_season_sim_2026.py` (season-long full resim off the resynced rosters, 272 games) in the background -- first background-launch attempt failed silently (a `$TMPDIR`-based log redirect resolved to an unwritable root path, so the python process never even started; the tool's own `run_in_background` on the retry worked correctly).
    - Full test suite: 92 passed (unchanged by the API/runner additions -- no existing tests cover `app.py`).
- **`apply_team_season_overrides` gained roster-membership sync -- real bug found and fixed.** Cam asked whether season-long sims can read off the per-team sheets instead of the single legacy CSV. Confirmed the sim (`game_engine.py`/`batch.py`) has only ever read `current_rosters/{TEAM}_traits_2026.json`, never any CSV -- the sheets have always been purely an upstream edit surface, and "swap to per-team sheets" already happened architecturally when they were built. Ran `apply_team_season_overrides_v_0_1_0.py` for real for the first time this session and found: **32 players removed across the whole review (Jaydon Blue, Cornelius Johnson, Van Jefferson, etc.) still held their original nonzero `target_share`/`carry_share` in the traits JSON** -- `apply_preseason_overrides`/`apply_zone_usage_overrides` only ever UPDATE an existing JSON entry, they never delete one, so every one of Cam's roster-removal edits was silently invisible to the sim. Symmetric gap the other way: **8 players with a real hand-assigned role (GB Kaleb Johnson at 27% carry share, HOU Kayshon Boutte at 11% target, CAR Darren Waller, etc.) didn't exist in the traits JSON at all** and could never receive an applied value ("player not found" on every run).
  - Fix: new `sync_roster_membership()` in [`apply_team_season_overrides_v_0_1_0.py`](scripts/roster_management/apply_team_season_overrides_v_0_1_0.py), run after the two existing applies -- zeroes (`target_share`/`carry_share` + splits + `preseason_projection`, all zones) any traits.json player no longer present in ANY sheet; creates a full schema entry via `enrich_player` (same helper `export_team_season_overrides` uses) for any sheet player not yet a traits.json key, then overlays the sheet's 15 flat + 4 zone field values on top. Re-sorts `traits` by position after inserting new keys so `validate_rosters.py`'s ordering check stays meaningful.
  - Verified: 0 orphaned nonzero-share players and 0 missing-but-assigned players after the fix (was 32 and 8). `validate_rosters.py 2026` now 32/32 PASSED (29 teams had drifted out of position-sorted order from the new-entry inserts -- one-off resort applied, all clean). 92 tests still green.
  - **Discovered `current_rosters/*_traits_2026.json` is gitignored** (confirmed via `git check-ignore`) -- there is no git safety net on it, unlike what was assumed earlier in the session. The per-team CSVs (also currently untracked, never committed) are the only real source of truth now; recommend Cam commit `data/overrides/2026/` to git given the review work now living there.
  - **Open discussion items (from the review):** (1) GB — model as one of the most pass-happy teams post-Jacobs; PROE knobs assessed (`coach_dna.json` flat proe is live; `coach_coordinator_levers_2026.csv` + zone-split `proe_by_filter` model feature are dormant) — Cam wants to reassess after the first sim run. (2) Mid-season QB changes (LV Cousins→Mendoza, ARI, ATL, CLE) — proposed a `_qb_transitions.csv` + season-sim wrapper flipping the engine's `starter_override` per week, mirroring the injury `return_week` mechanism; blocked on the same "wire resolve_week_rows into the season sim" step. (3) Rookie efficiency fields — Cam hand-filling (no script). (4) CAR — TE room / WR bumps / Bryce Young still open.
  - **Rookie preseason-efficiency fields — checked per Cam's ask, no clean auto-fill exists yet.** 2026 nflverse data (PBP / player_stats / NGS) all still 404 — the "real preseason snaps" plan is still blocked. `college_stats_2026.json` (80 drafted rookies) has `receiving: {REC, YDS, YPR}` + `usage.pass` but **no targets** → can't derive `catch_rate`; `YPR` includes air yards → can't isolate `yac_per_rec`. Only RB `ypc` maps cleanly (already filled, ~12 RBs). **Scope is small:** only ~15 players with target_share ≥ 3% are still on flat position defaults (Carnell Tate, Makai Lemon, KC Concepcion, Jordyn Tyson, Kenyon Sadiq, Omar Cooper, Denzel Boston, etc. — all drafted rookies, all but 2 in college_stats). Options given to Cam: hand-fill the 15 / a rough college-YPR→adot script / a proper historical regression (college + draft round → rookie-year NFL efficiency). **Cam's call: hand-fill every rookie manually (important ones before the first run, rest after) — no script.**
- **Immediate Next Steps:** (1) Cam continues the sheet review (CAR onward), then runs `apply_team_season_overrides` + `build_roster_md`. (2) Still: wire `resolve_week_rows` / the `week_NN/*.csv` sheets into the season-sim path. (3) `suspended` return_week is a flat 7-week placeholder - set real numbers.

---

### [2026-09-02] Handoff from Claude Sonnet 5 (per-team 2026 override sheets + roster markdown + per-week injury resolver)

- **Active Task:** Season about to start, 53-man rosters set. Cam wanted, per team: (1) a **readable markdown roster** (active + IR/PUP/practice-squad), (2) **Excel-editable override sheets** for the season-long projection and for each week's hand adjustments, replacing `preseason_overrides_2026.csv` as the edit surface. Base = the current `preseason_overrides_2026.csv`, roster-corrected against the 2026 feed (drop cut players, add players new to a team at 0/0 shares). First few weeks are hand-tuned; once real games are in, the rolling L4 refresh takes over the weekly sheets.
  - **This went through two false starts this session** (a per-team JSON layer, then league-wide `injury_overrides`/`week1_usage` CSVs) before Cam redefined what he wanted — see "Retired" below. The final design is the per-team `data/overrides/2026/` sheets.
- **Files Created:**
  - `data/overrides/2026/season_long/{TEAM}.csv` ×32 — the season-long edit surface. Columns: `player_name, team, pos, player_id, roster_slot, return_week, note` + the 15 tunable fields from `preseason_overrides_2026.csv`. `roster_slot` ∈ {active, practice_squad, ir, pup, nfi, suspended, exempt, cut}. Seeded from `preseason_overrides_2026.csv` + feed: zero-role cut players dropped; cut players who **had** a projected role kept as `roster_slot=cut` with a "reassign + delete" note (never silently drop a real role); feed-active players missing from the sheet added at 0/0 with career-DNA/position-default efficiency fields. **0 field changes to any retained player's hand-tuned values** (verified by diff). 877 rows total (was 913; 59 added, ~95 zero-role cuts dropped, 14 role-cuts kept flagged).
  - `data/overrides/2026/week_01/{TEAM}.csv` ×32 — Week 1 expectation: reserve players whose `return_week` hasn't arrived (and `cut` players) zeroed, share redistributed **pro-rata to same-position `active` players** (equal-split fallback; team totals preserved; `!! LOST share` printed if a position empties out). Generated by `build_week_overrides_v_0_1_0.py <week>`.
  - `docs/rosters/2026/{TEAM}.md` ×32 — readable roster: Active roster by position (usage-ordered), Injured/Reserve (slot + est. return week), Practice squad, New to team. ASCII-only. Generated by `build_roster_md_v_0_1_0.py`.
  - New scripts in `scripts/roster_management/`: [`roster_feed_v_0_1_0.py`](scripts/roster_management/roster_feed_v_0_1_0.py) (shared feed helpers — `build_feed_index`, `clean_name`, `match_key` (suffix-stripped alnum key — bridges "Marvin Harrison Jr." vs "Marvin Harrison"), status→slot maps, `RETURN_WEEK_SEED`), [`export_team_season_overrides_v_0_1_0.py`](scripts/roster_management/export_team_season_overrides_v_0_1_0.py), [`build_week_overrides_v_0_1_0.py`](scripts/roster_management/build_week_overrides_v_0_1_0.py), [`build_roster_md_v_0_1_0.py`](scripts/roster_management/build_roster_md_v_0_1_0.py), [`apply_team_season_overrides_v_0_1_0.py`](scripts/roster_management/apply_team_season_overrides_v_0_1_0.py) (concat 32 sheets → regenerate `preseason_overrides_2026.csv` → run existing `apply_preseason_overrides` → traits JSON).
  - [`data/overrides/2026/README.md`](data/overrides/2026/README.md) — full workflow + column reference + feed caveats.
  - [`src/data_pipeline/week_roster_v_0_1_0.py`](src/data_pipeline/week_roster_v_0_1_0.py) — **rewritten** for the flat sheet schema (was the JSON-`full_doc` version from the false start): `parse_return_week`, `is_available(row, week)`, `resolve_week_rows(rows, week) -> (new_rows, report)`. Pure, no I/O.
  - [`tests/test_week_roster.py`](tests/test_week_roster.py) — **rewritten**, 13 tests (return-week parsing, availability gating, week-1 redistribution + team-total preservation + practice-squad exclusion, mid-season return + fill-in revert, exempt-never-returns, cut-player handling, lost-share). Full suite: **91 passed**.
- **Encoding gotcha:** `preseason_overrides_2026.csv` is **latin-1** (has "Audric Estimé"). All the new scripts read/write it and the sheets with `encoding="latin-1"` to match the existing `export/apply_preseason_overrides` pipeline; the `.md` files are ASCII-only.
- **Verification:** `apply_team_season_overrides` dry-run diff vs current `preseason_overrides_2026.csv` — 0 field changes among retained players, adds/removes all trace to real feed cut/signing rows. `build_week_overrides 1` reproduces the earlier GB numbers (Lloyd 0.806, Brooks 0.124). Could NOT run the full `apply_team_season_overrides` write — `preseason_overrides_2026.csv` was **locked by another process** (Excel, presumably Cam's) the whole session; Cam runs that step himself after editing.
- **Retired this session** (all were untracked, no git history): `build_2026_week1_files_v_0_1_0.py` + its 96 `{TEAM}_roster_final/full/week1_usage_2026.json`; `data/dna/injury_overrides_2026.csv` + `week1_usage_overrides_2026.csv` + their 2 export wrappers; `data/current_rosters/week1_2026_{files_notes.md,build_report.json}`.
- **Current System Status:** The per-team sheet system is built and self-consistent; the season-long sheets are seeded and ready to hand-edit. **Not yet wired into any season-sim driver** — a sim would read `week_NN/{TEAM}.csv` for the current week (or the rolling refresh writes those). `apply_team_season_overrides` has not been run against the live `preseason_overrides_2026.csv` yet (file was locked).
- **Flags for Cam:**
  - **14 role-cut players** kept as `roster_slot=cut` in the sheets — reassign each one's share then delete the row: ATL Nate Carter, BAL Cornelius Johnson/Dayton Wade/Lucas Scott, CIN Charlie Jones, CLE Gage Larvadain, **DAL Jaydon Blue (21% carry)**, DAL Joe Milton, **DEN Jaleel McLaughlin (11% carry)**, DET Teddy Bridgewater, KC Jason Brownlee, **MIA Theo Wease (11.5% tgt)**, NO Bub Means, NO Ja'Lynn Polk.
  - **Pro-rata pile-ups in week_01** (accepted): GB Lloyd 26→81% carry (Jacobs), SEA Price 45→73% (Charbonnet), SF McCaffrey 75→89% (Guerendo), NO Olave 23→43% tgt, HOU N.Collins 28→44% tgt. Pin specific numbers by editing the weekly sheet.
  - **Feed noise:** WAS "Jacoby Jones", NO "Ja'Lynn Polk" look like bad rows. Real vets with no feed match stay `active` (safe).
  - `return_week` seeds (ir 8 / pup·nfi 9 / suspended 7 **placeholder** / exempt 99) are guesses — hand-tune in the sheets. Jacobs = exempt/99.
- **Immediate Next Steps:**
  1. Cam edits the season-long sheets (esp. the 14 role-cuts), then runs `apply_team_season_overrides_v_0_1_0.py` + `build_roster_md_v_0_1_0.py`.
  2. Wire `resolve_week_rows` / the `week_NN/*.csv` sheets into the season-sim path — a sim should read the current week's sheet instead of the raw traits file. Not started.
  3. Once games are played: the weekly DNA refresh should write the rolling L4 average into `week_NN/*.csv` (same schema) instead of hand projection.

---

### [2026-08-20] Handoff from Claude Sonnet 5 (CollegeFootballData.com integration for drafted-rookie stats — built, ran, scoped down to RB ypc only)

- **Active Task:** Following up on the automation research from earlier this session (see the "CFBD API access" thread below), Cam had me actually build and run the CFBD pull for the 80 real 2026 draft picks (skill positions), using 2024+2025 college seasons.
- **Files Modified/Created:**
  - New [`scripts/roster_management/pull_2026_rookie_college_stats.py`](scripts/roster_management/pull_2026_rookie_college_stats.py): joins our roster's draft_capital to CFBD's `/draft/picks` by overall pick number, pulls each matched player's `get_player_season_overview()` for 2024+2025, and writes real data to `data/dna/college_stats_2026.json` for all 80. Requires a new isolated venv (`venv_cfbd/`, gitignored) since the `cfbd` package pins `pydantic<2`, which would otherwise conflict with the main `venv/`'s pydantic 2.x (FastAPI backend needs it) -- caught this BEFORE it broke anything by testing in a scratch install first, reverted the collateral global-env downgrade, then built the isolated venv.
  - [`data/dna/college_stats_2026.json`](data/dna/college_stats_2026.json) (new): raw CFBD pull for all 80 drafted skill-position rookies -- box score categories (rushing/receiving/passing/defensive/etc.), `usage` (share of team plays by down/situation), and `ppa` (predicted points added, CFBD's EPA-equivalent) per season. Kept as reference even though most of it isn't wired into any current_rosters field (see below).
  - `data/current_rosters/*_traits_2026.json`: only `ypc` (real rushing yards/carry, RB only) actually got written back -- 12 of 80 (the RBs with real college carries; the 13th RB, Max Bredeson, has zero rushing production in CFBD's data either year).
- **Two real bugs caught and fixed mid-session, both before they caused lasting damage:**
  1. First run matched only 10/80 (silently, no error) -- CFBD's `DraftPick.pick` field is ROUND-RELATIVE (1-32 within each round), not the overall 1-257 slot our `draft_capital["pick"]` uses (from nfl_data_py). Only round 1 has identical relative/overall numbers, which is exactly why exactly the round-1 picks matched and nothing else. Fixed by joining on CFBD's separate `overall` field instead -- reran, got 80/80.
  2. The real CFBD_API_KEY ended up pasted into the script's docstring (a tracked, committable file) instead of only `.env` (gitignored). Caught it, stripped it, confirmed via `git log` the script had never actually been committed so the key never reached git history -- close call, not an actual leak.
- **Scope decision (Cam, this session):** CFBD's real college data turned out to only cleanly map to ONE existing field -- `ypc` for RB, which is unambiguously "rushing yards per carry" everywhere else in this codebase (confirmed by reading `src/data_pipeline/roster_manager.py`). Explored three alternate directions for the fields Cam actually wanted (elusiveness/broken_tackle_rate, which need NFL Next-Gen-Stats-style tracking data with no public college equivalent):
    - CFBD's PPA (predicted points added) / success rate / weighted-EPA / recruiting stars -- Cam: "I dont love any of these" (different unit than what elusiveness/broken_tackle_rate expect -- expected points, not yards-over-expected -- would need a new field, not a substitution).
    - 2026 NFL preseason PBP, same formulas already used for veterans -- **currently impossible, not just noisy**: confirmed via a live pull that nflverse has published ZERO 2026 data yet (PBP pull 404s, schedule endpoint shows 0 PRE games). Cam wants to check back after week 2 of the 2026 preseason.
    - Extending the WR/TE/QB non-`ypc` production (real REC/YDS/TD is already sitting in `college_stats_2026.json`) into `yac_per_rec` -- ruled out, same wrong-unit problem (CFBD's YPR includes air yards, `yac_per_rec` specifically excludes them).
  - **Final call: leave everything else on the position-average default.** Don't force a mismatched-unit number into an existing field just to have *something* there.
- **Current System Status:** 12 RBs have real `ypc`; everything else on all 214+ promoted rookies (including the 68 non-RB drafted picks and all 145 UDFAs) is still the position-average default from the original promotion. `data/dna/college_stats_2026.json` holds real PPA/usage/box-score data for the 80 drafted rookies that isn't wired into anything yet -- available if a future session finds a real use for it (e.g. a from-scratch "explosiveness" field, if Cam ever wants to build one with correctly-labeled units).
- **Immediate Next Steps for the Next Agent:**
  1. **Cam wants to check nflverse for 2026 preseason PBP after week 2 of the 2026 preseason** -- rerun the availability check (`nfl.import_pbp_data([2026])`) before assuming it's still empty; if real data exists by then, extend `pull_2026_rookie_college_stats.py`'s approach (or a new sibling script) to pull real elusiveness/broken_tackle_rate/etc. from actual NFL preseason snaps using the exact same formulas `build_full_name_dna.py` already uses for veterans -- expect very small samples even once available (most rookies play limited preseason snaps), so surface sample sizes visibly rather than presenting a tiny-N result as robust.
  2. **Hard deadline: start of September (regular season kickoff)** -- Cam said if real preseason data isn't usable by then, he'll hand-fill the remaining rookie fields himself, same team-by-team style as `preseason_overrides_2026_notes.md`'s existing veteran pass.
  3. Cam mentioned he can get a UDFA list (undrafted free agents, currently the 145 not covered by this CFBD pass at all -- CFBD's `/draft/picks` only covers drafted players by construction) if useful -- paused on this pending the field-mapping question above, since the same "no clean unit match" limitation would apply to them too. Revisit if Cam brings it back up.

---

### [2026-08-20] Handoff from Claude Sonnet 5 (preseason_overrides_2026.csv / zone_usage_overrides_2026.csv — 3 missing FA signings + all 214 curve-based rookies promoted to the flat track)

- **Active Task:** Cam asked for skill-position free-agent signings (Najee Harris→NYG, Stefon Diggs→WAS, Deebo Samuel→SF, then a follow-up: Keenan Allen→IND) and "any skill position player drafted in 2026 not already included" to be added to both flat CSVs, using last year's real stats for the signings (2025 default, 2024 for Najee specifically — his call, since 2024/PIT was a better basis than his 2025/LAC year; both 2024+2025 pooled for Keenan Allen, since both were real meaningful-volume seasons for him, CHI then LAC) with shares left at 0, and league-average stats for rookies. Investigation found all 4 signings missing from `current_rosters/*_traits_2026.json` entirely (not even under their old team) and absent from `nfl.import_seasonal_rosters([2026])` too — the live feed hasn't caught up to these moves, so they had to be hand-added rather than auto-detected. Separately found all 80 2026 skill-position draft picks WERE already present in `current_rosters`, but 214 of them were still on the curve-based ramp track (`rookie_projections_2026.json`), which is why they didn't show up as CSV rows (both export scripts skip curve-based rookies by design). Flagged this design tension to Cam via AskUserQuestion before touching anything — he confirmed (Recommended-diverging choice): promote all 214 to the flat track too, understanding that pulls them off the ramp-up curve system.
- **Files Modified/Created:**
  - New [`scripts/roster_management/add_2026_missing_skill_players.py`](scripts/roster_management/add_2026_missing_skill_players.py): (1) adds each signing (`NEW_SIGNINGS` dict, designed to keep growing as Cam surfaces more — skips rather than errors on a name already present, so it's safe to add one new entry and rerun) using `build_2026_rosters_v_0_1_0.enrich_player()` (imported, not reimplemented) fed real PBP+NGS stats computed with the same per-player formulas `build_full_name_dna.py` already uses (career-averaged there; restricted to specific season(s) here — a multi-season entry like Keenan Allen's `[2024, 2025]` pools the raw plays across both rather than averaging two yearly rates, which naturally volume-weights by real target count, same convention as every other multi-season DNA build in this repo) — target_share/carry_share forced to 0.0 per Cam's instruction, only the efficiency-style fields (catch_rate, ypc, yac_per_rec, adot, deep_target_rate, elusiveness, broken_tackle_rate, avg_separation_yds) come from real history. (2) Promotes all 214 remaining curve-based rookies using the identical freeze-snapshot logic `enrich_player()` already applies to veterans, and deletes each from `rookie_projections_2026.json` — necessary, not cosmetic: confirmed by reading `refresh_weekly_dna_v_0_1_0.blend_one_player()` that its `is_rookie` check keys off membership in that file, not the presence of a `preseason_projection` block, so skipping the deletion would have silently kept routing them through the curve system regardless.
  - [`data/current_rosters/{NYG,WAS,SF,IND,...}_traits_2026.json`](data/current_rosters): 4 new player entries (Harris/Diggs/Samuel/Allen) + `preseason_projection` blocks added to 214 rookie entries across all 32 team files.
  - [`data/dna/rookie_projections_2026.json`](data/dna/rookie_projections_2026.json): all 214 promoted rookies' entries deleted (now just an empty `_metadata` shell — every 2026 skill-position rookie is on the flat track now, none left on the ramp curve).
  - [`data/dna/preseason_overrides_2026.csv`](data/dna/preseason_overrides_2026.csv) / [`data/dna/zone_usage_overrides_2026.csv`](data/dna/zone_usage_overrides_2026.csv): regenerated fresh via their existing export scripts (`export_preseason_overrides_v_0_1_0.py 2026`, `export_zone_usage_overrides_v_0_1_0.py`) now that the roster files carry the new data — 695 → 912 → 913 rows each, every previously hand-tuned value preserved untouched (they were already baked into the roster JSONs, the CSVs are just a view).
  - [`AGENTS.md`](AGENTS.md): documented the new script and flagged `build_rookie_projections_v_0_1_0.py`'s output as effectively superseded (empty) until a future full roster rebuild repopulates it.
- **Verification Performed:** Confirmed via `nfl_data_py` that none of the 4 signings appear under any team (old or new) before hand-adding them. Cross-checked the 214 promoted rookies' resulting shares: 195 were already 0.0/0.0 (build-time default), 8 had real Cam-hand-tuned non-zero shares from an earlier session (e.g. Jeremiyah Love/ARI at 11.5% target / 53.5% carry share) — confirmed those are preserved as-is by the freeze, not incorrectly zeroed, since Cam's "leave shares at 0" instruction was for the new signings specifically, not a blanket rule for every rookie. Spot-checked Carson Beck's (an already-promoted rookie from a prior session) row survived the CSV regeneration byte-for-byte on his hand-tuned fields. Row-count math confirmed exactly (695 + 214 + 4 = 913) in both CSVs. Keenan Allen's pooled 2024-2025 stats show 253 real targets across both seasons, confirming the multi-season path pulled real volume rather than silently falling back to defaults.
- **Current System Status:** Both flat CSVs now cover every 2026 skill-position player in the pipeline — no more curve-only rookies invisible to the hand-editable sheets, and all 4 known FA signings are represented. `rookie_projections_2026.json`'s ramp-curve system is currently unused (empty) as a direct consequence of Cam's choice this session.
- **Immediate Next Steps for the Next Agent:**
  1. The 214 newly-promoted rookies' non-share fields are still position-average defaults (real "league average" stand-ins, not per-player projections) — same status as any other un-hand-tuned row in these CSVs, expected to get Cam's usual team-by-team pass eventually, not urgent.
  2. If Cam wants the rookie ramp-curve system back for future draft classes (2027+), `build_rookie_projections_v_0_1_0.py` still works as before — this session's change only emptied the 2026 entries, didn't touch the mechanism itself.
  3. Confirm with Cam whether any OTHER 2026 FA signings/trades beyond the 4 named this session need the same hand-add treatment — `nfl_data_py`'s live 2026 roster feed is confirmed lagging real-world moves, so it can't be trusted to surface them automatically the way it normally does for future sessions. `add_2026_missing_skill_players.py`'s `NEW_SIGNINGS` dict is built to keep growing for exactly this.

---

### [2026-08-20] Handoff from Claude Sonnet 5 (coach_dna.json / coach_coordinator_levers_2026.csv — real proxies for 4 of the 5 flagged 2026 first-time-HC gaps)

- **Active Task:** Cam supplied real coaching-history facts for 5 of the coaches `merge_2026_coach_placeholders.py` (2026-08-05) and `export_coach_coordinator_levers_v_0_1_0.py` (2026-08-17) had left as either a full league-average placeholder or a blank cell: Mike LaFleur (ARI) was Jets OC 2021-2022 then Rams OC 2023-2025; Jesse Minter (BAL) is a DC — BAL's real 2026 OC is Declan Doyle, Bears OC in 2025; Joe Brady (BUF) was Bills OC 2023-2025 (also Panthers OC 2020-2021); Todd Monken (CLE) was Ravens OC 2023-2025 (also Bucs OC 2016-2018); and Klint Kubiak (LV) — already aliased to Mike Macdonald's 2024-2025 Seahawks HC entry — was actually Saints OC in 2024, only moving to Seattle in 2025, meaning the existing alias's 2024 half was the wrong team. This closes 2026-08-05's Immediate Next Step #2 (the flagged Brady/Monken interim gap) plus 3 more.
- **Files Modified/Created:**
  - New [`scripts/roster_management/build_2026_ooc_proxies.py`](scripts/roster_management/build_2026_ooc_proxies.py): since nflfastR's PBP only ever credits the actual HC of record (never a separate OC/DC identity), this pulls 2023-2025 PBP and filters by the specific **team+season** combos each coach really called plays for, then recomputes each downstream file's fields using that file's own existing methodology exactly (mirrors `R/scripts/build_dna_registry_v_0_1_0.R`'s `build_coach_dna()` for `coach_dna.json`'s flat fields, and `export_coach_coordinator_levers_v_0_1_0.py`'s zone-split formulas for the CSV) rather than inventing a new formula. Proxy spans, confirmed with Cam via AskUserQuestion where ambiguous: LaFleur ← LA/2023-2025 (chose the larger/more-recent Rams sample over Jets 2021-2022); Minter ← CHI/2025 (proxies Declan Doyle's real playcalling, single season); Brady ← BUF/2023-2025; Monken ← BAL/2023-2025; Kubiak ← NO/2024 + SEA/2025 (blend, correcting the prior Macdonald alias's wrong 2024 team). Hafley/MIA untouched — no history supplied yet, still on the league-average placeholder.
  - [`data/dna/coach_dna.json`](data/dna/coach_dna.json) (gitignored, regenerated locally): 5 entries overwritten with real proxy data + updated `_note` fields. Not committed (regeneratable, per `.gitignore`).
  - [`data/dna/coach_coordinator_levers_2026.csv`](data/dna/coach_coordinator_levers_2026.csv): ARI/BAL/BUF/CLE/LV rows overwritten with real zone-split numbers + matching notes (was previously untracked in git, still is).
  - [`AGENTS.md`](AGENTS.md): documented the new script in §1 alongside `merge_2026_coach_placeholders.py`, noting it must run AFTER that script (it overwrites 4 of its 5 placeholders) any time `coach_dna.json` gets rebuilt from scratch.
- **Verification Performed:** Confirmed `coach_dna.json` previously had no "Mike LaFleur" entry at all (ARI was silently falling back to zero-defaults, not just a flagged placeholder). Spot-checked that CHI's own existing row (Ben Johnson, real 2025 Bears HC) numerically matches the CHI/2025 slice built for Minter's proxy — expected, since both draws are the same underlying 2025 Bears offensive PBP, and confirms Declan Doyle's real 2025 OC playcalling is what's captured. Re-read both source formulas (R script + export script) line-by-line before reimplementing in Python to avoid drift from the existing methodology.
- **Current System Status:** 4 of the 5 flagged 2026 first-time-HC gaps now carry real team+season-filtered data instead of a league-average placeholder or a wrong alias. Only Jeff Hafley (MIA) remains on the league-average placeholder.
- **Immediate Next Steps for the Next Agent:**
  1. If Cam ever supplies Jeff Hafley's (MIA) real OC/DC history, add him to `build_2026_ooc_proxies.py`'s `PROXIES` dict the same way as the other 5 — don't hand-edit `coach_dna.json`/the CSV directly, since both are meant to be rebuilt fresh from this script.
  2. `coach_coordinator_levers_2026.csv` is still untracked in git (never `git add`ed) — confirm with Cam whether it should be committed now that it's more complete, or stays a local working file.
  3. Joe Brady's BUF proxy uses full 2023-2025 seasons even though he was only interim OC from Nov 2023 onward (documented caveat in the script's docstring) — a week-filtered rebuild would be more precise but wasn't requested this session.

---

### [2026-08-13] Handoff from Claude Sonnet 5 (zone_usage_overrides_2026.csv — first pass at the red-zone/inside-the-5 usage gap)

- **Active Task:** Cam asked for a companion document to `preseason_overrides_2026.csv`, scoped to just `target_share`/`carry_share`, split by zone (redzone + inside-the-5), prepopulated with the current flat overrides as placeholders, plus real historical zone usage for the last two seasons for context. This is the first concrete step on the gap `preseason_overrides_2026_notes.md`'s general section and `AGENTS.md` §0 both flag ("red-zone/inside-the-5 usage-and-efficiency splits are not modeled anywhere") — usage only, not efficiency fields, and explicitly not wired into any blend/apply pipeline yet.
- **Files Modified/Created:**
  - New [`scripts/roster_management/export_zone_usage_overrides_v_0_1_0.py`](scripts/roster_management/export_zone_usage_overrides_v_0_1_0.py): pulls real 2024+2025 PBP via `nfl_data_py`, classifies plays with `rolling_stats_v_0_1_0.classify_zone()` (goalline = `yardline_100<=5`, redzone = 6-20, mutually exclusive of goalline), and computes each player's zone target_share/carry_share as their own zone count ÷ their team's zone total that season (team resolved via mode-posteam across their zone plays, same approach `build_player_game_log()` already uses for trades). Matches players to real PBP via `player_id` (gsis_id) from `data/current_rosters/{TEAM}_traits_2026.json`, not name (PBP's own name columns are short-form).
  - New [`data/dna/zone_usage_overrides_2026.csv`](data/dna/zone_usage_overrides_2026.csv) (695 rows, matches `preseason_overrides_2026.csv`'s row count exactly): `rz_target_share`/`rz_carry_share`/`five_target_share`/`five_carry_share` (2026 placeholders, prepopulated with each player's flat share from `preseason_overrides_2026.csv`) + `hist_2024_*`/`hist_2025_*` share+count columns per zone. 470/695 rows have real 2024/2025 zone activity (the other 225 are rookies, inactive/deep-bench filler rows, or backup QBs with no real snaps in the window — expected, not a gap).
  - New [`data/dna/zone_usage_overrides_2026_notes.md`](data/dna/zone_usage_overrides_2026_notes.md): zone definitions (flags the redzone/goalline split is mutually exclusive, NOT the traditional inclusive "inside the 20" stat), column definitions, small-sample caveat (goalline volume is often single-digit for a season — check the count column before trusting a share), and explicitly notes no apply/blend wiring exists yet.
- **Verification Performed:** Spot-checked several known players against real-world expectation — James Conner's `hist_2024_rz_carry_share` (66%) vs. `hist_2025_rz_carry_share` (6%) tracks his real 2025 role change; Jonathan Taylor's ~60-70% RZ carry share across both years matches his bell-cow role; Josh Allen's `hist_2024/2025_five_carry_share` (36%/42%) matches Buffalo's well-known goal-line-QB-run scheme. Confirmed the CSV's row count matches `preseason_overrides_2026.csv` 1:1 and that zero-historical rows are all genuinely rookies/inactive players, not a matching bug.
- **Current System Status:** Usage-only zone split now exists as a hand-editable reference document, same starting stage `preseason_overrides_2026.csv` began at before `apply_preseason_overrides_v_0_1_0.py` existed. Efficiency-field zone splits (catch_rate, adot, etc.) and any actual game-engine wiring remain open, undecided design work — do not build either without checking with Cam first.
- **Immediate Next Steps for the Next Agent:**
  1. Cam to hand-edit `rz_*`/`five_*` columns against the `hist_2024_*`/`hist_2025_*` reference data, same style as the `preseason_overrides_2026.csv` team-by-team pass.
  2. Efficiency-field (catch_rate/adot/ypc/etc.) zone splits are still unbuilt — a natural follow-on once usage is hand-tuned.
  3. Whether/how zone-specific shares actually feed `game_engine.py` is still an open design decision, not started.

---

### [2026-08-05] Handoff from Claude Sonnet 5 (all 32 teams' preseason override notes; coach_dna 2026 gap closed for a baseline run)

- **Active Task:** Two threads. (1) Cam went team-by-team, alphabetically ARI→WAS, dictating qualitative reasoning behind his hand-edits to `preseason_overrides_2026.csv` (usage-share trims, rookie-role calls, injury/regression judgment calls, several real-but-surprising 2026 team-assignment confirmations cross-checked against the CSV/`rookie_projections_2026.json` along the way — Tua/ATL, Pacheco/DET, Njoku/LAC, Kyler Murray/MIN, Isaiah Likely/NYG, Kenneth Walker/KC, Rachaad White & Jerome Ford/WAS). Logged into [`data/dna/preseason_overrides_2026_notes.md`](data/dna/preseason_overrides_2026_notes.md), now covering all 32 teams plus a general/cross-team section (scramble-rate ceiling, a flagged inside-the-5 modeling gap). Includes a few ad-hoc data pulls Cam asked for inline: league-wide WR/TE comparison tables (Higgins/Pickens adot-catch_rate sanity check, Pierce's league-#1 adot ranking, Kraft's after-catch outlier profile, league-average WR/TE placeholder stats for un-promoted rookies), plus researched bios (5 obscure KC WRs, sourced) and rookie-curve confirmations against `rookie_projections_2026.json` (Stowers/PHI, Bernard/PIT, Stribling/SF, Hurst/TB, A.Williams+K.Allen/WAS). (2) Cam confirmed the overrides sheet itself is now fully updated (rookies/recent FA signings knowingly still gapped, red-zone/inside-the-5 splits knowingly deferred) and asked how close a baseline 2026 sim run is — closed the one real remaining blocker: `coach_dna.json`'s gap for first-time 2026 HCs.
- **Files Modified/Created:**
  - [`data/dna/preseason_overrides_2026_notes.md`](data/dna/preseason_overrides_2026_notes.md): added CAR through WAS (28 team sections) plus one general/cross-team addition (inside-the-5 usage-split gap, cross-referenced from the JAX section where it surfaced). ARI/ATL/BAL/BUF predate this session.
  - New [`scripts/roster_management/merge_2026_coach_placeholders.py`](scripts/roster_management/merge_2026_coach_placeholders.py): of the 9 team_to_coach_2026.json first-time HCs, 4 (Schottenheimer/DAL, Coen/JAX, Moore/NO, Glenn/NYJ) turned out to already have real 2025 HC-level `coach_dna.json` entries (they were already their team's credited HC last year) — no action needed. The other 5 genuinely have none, because `coach_dna.json` is keyed off nflfastR's `home_coach`/`away_coach` columns, which only ever credit the actual HC of record, never an OC/DC by name. Researched each of the 5's real 2025 role (web search, sourced in-conversation): Klint Kubiak (LV) was Seahawks OC 2024-2025, and Mike Macdonald's own entry happens to span exactly those 2 seasons — **aliased** directly as a clean, non-diluted proxy. Jesse Minter (BAL) and Jeff Hafley (MIA) were DCs (LAC, GB) — defensive background, so a team-offense proxy from their old team would misattribute someone else's scheme; **set to a freshly-computed league-average placeholder** instead, correctly flagged. Joe Brady (BUF) and Todd Monken (CLE) were real play-calling OCs (BUF 2023-2025, BAL 2023-2025), but the existing McDermott/Harbaugh entries span 9-11 seasons each, mostly predating them — reusing those directly would badly dilute their real numbers; **also set to the league-average placeholder** as an interim measure rather than ship a falsely-precise-looking diluted number. Every placeholder/alias entry carries a new `_note` field for future-session visibility. `coach_dna.json` now has 103 coaches (98 + 5).
  - [`AGENTS.md`](AGENTS.md): added the new script to §1's repo layout, same list position as `merge_coach_proe.py` (same failure mode — rerun both after any full `coach_dna.json` rebuild).
- **Verification Performed:** Confirmed via web search which 5 of the 9 were real gaps vs. already-covered (sourced: Brady/ESPN+CBS, Monken/multiple, Kubiak/ESPN+Sky Sports, Minter/ESPN+NFL.com, Hafley/NFL.com+CBS — links given to Cam in-conversation). Script run twice to confirm idempotency (second run recomputes league average from the then-103-coach pool and overwrites cleanly, no duplication, no crash). Spot-checked `preseason_overrides_2026.csv` values against several teams' dictated numbers while writing notes and flagged real mismatches to Cam as they came up (not silently assumed correct) — Cam later confirmed the sheet is now fully updated as of this session, superseding those earlier mid-session mismatches.
- **Current System Status:** `preseason_overrides_2026_notes.md` covers all 32 teams. `coach_dna.json` has no more silent zero-value gaps for any 2026 HC — 4 real, 1 real-alias, 4 flagged-placeholder, rest genuine multi-season career data. Given Cam's explicit acceptances (rookie/FA gaps OK, red-zone split deferred), nothing else identified this session blocks a baseline 2026 sim run. **Not yet done, per Cam's explicit "let's not run" this session:** no batch/season sim was actually executed.
- **Immediate Next Steps for the Next Agent:**
  1. Cam wants to actually kick off a baseline 2026 run soon — when he does, this is the first real end-to-end exercise of the full 2026 rollover pipeline (Phases 0-7 from 2026-07-22) against real hand-tuned preseason data. Watch for the same class of issue Phase 7's verification round surfaced last time (silent zero-defaults, stale-year hardcoding) — don't assume clean just because individual pieces were unit-verified separately.
  2. Real fix for Joe Brady/BUF and Todd Monken/CLE's `coach_dna.json` placeholders: a tenure-filtered rebuild of their actual OC-era PBP (BUF 2023-2025, BAL 2023-2025) using the R script's already-existing `coach_season` per-season intermediate table (currently computed but not written to JSON) rather than the career-aggregate table. Deliberately not attempted this session — real PBP re-aggregation work, correctness risk if formulas are reimplemented ad hoc rather than reusing the R script's actual logic.
  3. Phase 7's code changes + sacks recalibration (see 2026-07-22 entry) were still uncommitted as of that entry — check whether that's since been committed before assuming it has or hasn't.
  4. Rookies/recent signings knowingly not yet promoted to the flat sheet, per this session's per-team notes: Germie Bernard (PIT), De'Zhaun Stribling (SF), Eli Stowers (PHI), Ted Hurst (TB), Antonio Williams + Kaytron Allen (WAS), plus free-agent moves never added at all (Stefon Diggs/WAS, Deebo Samuel re-sign/SF). Cam has explicitly accepted this gap for the first baseline run.
  5. Red-zone/inside-the-5 usage-and-stat splits are confirmed unmodeled anywhere (see the notes doc's general/cross-team section) — Cam has explicitly deferred this, plan is to implement it after the baseline run rather than before.

---

### [2026-07-22] Handoff from Claude Sonnet 5 (sim input bug list closed out; 2026 Roster & DNA Blending Pipeline Phases 0-6; Phase 7 makes every model input live; sacks recalibration)

- **Active Task:** Three threads, same day. (1) Closed out the last two open items on `docs/sims/inputs/README.md`'s known-bugs list (trench_tiers `LA`/`CIN` key issues). (2) Built the full 2026 season rollover: real 2026 rosters in the engine's format, plus a formal rule (agreed with Cam in conversation) for how a player's simulated stats blend recent form vs. season-long baseline vs. preseason projection as the season progresses (Phases 0-6). (3) Cam's stated principle — "any value used in a model in the engine should be the blend of L4 and games this season" — turned out not to be true for most non-usage fields; Phase 7 (7a-7e) closes that gap, and verifying it surfaced a real, unrelated regression (sacks/game) that got fixed the same session. Plan written and approved via plan mode: `C:\Users\txcwa\.claude\plans\giggly-launching-thacker.md` (absolute path, outside the repo — referenced here for continuity, not a violation of the no-absolute-paths rule below).
- **Confirmed design (Cam's spec):** steady-state (game 6+): `stat = (2/3)*L4 + (1/3)*season_to_date`. Taper (games 1-5): projection weight 100/80/60/40/20% before games 1-5, actual-so-far fills the rest, zero projection weight from game 6 on. Rookies: usage fields (target_share/carry_share) and efficiency fields get independently-tunable parametric curves (ramp start/steady week, early/late value), since usage is coach-gated separately from real talent. Same formula/taper applies to team defense. Scope: offensive skill positions only (QB/RB/WR/TE) at the player level; defense is team-level only.

- **Files Modified/Created (grouped by phase):**
  - **Bug-list close-out (pre-Phase-0):** [`data/dna/trench_tiers_2025.json`](data/dna/trench_tiers_2025.json): `"LAR"` → `"LA"` (rename only, preserved existing curated values), added a missing `"CIN"` entry (tier fields are confirmed dead code; `qb_cpoe_z` is still live, set to an explicitly-flagged estimate). [`docs/sims/inputs/README.md`](docs/sims/inputs/README.md): bugs #1/#2 closed — **no open items remain on the list.**
  - **Phase 0 (engine year-param fix):** [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): two paths (`team_to_coach_2025.json`, `trench_tiers_2025.json`) were hardcoded to 2025 regardless of the `year` param the engine already accepted — now interpolate `self.year`. New [`data/dna/team_to_coach_2026.json`](data/dna/team_to_coach_2026.json) — built from two independently cross-checked live sources (Wikipedia + Gridiron Experts), not copied forward, because the *existing* 2025 file turned out to already be stale (Dallas/Vegas/New England/Jacksonville coaches were wrong even for 2025). New [`data/dna/trench_tiers_2026.json`](data/dna/trench_tiers_2026.json) (copy-forward, tier fields dead, `qb_cpoe_z` flagged as needing a real-2026-starter revisit).
  - **Phase 1 (career DNA rebuild through 2025):** [`scripts/roster_management/build_full_name_dna.py`](scripts/roster_management/build_full_name_dna.py): season range 2021-2024 → 2021-2025, metadata bumped to V.0.3.0. Rebuilt `qb_dna.json`/`rb_dna.json`/`wr_dna.json`/`te_dna.json`/`skill_dna.json`. **Found along the way:** `coach_dna.json` and `trench_dna.json`'s legacy fields (`def_pressure_rate`/`def_sack_rate`/`sack_rate_allowed` — still live inputs to the sacks-gate model) were *also* stuck on 2015-2024 — every 2025 sim had silently been using generic defaults instead of real team data for those three fields. Fixed via [`R/scripts/build_dna_registry_v_0_1_0.R`](R/scripts/build_dna_registry_v_0_1_0.R) (SEASONS extended to 2015:2025; restructured so it no longer touches `qb_dna.json`/`skill_dna.json` at all — those are now solely Python's — and writes `trench_dna.json`'s legacy fields to a staging file instead of overwriting the file directly) + new [`scripts/roster_management/merge_trench_dna_legacy.py`](scripts/roster_management/merge_trench_dna_legacy.py) (merges the staged legacy fields into `trench_dna.json` without disturbing the v0.4.0 composite fields already there for 2025, self-verifies before deleting the staging file). Also fixed a stale hardcoded `"2015-2024"` metadata string in the R script (now derives from `SEASONS`).
  - **Phase 2 (blending engine, pure functions):** New [`src/data_pipeline/rolling_stats_v_0_1_0.py`](src/data_pipeline/rolling_stats_v_0_1_0.py), [`src/data_pipeline/dna_blender_v_0_1_0.py`](src/data_pipeline/dna_blender_v_0_1_0.py), [`src/data_pipeline/rookie_curves_v_0_1_0.py`](src/data_pipeline/rookie_curves_v_0_1_0.py), [`tests/test_dna_blender.py`](tests/test_dna_blender.py) (27 tests). **Found and fixed:** first draft matched `nfl_data_py` PBP on `receiver_player_name`/`passer_player_name`, which is short-name form (`J.Chase`) — always empty against the full display names the rest of the pipeline uses. Fixed to match on `player_id` (gsis_id), same approach `build_full_name_dna.py` already uses. **Found and fixed a second field-naming mismatch:** rolling-stats used the DNA-file field names (`yac_per_reception`, `avg_target_depth_yds`); `current_rosters`' top-level schema actually uses `yac_per_rec`/`adot` (the DNA names only apply inside the nested `splits` sub-object) — would have silently broken the blend once Phase 5 tried to write results back.
  - **Phase 3 (2026 roster shell):** New [`scripts/roster_management/build_2026_rosters_v_0_1_0.py`](scripts/roster_management/build_2026_rosters_v_0_1_0.py). Pulls `nfl_data_py`'s 2026 rosters (QB/RB/WR/TE only), joins returning players against Phase 1's rebuilt career DNA, uses each returner's actual 2025 role as their 2026 projection basis, flags all 225 `entry_year: 2026` rookies with `draft_capital` rather than guessing a share for them. QA gate diffs the output against real round-1 draft picks and fails loudly on any miss — caught 8 real gaps in `nfl_data_py`'s roster feed (not a bug in the matching logic, verified directly against the raw source; both round-1 picks were backup-slot rookie QBs behind entrenched veterans) and self-heals by patching them in from the draft data. `scripts/roster_management/validate_rosters.py` generalized to take a year argument (was hardcoded to 2025). Rebuilt a second time later in the session to add `player_id` (needed for Phase 5's real-PBP join) and a frozen `preseason_projection` snapshot (so the taper always blends against a stable original baseline, not a re-derived one).
  - **Phase 4 (rookie projections):** New [`scripts/roster_management/build_rookie_projections_v_0_1_0.py`](scripts/roster_management/build_rookie_projections_v_0_1_0.py) → [`data/dna/rookie_projections_2026.json`](data/dna/rookie_projections_2026.json) (225 rookies, sorted by draft round). Every curve value is an explicitly-flagged placeholder for Cam to hand-edit. QB rookies get no `usage_curve` — a QB1 promotion is a status flip driven by real evidence, not a gradual ramp (see Phase 5).
  - **Phase 5 (weekly refresh):** New [`scripts/roster_management/refresh_weekly_dna_v_0_1_0.py`](scripts/roster_management/refresh_weekly_dna_v_0_1_0.py) — pulls real PBP through the completed week, blends per player, and includes starter-flip detection (compares real recent attempt/carry volume between same-position teammates, flips `status` when someone's clearly taken over — `game_engine.py`'s `_get_starter_static()` sorts by static career volume and has no other way to react to an in-season change). Fixed a real edge case found during testing: `nfl_data_py` returns a fully columnless frame (not just zero rows) when a season has no PBP at all yet, which crashed the pipeline before the fix.
  - **Phase 6 (team defense parity):** New [`scripts/roster_management/build_2026_trench_shell_v_0_1_0.py`](scripts/roster_management/build_2026_trench_shell_v_0_1_0.py) establishes `trench_dna.json["2026"]` (carries forward 2025, freezes a `preseason_projection`). Wired `refresh_team_defense()` into the Phase 5 script using Phase 2's `blend_team_dna()`. **Scoping note, not fully closed:** only the 3 simple-rate fields get weekly-refreshed — the v0.4.0 composite z-scores (`run_block_off_z` etc.) stay frozen at 2025 values all season; recomputing those weekly would mean re-running the full z-scoring methodology against a partial-season sample, materially more work than a rolling average, out of scope this session.
  - **Repo cleanup + commits (between Phase 6 and Phase 7):** Committed all of Phases 0-6 in logical per-phase chunks (12 commits total). **Caught and fixed my own mistake mid-stream:** a first attempt to commit `game_engine.py`'s Phase 0 fix used a broad `git add`, which staged the file's *entire* current diff (~945/513 lines) — including a large pre-existing unrelated backlog already sitting in the working tree before this session — under a commit message describing only the 2-line fix. Caught before pushing, undone via `git reset --soft`, redone as an isolated 2-line commit (checked out the file to `HEAD`, reapplied just the fix, committed, restored the rest of the working tree). **Data-inclusion policy decided with Cam:** repo is public and stays public (site + 4th-down bot), so gitignoring data does NOT hide methodology (the code computing it is already visible) — this was purely a repo-size decision. `qb_dna.json`/`rb_dna.json`/`wr_dna.json`/`te_dna.json`/`skill_dna.json`/`coach_dna.json` and `current_rosters/*_traits_2026.json` are now gitignored, with new [`scripts/roster_management/regenerate_dna_v_0_1_0.py`](scripts/roster_management/regenerate_dna_v_0_1_0.py) to rebuild them from a fresh clone. Fixed a real non-determinism bug found while doing this (`build_full_name_dna.py`'s `contested_catch_rate`/`top_speed_mph` fallbacks used unseeded `np.random`) so regeneration is actually reproducible. `trench_dna.json` stays tracked — its composite z-score fields depend on an EDA CSV chain (`docs/eda_outputs/*`) not itself verified-regeneratable, didn't want to claim reproducibility risk couldn't back up.
  - **Phase 7 (7a-7e) — make every model input actually live, not just target_share/carry_share.** Full audit of `game_engine.py` found the taper/blend pipeline above only reached `target_share`/`carry_share`/`scramble_rate`/team-defense trench fields; everything else (QB `cpoe`, TTT, `avg_air_yards_per_att`, receiver `catch_rate`/`adot`/`contested_catch_rate`/`elusiveness`/`broken_tackle_rate`/`deep_target_rate`/`avg_separation_yds`, and the zone-split "`_by_filter`" features feeding Play Selection/Air Yards/YAC/Rush Yards) read the static career-average `qb_dna.json`/`skill_dna.json`, or — for the zone-split features — a process-level `ModelRegistry` singleton, stale across every concurrent game. **7a:** extended [`rolling_stats_v_0_1_0.py`](src/data_pipeline/rolling_stats_v_0_1_0.py) to cover all of these (new NGS-sourced fields for TTT/separation, new zone-split rolling stats with a small-sample fallback) — except `contested_catch_rate`, confirmed to have zero real signal anywhere in `nfl_data_py` (checked PBP + all 3 NGS categories), left static rather than faked with a proxy, per Cam's call. **7b:** wired the new fields + per-zone `splits` blending into [`refresh_weekly_dna_v_0_1_0.py`](scripts/roster_management/refresh_weekly_dna_v_0_1_0.py); added a frozen `preseason_projection.splits` baseline to the roster-shell builder (a prerequisite gap found along the way). **7c:** fixed a real schema gap — RB entries were missing `avg_separation_yds`/`deep_target_rate` entirely (present for WR/TE) in both the roster builder and rookie-projections template. **7d:** rewired `game_engine.py` itself — `_precompute_matchup_tables()` and `__init__`'s zone-split precompute dicts now source from `self.rosters` instead of `self.dna`/the registry singleton, with the static file kept only as a defensive fallback. **7e:** fixed `proe_overlay_v_0_1_0.py`'s hardcoded `team_to_coach_2025.json` path (same bug class as the original Phase 0 fix) and wired Gate 2's `off_sack_rate_l4`/`def_sack_rate_l4` features to genuine rolling L4 data instead of a duplicate of the season value.
  - **Regression found and fixed during Phase 7e verification, not caused by Phase 7's own code — traced to Phase 1 (this session, same day):** `coach_dna.json`'s `"proe"` field (originally merged in from the retired `coordinator_atlas.json` on 2026-07-16) got silently wiped when the R script was rerun in Phase 1 to extend `coach_dna.json` through 2025 — `build_coach_dna()` never produced that field itself, it was always a separate one-time merge nobody re-ran. Every `get_coach_proe()` call was silently returning `0.0` for every team instead of real values (Andy Reid should be ~+6.37). Recovered from git history (never actually committed as deleted, just sitting in the pre-existing backlog) via new [`scripts/roster_management/merge_coach_proe.py`](scripts/roster_management/merge_coach_proe.py), safe to rerun any time `coach_dna.json` gets rebuilt again.
  - **A second regression found via the first real end-to-end batch run of the session — also traced to Phase 1, not Phase 7's code (confirmed via a `git stash` A/B test) — but the initial measurement of its size was wrong, caught and corrected within the same round.** Discovered `venv/Scripts/python.exe` is required for `ModelRegistry()` to load at all (a bare `python` on PATH was resolving to an unrelated system install this whole session) — once found, ran a real batch and measured sacks *summed across both teams* at ~6.3-6.4/game vs. Round 15's calibrated `2.41` target. **That target is per-team-per-game everywhere in this codebase** (confirmed directly in `run_historical_eda.py`/`run_weeks_1_to_4_2025.py`'s source) — comparing a combined-both-teams number against a per-team target is a 2x error; correctly halved, the real drift was ~3.19/team (+32.5%, not +165%). First recalibration attempt (`1.24`→`0.494`) was computed from the wrong combined baseline and badly overcorrected — confirmed by running the full official 64-game audit *with* `0.494` in place: `sacks_per_game: 1.133` (-53% low). **Final: `SACK_PROB_CALIBRATION_MULT = 1.134`**, reached over 3 successive full official 64-game audit runs (`0.494`→`1.133` sacks/game, `1.051`→`2.235`, `1.134`→`2.384` vs. real `2.4106`, -1.1%, matching Round 15's original precision). Root cause of the original (smaller) drift: `trench_dna.json`'s `def_pressure_rate`/`def_sack_rate`/`sack_rate_allowed` used to silently default to a flat, identical `0.15`/`0.06`/`0.06` for every 2025 team (the exact bug Phase 1 fixed) — Round 15's original multiplier was tuned against that zero-variance world; not fully decomposed beyond that, an empirical recalibration was the right call given how much smaller the real drift turned out to be.
  - **Retracted: completion % is NOT elevated.** Initially flagged as a second open regression from a small KC/BUF-only spot-check (~68%) — the full official 64-game audit shows `completion_rate_pct: 63.89%`, within 0.5pp of the 64.40% target, i.e. fine. Same underlying lesson as the sacks measurement error above: a 2-3-matchup hand-picked sample (here, two real-world elite-accuracy QBs) is not representative of league-wide calibration. **General lesson for future sessions: run the full official audit BEFORE concluding a regression exists or attempting a recalibration, not after as a confirmation step** — see the new `AGENTS.md` §8 entry.
  - Not part of this work, pre-existing at session start and untouched: a large backlog of already-modified files (`docs/boxscores/*`, `docs/reports/*`, `pfr_adv_rushing_stats.csv`, `schedules_2015_2024.csv`, `app.py`, `batch.py`, several model files) — flagged to Cam, not investigated or committed this session.

- **Verification Performed:** Every phase backtested against real data before being trusted, not synthetic-only. Phase 1: qb/rb/wr/te career stats spot-checked (Burrow/Stafford/Mahomes CPOE all sane, `seasons_observed` correctly bumped to 5); trench merge self-verified both legacy and composite fields coexist post-merge. Phase 2: rolling-stats validated against real 2025 Ja'Marr Chase and CIN defensive data. Phase 3: `validate_rosters.py` passes all 32 teams for both 2025 (regression) and 2026; no team missing any of the 4 positions. Phase 4: a real generated rookie entry run end-to-end through `resolve_rookie_curves()` → `blend_player_dna()` across games 1/3/5/6. Phase 5: blend math checked against real 2025 Chase/CIN data at every taper stage (exact match to hand-calculated values); starter-flip logic tested against real Joe Burrow 2025 data including his actual injury absence (weeks 3-12) — correctly stayed inert on zero real data rather than forcing a wrong flip, which surfaced during testing and validated the logic rather than exposing a bug. Phase 7a: zone-split rolling stats backtested against real 2025 data (Chase's real primary/redzone splits computed correctly, goalline correctly falls back to the flat value on thin samples). Phase 7d: isolated logic-replication against real data (Mahomes live-vs-static CPOE, the `adot`/`avg_target_depth_yds` field-name distinction) — done before the `venv/` interpreter fix was found, so not yet a full engine-level check at the time. Phase 7e: PROE fix verified for both years; Gate 2 L4 fields verified end-to-end with real CIN data. Sacks recalibration: verified via real batch runs across 8 matchups once `venv/` unblocked full engine execution. `python -m pytest tests/test_dna_blender.py` — 30/30 passing throughout (27 Phase 2 + 3 new Phase 7a `classify_zone` tests).
- **Current System Status:** Phases 0-6 committed (12 commits). Phase 7's code changes (`game_engine.py`, `proe_overlay_v_0_1_0.py`, `rolling_stats_v_0_1_0.py`, `refresh_weekly_dna_v_0_1_0.py`, the two roster/rookie builders, `merge_coach_proe.py`, `tests/test_dna_blender.py`) and the sacks recalibration are **uncommitted as of this entry** — Cam asked for docs updated first. `SACK_PROB_CALIBRATION_MULT` is **finalized at `1.134`** (was `1.24`; briefly `0.494` then `1.051` mid-session due to a measurement error, see above) — confirmed via 3 successive full official 64-game audits, final result `sacks_per_game: 2.384` vs. real `2.4106` (-1.1%). Real 2026 season PBP still doesn't exist (confirmed: `nfl_data_py.import_pbp_data([2026])` returns a 404/empty).
- **Immediate Next Steps for the Next Agent:**
  1. Commit Phase 7's changes (code + the recalibration) — the sacks number is now confirmed final, no more audit runs needed for it. Cam hasn't asked for this explicitly yet this round, confirm first.
  2. Cam wants to discuss what data/projections belong in the repo vs. staying local-only — **resolved this session** (see the repo-cleanup bullet above), included here in case it wasn't fully closed in conversation.
  3. Coordinator-level DNA for first-time 2026 head coaches (Minter, Brady, Monken, Schottenheimer, Coen, Kubiak, Hafley, Moore, Glenn — no `coach_dna.json` entry, no HC-level nflfastR history exists for them) is still open. Cam is planning to hand-supply team/year mappings for their prior OC/DC roles; a team-season proxy is confirmed mechanically buildable from the same PBP source once he does. Not started.
  4. Weekly refresh (`refresh_weekly_dna_v_0_1_0.py`) is deliberately **not** automated/scheduled — Cam wants to run it by hand for at least the first few weeks of the season before considering the `schedule` skill's cron-based routine.
  5. Rookie curve values in `rookie_projections_2026.json` are 100% placeholders (position-baseline guesses, clearly flagged in the file's own metadata) — Cam said he needs to think more about the exact numbers, especially for delayed-role rookies. Not blocking, but don't treat the current values as real.
  6. The unrelated ~67K-line pre-existing backlog (see above) is still sitting uncommitted — Cam was offered help sorting/committing it separately and hasn't responded yet.
  7. **General lesson from this session's own mistakes, don't repeat them:** run the full official audit BEFORE concluding a regression exists or attempting a recalibration, not after as a confirmation step — a 2-3-matchup manual spot-check invented one phantom regression (completion %) and badly mismeasured a real one (sacks, off by 2x from a combined-vs-per-team mixup). See `AGENTS.md` §8's new entry.
  9. **Always use `venv/Scripts/python.exe` (or activate `venv/`) for anything touching `ModelRegistry`/trained models** — see `AGENTS.md` §8's new entry. A bare `python` cost significant verification time this session before being found.

---

### [2026-07-21] Handoff from Claude Sonnet 5 (magic-numbers documentation pass, QB scramble yardage recalibration)

- **Active Task:** Direct follow-up to the same day's "Fragile / confusing cleanup pass" entry below — Cam asked what other numeric constants in `game_engine.py` were un/under-documented (beyond what that round already fixed), then gave a per-item disposition for each finding.
- **Files Modified/Created:**
  - [`scripts/eda/analyze_scramble_yardage.py`](scripts/eda/analyze_scramble_yardage.py) (new): measures real QB scramble yardage via `nfl_data_py`'s `qb_scramble` PBP column directly (already known reliable from this session's earlier fumble-rate EDA) — no heuristic needed despite Cam's initial assumption the data couldn't distinguish scrambles from designed QB runs. Output: [`docs/eda_outputs/qb_scramble_yardage/README.md`](docs/eda_outputs/qb_scramble_yardage/README.md).
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): scramble yardage model `Normal(5, 4)` → **`Normal(7.0, 6.1)`**, matching the real 2020-2025 mean (6.995) and std (6.074) measured by the new script — previous model was undershooting the real mean by ~2 yards. Six documentation-only additions (no behavior change): return-yardage distribution params (kickoff/punt/interception/fumble) flagged as unverified against real data, deprioritized (rare plays, and Cam wants any future recheck to weight the clock-timing effect over exact yardage shape) but explicitly not dropped; QB time-to-throw distribution confirmed as intentional simplified design (not an oversight), with the deferred future idea of tying it to trench-matchup quality noted; the completion-probability model's separation-roll std and its 1-yard contested-vs-open cutoff flagged for future research (the cutoff is believed to match the community definition of a contested catch, unverified); the YAC noise formula's elusiveness/broken-tackle-rate coefficients (1.8, 6.0) flagged as an offseason investigation (unlike the formula's base_scale term, which already has a full Round 9 citation).
  - [`AGENTS.md`](AGENTS.md): added the un/under-documented-constants findings and per-item dispositions to §0, added the new script to §1's repo layout.
- **Verification Performed:** New EDA script run against real 2020-2025 PBP (n=6,230 scrambles). Smoke test (1000-game batch, `BatchSimulator`) after the scramble-yardage change: no NaNs, sane score distributions. Documentation-only edits carry no behavioral risk by construction (comments only).
- **Current System Status:** All changes uncommitted. No known regressions. Cam is separately rerunning `run_full_season_sim_2025.py` to refresh the stale parquet cache flagged in the prior entry (unrelated to this round's changes, but same outstanding item).
- **Immediate Next Steps for the Next Agent:**
  1. None of this round's 6 documentation-only flags are scheduled — they're intentionally deferred (return-yardage distributions: low priority, rare plays; QB TTT-trench tie-in: "sounds involved," on hold; separation/contested-catch verification: future research; YAC coefficients: offseason).
  2. If revisiting return-yardage distributions later, measure the clock-timing implication alongside yardage shape — that was Cam's stated priority, not yardage accuracy alone.
  3. Nothing has been committed this session — check with Cam before committing.

---

### [2026-07-21] Handoff from Claude Sonnet 5 (fragile/confusing cleanup pass — game_engine.py cleanup audit's remaining section)

- **Active Task:** Continuation of the same-day session (see the 2026-07-21 entry directly below this one for the trench-gates/bug-fixes/optimization-pass work that came first). This entry covers the audit's last untouched section: [docs/audit/game_engine_cleanup/README.md](docs/audit/game_engine_cleanup/README.md)'s "Fragile / confusing (not bugs, but worth cleaning up)" list, item by item.
- **Files Modified/Created:**
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): `qb_fumble_rate` mislabeling confirmed already resolved by the earlier sack-fumble rework this session (no code change needed, just verified). Fixed two stale/inaccurate comments (a dead line-number citation; a misleading claim about how turnover-return TDs avoid double-handling — corrected to explain the real mechanism, `td_mask` never including them). `regular_clock`'s hand-maintained NOT-list (`~inc_mask & ~turnover_clock & ...`) replaced with an additive `clock_handled` mask each Phase 7 category ORs itself into — a future new category can't be silently forgotten anymore. Extracted 5 independently-duplicated `yardline_100<=5/<=20` zone-classification copies (one more than the audit's original count of 3 — also duplicated in the completion-probability model) into one `_classify_zone()` helper + named `GOALLINE_YARDLINE`/`REDZONE_YARDLINE` constants. Kickoff runoff now differentiates touchback (near-instant, was wrongly sharing the return's 4-7s range) from return (unchanged). Punt base runoff (touchback/fair-catch cases) `randint(4,7)` → `randint(7,10)`. FG runoff (both made and missed) now a flat 4s, replacing a made/missed asymmetry (4-6s vs 5-9s) real data doesn't support. `off_score`/`def_score` renamed to `away_score`/`home_score` throughout (see below).
  - [`src/nfl_sim/batch.py`](src/nfl_sim/batch.py), [`src/api/app.py`](src/api/app.py), [`scripts/simulation_runners/run_weeks_1_to_4_2025.py`](scripts/simulation_runners/run_weeks_1_to_4_2025.py), `run_weeks_1_to_18_2025.py`, `run_first_4_games_2025.py`, `run_full_season_sim_2025.py`, [`scripts/eda/diagnose_scoring_efficiency.py`](scripts/eda/diagnose_scoring_efficiency.py): `off_score`/`def_score` → `away_score`/`home_score` (was always away/home in this path, never real offense/defense). `src/live/` (`espn_adapter.py`, `posting_policy.py`) deliberately left alone — there the same field names correctly mean real, dynamic possession-based offense/defense, a different and legitimate concept; confirmed zero data/import crossover with the sim path before touching anything. `legacy/` also left alone (archival).
  - [`scripts/eda/analyze_kicking_timing.py`](scripts/eda/analyze_kicking_timing.py) (new): real 2021-2025 PBP EDA verifying kickoff touchback rate and kickoff/punt/FG live-play timing, same `game_clock_elapsed` (consecutive-play delta) methodology as `analyze_clock_pace_grid.py`. Output: [`docs/eda_outputs/kicking_timing/README.md`](docs/eda_outputs/kicking_timing/README.md) + 2 CSVs.
  - [`AGENTS.md`](AGENTS.md): added a resolved fragile-area note for the `off_score`/`def_score` rename (including the known residual risk below), updated the `_run_clock`-adjacent notes.
- **Key finding along the way:** the kickoff touchback rate constant (`0.2068`) turned out to already be correctly calibrated to real 2025 data (measured 576/2785 = 0.20682, matching to 4 decimals) — it just had zero citation. Real per-season rates confirm the new 2025 kickoff rule's effect is dramatic: 2021-2024 ranged 57.5%-73.0%, 2025 dropped to 20.68%.
- **Verification Performed:** Every pure-refactor change (`regular_clock`, zone-classification extraction, stale comments) verified via same-seed-twice determinism checks (bit-for-bit identical) — these are boolean-algebra-equivalent or non-functional changes, confirmed to produce zero behavioral drift. The kickoff/punt/FG timing changes and the sack-fumble/off_score work are real behavior changes, verified via smoke tests (multiple matchups, no NaNs/crashes, sane score distributions) rather than parity, as expected for intentional calibration changes. `py_compile` clean on all 8 touched Python files.
- **Current System Status:** All changes uncommitted (working tree, same as the earlier same-day entry). No known regressions. **Known residual risk (Cam is aware, planning to address shortly):** `data/interim/sim_results_2025_{games,players}.parquet` (gitignored, generated by `run_full_season_sim_2025.py`) was not regenerated by this session — if it predates this rename, it still has the old `off_score`/`def_score` column names on disk, which would break `app.py`'s `/api/week_projections` (or anything else reading that cache) until `run_full_season_sim_2025.py` is rerun.
- **Immediate Next Steps for the Next Agent:**
  1. Cam is rerunning `scripts/simulation_runners/run_full_season_sim_2025.py` shortly to refresh the now-stale parquet cache — don't duplicate that effort if picking this up soon after.
  2. A broader magic-numbers documentation pass is still open (Cam asked "what other numbers are un/underdocumented" as a live follow-up at the end of this session) — see this entry's companion investigation, not yet acted on.
  3. `game_sec`'s inverted-quarter representation (`(4-quarter)*900 + time_remaining`) was investigated and deliberately left as-is — it's a trained-model feature (Gate 1, 4th-down model, play-selection/air-yards/YAC/rush buckets all depend on this exact representation), already computed efficiently (once per step, not recomputed), and Cam confirmed the only real complaint (human readability of raw threshold checks like `game_sec <= 420`) isn't worth the change. Don't revisit without a new reason.
  4. Nothing has been committed this session — check with Cam before committing.

---

### [2026-07-21] Handoff from Claude Sonnet 5 (trench matchup gates, 9 critical bug fixes, dead-code cleanup, Gate 2b, game_engine.py optimization pass)

- **Active Task:** Multi-phase `game_engine.py` work, in order: (1) EDA-grounded trench (O-line/D-line) matchup mechanics for rushing, (2) a full code-review audit (task brief: [docs/audit/game_engine_cleanup/README.md](docs/audit/game_engine_cleanup/README.md)) covering 9 critical bugs + dead code + optimization opportunities, (3) fixing all 9 critical bugs, (4) dead/write-only code removal, (5) a pass-side trench mechanic (Gate 2b), (6) the audit's optimization-opportunities list, (7) a follow-up architecture fix to `_run_clock`. No single task brief covers the whole arc; this entry is the record.

- **Files Modified/Created (grouped by phase):**
  - **Rush trench gate:** [`scripts/eda/build_trench_dna_composites.py`](scripts/eda/build_trench_dna_composites.py) (new) — within-season z-scored run-block/run-defense composites into `data/dna/trench_dna.json`. [`scripts/model_training/measure_rush_organic_tail_rates.py`](scripts/model_training/measure_rush_organic_tail_rates.py) (new) — measures organic negative/explosive rush rates from real simulated games (gate forced to 0), used to calibrate a 3-way mode-frequency gate (`p_neg_gate`/`p_exp_gate`/`p_rescue_gate`) replacing the old flat `run_mult_*` multiplier in [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py). `data/external/pfr_adv_rushing_stats.csv`/`schedules_2015_2024.csv` extended to include 2025 (were stale at 2024).
  - **9 critical bug fixes** (all in `game_engine.py`, see [docs/audit/game_engine_cleanup/README.md](docs/audit/game_engine_cleanup/README.md) for full detail on each): OT was unreachable (`self.quarter` could never hit 5) — added `is_playoff` param, full OT state, rewrote `_run_clock`'s end-of-quarter block with correct reg-season-vs-playoff timeout/period rules; sack probability missing a `np.clip` (could exceed 1.0); kneel-down mathematically unreachable on 4th down (`max_bleed_time` always ≤0 there) — added a `concede_mask` (42s/0-timeouts rule); a would-be TD could be silently erased by a same-play fumble roll; QB scrambles were invisible to fumble risk and the big-play counter; punt returns weren't re-clipped into valid field position (kickoffs were); return-yardage stats could disagree with actual field position (negative raw values floored for the stat but not the yardline update); Gate 2 fed duplicated last-4-games features (documented, deferred to v0.4.0); pass-branch penalty-decline logic was asymmetric vs. the run branch (fixed after confirming via real PBP data that declined penalties read `penalty=0` in the structured column). Also added safety detection (was entirely missing) as a prerequisite for the OT fix.
  - **Dead-code cleanup:** removed `drives_count`, `gross_sacks`/`net_sacks` (diagnostic-only), `fg_made_away/home`, `fourth_down_goes_away/home`, `oob_plays`/`oob_eligible_plays`, several write-only locals, `get_stats_report` (dead stub), `_load_skill_dna`'s unreachable fallback — all confirmed via grep-before-cut, not visual inspection.
  - **Gate 2b (pass-side trench mechanic):** [`scripts/eda/build_trench_dna_pass_composites.py`](scripts/eda/build_trench_dna_pass_composites.py) (new) — pass-block/pass-rush composites (`pass_block_off_z`/`pass_def_z`) into `trench_dna.json`, mirroring the run-side script. [`src/nfl_sim/models/chaos_v_0_1_0/train_gate2b_trench_correction.py`](src/nfl_sim/models/chaos_v_0_1_0/train_gate2b_trench_correction.py) (new) — trains a small XGBoost correction gate (3 features: Gate 2's own calibrated probability + the two new composites) that adjusts Gate 2's sack probability without retraining Gate 2 itself. [`src/nfl_sim/models/chaos_v_0_1_0/inference.py`](src/nfl_sim/models/chaos_v_0_1_0/inference.py): loads `gate_2b_trench_correction.joblib`, adds `predict_sack_proba_corrected`. `game_engine.py`: wired in after Gate 2's existing calibration, removed the old flat `pass_mult_*` trench-tier multiplier (kept the unrelated QB-CPOE component it was bundled with).
  - **`game_engine.py` optimization pass** (all 8 items from the audit's opportunities list): added `_precompute_matchup_tables()` (per-team/per-zone CPOE/target-share/carry-share/trench-feature tables, computed once instead of every play across 5 call sites — Phase 3 play-selection, sack-gate, air-yards, YAC, run-resolution) plus module-level `_zone_split_vector`/`_zone_scalar_vector` helpers; precomputed per-receiver DNA trait dicts (`receiver_traits`) replacing live `self.dna['skill']` lookups + a `get_safe_float` closure that was being redefined every play; `receivers_cache`/`rusher_cache` now store real `np.array`s instead of converting fresh every play; fumble mechanics reworked — deleted a per-carrier DNA `fumble_rate` multiplier that was mathematically always 1.0 (the field doesn't exist in any DNA file) and replaced the mislabeled `qb_fumble_rate` (actually QB sack-rate) sack-fumble scaling with a flat, real-data rate (`SACK_FUMBLE_RATE = 0.1253`, 2020-2025 PBP, Cam's call to use raw with no dampener); `_predict_4th_down_probas_batch`'s pandas DataFrame replaced with `booster.inplace_predict` in [`src/nfl_sim/models/fourth_down_conversion_v_0_1_0/inference.py`](src/nfl_sim/models/fourth_down_conversion_v_0_1_0/inference.py) — **found and fixed a real bug in passing**: this model (and Gate 2b, and Gate 4) were trained with early stopping but `inplace_predict` wasn't passing `iteration_range`, so production silently used more trees than what was validated (max divergence 0.058 probability for Gate 4); fixed all three via a stored `iteration_range` in `ChaosModelV010.__init__`. Small items: `is_offensive` now draws only `sum(has_penalty)` randoms instead of `N`; `_sigmoid_arr`/`_logit_arr` hoisted to module level.
  - **`_run_clock` architecture** (tabled during the optimization pass, done as a follow-up): consolidated 9 disjoint per-category clock calls in Phase 7 into one combined call (`combined_seconds` array written per-category, one `_run_clock` call at the end) — cut redundant full-N overhead but revealed `_run_clock`'s internal OT-entry coinflip reordering relative to other randomness. Fixed properly: split `_run_clock` into pure runoff+2-minute-warning mechanics, and moved all quarter/OT-period-transition logic (including the coinflip) into a new `_resolve_quarter_transitions(active)` method called exactly once per step via a new `self.step_start_active` snapshot — restores full seeded determinism.
  - Also modified this session (not detailed above — see `git diff` for specifics): `src/api/app.py`, `src/nfl_sim/proe_overlay_v_0_1_0.py`, `src/nfl_sim/models/positional_ep_v_0_1_0/*`, `data/dna/coach_dna.json`, and several docs (`docs/api/local.md`, `docs/models/README.md`, `docs/sims/inputs/README.md`, `docs/study/ML_Concepts.md`).

- **Verification Performed:**
  - Rush gate: before/after bucket comparison (real team-games bucketed by matchup composite vs. real EDA targets).
  - 9 bug fixes + OT: dedicated verification round per bug plus a full-batch smoke test; the `~self.is_playoff` fancy-indexing bug (bitwise-not on a Python bool silently corrupting lane 0 every quarter boundary) was found via manual debug-print tracing and confirmed with `~True == -2` in isolation.
  - Gate 2b: training-time brier/logloss/PR-AUC + a bucket-level real-vs-predicted sack-rate table (confirms correction concentrated at matchup extremes, masked by the aggregate metric); live-engine sack rate checked post-deploy.
  - Optimization pass: items 1-3/5/6 verified via **exact seeded parity** (identical aggregate stats — pYds, rYds, sacks, fumbles, win totals — across a 3000-game batch, same seed, before/after each change). Item 4 (fumble mechanics, a real behavior change) verified via rate statistics instead (general rate unchanged as expected, new sack-fumble rate lands near 12.53%).
  - `_run_clock` consolidation: exact seeded parity failed as expected (internal coinflip reordering) — validated instead via large-N statistical A/B (pooled 40k vs. 60k games): tie-rate and OT-rate differences both consistent with sampling noise (p=0.37, p=0.21). The follow-up split restored **exact bit-for-bit determinism** (verified: same seed run twice → identical output) and was re-validated statistically against the consolidated-only baseline (p=0.35, p=0.95).
  - Wall-clock timing measured throughout: modest (~3.5%) improvement from items 1-6; `_run_clock` itself measured at only ~1% of total game time even fully consolidated, so its contribution to overall speed was never going to be large — kept for correctness/clarity, not performance.

- **Current System Status:** All changes uncommitted (working tree). No crashes, no NaNs in batch output, OT/ties resolve correctly, full determinism restored. Known, deliberately-unaddressed gaps (flagged to Cam, not fixed): general (non-sack) fumble rate is calibrated below the raw real per-play rate (~0.5% sim vs. ~1.1-1.5% real for run/completion/scramble) — untouched since it wasn't in scope and the history behind the existing `*0.80` calibration constant isn't known; overall sack rate runs ~5-15% hot vs. real depending on matchup (pre-existing, not caused by Gate 2b — confirmed via a bypass test showing Gate 2b mildly *improves* it).

- **Immediate Next Steps for the Next Agent:**
  1. Nothing currently blocked. Natural next candidates, none urgent: recalibrate the general fumble-rate base constants against real per-play rates; investigate the ~5-15% sack-rate overshoot (candidate: retrain Gate 2 itself, previously rejected mid-session to avoid disturbing its verified calibration, but may be worth revisiting given the drift found this session).
  2. Nothing has been committed yet — Cam has not asked for a commit/PR this session; check with him before committing this much surface area at once.
  3. Update `AGENTS.md` to reflect: OT/safety now implemented (was previously "unreachable" per the old audit), Gate 2b live, the trench-tier `pass_mult`/`run_mult` mechanism fully retired, and the `_run_clock`/`_resolve_quarter_transitions` split (companion task to this WORKLOG entry, in progress).

### [2026-07-15] Handoff from Claude Opus 4.8 (full repo audit — Python 3.12 upgrade, first git history, docs, dead-code cleanup)

- **Active Task:** Full-scale repo audit requested by Cam: the entire working tree (Python backend, both frontends, docs, trained models, R live bot) had never been committed to git — nothing was pushable. Landed as three merged PRs: [#1](https://github.com/txcwalker/NFLSims/pull/1) (environment + first commit of everything), [#2](https://github.com/txcwalker/NFLSims/pull/2) (module docstrings + READMEs), [#3](https://github.com/txcwalker/NFLSims/pull/3) (dead-code cleanup). No dedicated task-brief doc exists for this; this entry is the record.

- **Files Modified/Created (grouped — this touched most of the repo, first-ever commit for most of it):**
  - **Environment:** [`.gitignore`](.gitignore) rewritten (scoped `data/`, un-ignored `tests/`, fixed `venv/` mismatch, excluded legacy data/binaries, OneDrive sync-conflict dupes, runtime bot output); new [`.gitattributes`](.gitattributes) (LF normalization); new [`requirements.txt`](requirements.txt) pinned to a validated Python 3.12 set; [`README.md`](README.md) corrected (Python 3.10+ → 3.12).
  - **`venv/`:** rebuilt Python 3.8.10 → 3.12.13 (old env kept as `venv_py38_old/`, not yet deleted).
  - **`src/`** (49 files): first commit — engine, API, live bot, data pipeline.
  - **`src/nfl_sim/models/`** (103 files): first commit of all 11 trained model families; every `.joblib`/pickle re-saved under the new xgboost 3.3.0 / scikit-learn 1.9.0 (predictions verified byte-identical to pre-resave — no drift).
  - **`scripts/`, `tests/`** (51 files): first commit of tooling + test suite.
  - **`frontend/`, `frontend_analysis/`** (57 files): first commit of both React sites.
  - **`docs/`, `.agents/`, `ai_assistant_files/`** (300 files): first commit of the documentation tree.
  - **`legacy/`** (28 code files; data/binaries ~2.35GB deliberately gitignored): archived old R/Python implementation.
  - Root planning docs ([`AGENTS.md`](AGENTS.md), `GOAL_TRACKER.md`, `PROJECT_ROADMAP.md`, etc.) and `data/dna`, `data/current_rosters`, `data/external` (curated inputs): first commit.
  - **`R/`** (root): reconciled as **active** code (it's the live 4th-down bot, not deprecated — see AGENTS.md §2 correction below), merging cleanly with Cam's concurrent "Prepping Actions" commits on `main`. Retired a superseded `predict_fd.R`/`load_fd_model.R` pair to `legacy/models/fourth_down/`. Fixed [`nfl_manuel.yml`](.github/workflows/nfl_manuel.yml)'s CI step ordering.
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): removed a confirmed-dead DNA-injection block (4 lines; set attributes nothing read).
  - `legacy/models/air_yards_v_0_1_1/{train.py,evaluate.py}`, `legacy/models/yac_model_v_0_1_1/{train.py,evaluate.py}`: moved here (via `git mv`, history preserved) from the active model dirs — these are superseded single-model trainers that would silently corrupt the deployed zone-split models if run; each now has a `RETIRED` header pointing to the real `train_zone_split.py`.
  - 10 `src/` files gained module-level docstrings (`game_engine.py`, `model_registry.py`, `batch.py`, `visuals.py`, `script_chainer.py`, `api/app.py`, and 4 `data_pipeline/` files); 3 new READMEs (`tests/`, `frontend/src/`, `frontend_analysis/src/`).
  - [`AGENTS.md`](AGENTS.md) §2: corrected the stale "R deprecated" claim (root `R/` is the live bot) and the "legacy/ do not touch" claim (`batch.py` actively imports `legacy.game_engine_sequential`).

- **Verification Performed:**
  - `python -m pytest tests/test_positional_evaluator.py -v` → 18/18 passing, both before and after every change batch.
  - `python -m py_compile` on every edited engine/API file.
  - Full-season batch audit (`run_weeks_1_to_4_2025.py`) run on Python 3.8 vs. 3.12 — every tracked metric matched within ~0.4% (Monte Carlo noise, no systematic drift); completion rate matched to 0.016% specifically, ruling out silent model-version corruption.
  - Direct engine smoke test (`NFLGameEngine(...).simulate_play_step()` × 300) — reaches Q4 cleanly on the rebuilt venv.
  - R live-bot smoke test: piped a real 4th-&-2 game state through `Rscript run_one_sim.R` — loaded `.rds` models and returned a sensible GO/punt/FG recommendation end-to-end.
  - Secrets scan on every commit before push (no hardcoded credentials; bot creds are all env-sourced via `os.getenv`).

- **Current System Status:** All tests passing. PRs #1, #2, #3 merged into `main`; local repo clean and synced; feature branches deleted (local + remote). Active venv is Python 3.12.13 per `requirements.txt`. `venv_py38_old/` retained as a fallback, not yet deleted. Both frontends, the FastAPI backend, and the R live bot all verified working post-merge.

- **Immediate Next Steps for the Next Agent:**
  1. **In progress / up next (Cam's explicit request):** decide the fate of `MODEL_DEVELOPMENT_STANDARD.md` — referenced by `PROJECT_ROADMAP.md`/`GOAL_TRACKER.md`/`AGENTS.md` as the model-audit gate but does not exist in the repo. Either author it or strip the dangling references.
  2. **Function-level docstrings** (Inputs/Outputs/Purpose per Cam's global standard) across `src/` — deliberately deferred to be done **case-by-case**, not as one large pass. `optimizer.py` (10 functions, 0 documented) is a representative gap.
  3. Delete `venv_py38_old/` once Python 3.12 is fully trusted in daily use.
  4. `HistoricalLab` (frontend_analysis) is still marked "remove before launch" — not urgent, but a real pre-launch task.
  5. A code-quality/performance optimization pass on `src/` (especially `game_engine.py`) has **not** started — this audit was scoped to reorg + docs + dead-code only.

---

### [2026-07-11] Handoff from Claude Sonnet 5 (session 2 — YAC diagnosis + retrain)

- **Active Task:** `clock_physics_v020` audit, Rounds 8-9 — diagnosed and fixed the yards-per-completion shortfall (9.70 vs. real 10.93, ~−11%) that Round 6 had only ever diagnosed as secondary to completion rate, never actually fixed. Picked up cold in a fresh context window from the prior session's handoff below.

- **Files Modified/Created:**
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): added additive diagnostic instrumentation (`last_play_pre_yardline_100`, `last_play_is_complete_pass`, `last_play_yac`, `last_play_gain`, a `pre_yardline_100` snapshot at the top of `simulate_play_step`). Added `room_after_catch` as a 9th YAC-model feature. Fixed the post-model noise-injection scale (`base_scale = max(0.75, yac * 1.25)` instead of a flat `6.0`) that was mechanically inflating YAC wherever the real mean is small (goalline worst).
  - [`src/nfl_sim/models/yac_model_v_0_1_1/train_zone_split.py`](src/nfl_sim/models/yac_model_v_0_1_1/train_zone_split.py) (new): the actual, working zone-split YAC training script — live `nfl_data_py` pull (2020-2025) + `data/dna/*.json` joins, `GroupShuffleSplit` by `game_id` for 70/15/15 train/val/test, `reg:squarederror` objective (mean-targeting, see below). Produces `{primary,redzone,goalline}_yac_reg.joblib` + `metadata.json` directly in the folder `model_registry.py` already loads from.
  - [`scripts/eda/analyze_pass_yardage_breakdown.py`](scripts/eda/analyze_pass_yardage_breakdown.py) (new): real-vs-sim diagnostic comparing air yards/YAC/total gain by zone and by depth bucket. Drives `NFLGameEngine` directly (not `BatchSimulator`) to read the new per-play hook fields.
  - [`docs/audit/clock_physics_v020/README.md`](docs/audit/clock_physics_v020/README.md): Round 8 (diagnosis) and Round 9 (fix) written up in full.
  - [`AGENTS.md`](AGENTS.md): §0 priorities, §11.7, and several fragile-area notes updated to reflect the fix and the new open items.

- **Key findings, in order of discovery:**
  1. Air yards were already well-calibrated; the shortfall was entirely in YAC (yards after catch) — worst on deep balls and screens.
  2. Redzone/goalline had an *opposite-direction* problem (over-prediction, masked by a downstream clip) — turned out to be two separate bugs, both eventually fixed.
  3. Investigated retraining and found `yac_model_v_0_1_1/train.py` (and `air_yards_v_0_1_1/train.py`, and `rush_yards_v_0_1_0` with no script at all) don't reproduce what's actually deployed — a zone-split "V.0.2.0" architecture that superseded an earlier single-model generation without ever committing the new training code. Traced this to a much bigger fact: `git ls-files src/nfl_sim/models/` returns **zero tracked files, ever** — the entire models directory (and `data/`, gitignored outright) has never been in git. Flagged for Cam to decide whether that's intentional (OneDrive-sync-only for these directories).
  4. Root cause #1 (the big one): the model was trained with `reg:absoluteerror` (median-targeting) instead of `reg:squarederror` (mean-targeting). YAC is right-skewed, so median-fit systematically undershoots the mean that season-long aggregates depend on. This one fix closed most of the gap (primary zone: −30.9% → −0.9% on held-out test data).
  5. Root cause #2 (surfaced only after #1 was fixed): a flat noise-scale floor (6.0 yards) in `game_engine.py`'s post-model noise injection, combined with flooring negative draws at zero, mechanically inflated the mean wherever real YAC is small (goalline hit hardest). Fixed by scaling noise to the play's own predicted YAC instead.
  6. Added `room_after_catch` (yardline_100 − air_yards) as an experimental feature per Cam's direction (give the model yardline + air yards, let it learn the taper) — confirmed high feature importance, kept.
  7. Deliberately left out player-skill traits (elusiveness, broken_tackle_rate, top_speed_mph, avg_separation_yds) this round on Cam's call — checked NGS data coverage first and found `avg_separation_yds` has decent real coverage back to 2016 (better than Cam remembered) while `top_speed_mph` isn't in the standard NGS dataset at all (likely genuinely sparse) — situational-only features for now, `avg_separation_yds` flagged as worth a future incremental add.

- **Verification Performed:** `py_compile` + `pytest tests/test_positional_evaluator.py` (18/18) after every `game_engine.py` edit. Re-ran `analyze_pass_yardage_breakdown.py` after the model retrain AND again after the noise-scale fix to confirm each fix's effect in isolation. Full batch audit (`run_weeks_1_to_4_2025.py`, 64 games × 1000 iterations) run before/after to measure season-level cascading effects.

- **Current System Status:** All 18 tests passing. YAC: −14.4% → −1.6% overall (essentially exact in primary/deep/screen/standard; goalline improved 5x, +241%→+49.9%, small residual on a ~4%-of-completions, tiny-absolute-yardage zone). Pass yards/game: −13.0% → −6.2% (more than halved). Completion rate unaffected as expected (64.20% vs. real 64.40%). **Offensive/total snap-count overshoot did NOT improve** (+6.8%→+7.2% offensive, +6.2%→+6.6% total) — the hypothesis that fixing YAC would also fix snap-count is now confirmed only half right. Sacks unchanged (−29.1%, still deferred to v0.4.0).

- **Immediate Next Steps for the Next Agent:**
  1. **Snap-count overshoot is the next open thread**, now fully decoupled from passing accuracy. Round 5's squeeze-play theory (plays-per-drive running hot, not drive count) is the leading suspect again — the 50% second-squeeze probability is Cam's explicit spec, don't change without asking.
  2. **air_yards is now the largest remaining piece of the pass-yards gap** (−4.4% to −7.3% depending on zone/depth). `air_yards_v_0_1_1` has the identical orphaned-script problem YAC had; the same fix recipe (live pull + DNA joins, `reg:squarederror`, grouped split) would likely apply directly — `yac_model_v_0_1_1/train_zone_split.py` is a ready-made template.
  3. Goalline YAC's remaining +49.9% over-prediction is a candidate for a future look, low priority (small volume, tiny absolute yardage).
  4. **Ask Cam directly about the git-tracking gap** for `src/nfl_sim/models/` and `data/` before assuming it's fine to leave as-is — this wasn't addressed this session beyond flagging it.
  5. Re-run `analyze_pass_yardage_breakdown.py` after any future YAC/air-yards change to confirm nothing regressed.

---

### [2026-07-11] Handoff from Claude Sonnet 5 (session 1)

- **Active Task:** `clock_physics_v020` audit — engine clock-physics rebuild + completion-rate model fixes. Full round-by-round history: [docs/audit/clock_physics_v020/README.md](docs/audit/clock_physics_v020/README.md) (7 rounds). Session ending on context-window limit, not task completion — this is a mid-thread handoff.

- **Files Modified:**
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): the vast majority of this session's work. Fixed a long-standing bug where the game never simulated a 4th quarter (`_run_clock`'s `end_game` check re-read `self.quarter` after it had already been mutated for Q3→Q4 transitions). Rebuilt the clock/pace model (empirical bootstrap pools replacing a flat `randint(18,30)`), added an explicit out-of-bounds mechanism, an automatic two-minute-warning stoppage, and a "squeeze play" mechanic (teams rushing a snap to fit an extra play in before the warning, per Cam's explicit probabilities). Fixed several clock-stop bugs (scoring plays, turnover-on-downs, punts, field goals weren't stopping the clock). Recalibrated the screen catch rate (was hardcoded 80/95% RB/WR-TE, real data showed 82.68/80.01%) and fixed QB CPOE being applied in logit space instead of probability space (was compressing QB skill differentiation to ~1/3 of intended effect). Added a probability-space baseline calibration offset to hit the confirmed real completion-rate target (64.40%). New persistent counters: `oob_plays`, `drives_count` (diagnostic), `two_minute_warning_used`, `squeeze_plays_used`.
  - [`src/nfl_sim/model_registry.py`](src/nfl_sim/model_registry.py): wired in the new `clock_pace_v_0_1_0` model.
  - `src/nfl_sim/models/clock_pace_v_0_1_0/` (new): `inference.py` (ClockPaceModelV010), `pace_pools.json` + `metadata.json` (generated, empirical runoff pools by quarter/time-window x score-margin tier).
  - [`scripts/eda/analyze_clock_pace_grid.py`](scripts/eda/analyze_clock_pace_grid.py) (new): builds the pace grid/pools from real 2021-2025 pbp data.
  - [`scripts/print_play_by_play_v020.py`](scripts/print_play_by_play_v020.py) (new): non-interactive single-game play-by-play dump for manual clock verification (found the Q4 bug via this).
  - `scripts/eda/analyze_plays_per_game.py` (new): league-wide plays/game EDA, triggered by a Bills-specific observation (turned out to be their defense forcing short drives, not their offense).
  - [`scripts/simulation_runners/run_weeks_1_to_4_2025.py`](scripts/simulation_runners/run_weeks_1_to_4_2025.py) and [`scripts/eda/run_historical_eda.py`](scripts/eda/run_historical_eda.py): extended with new tracked metrics (snap counts by category, completion rate) across the session.
  - [`AGENTS.md`](AGENTS.md): updated this session with current priorities, new fragile-areas entries, updated test commands and repo layout.

- **Verification Performed:** `python -m py_compile src/nfl_sim/game_engine.py` and `python -m pytest tests/test_positional_evaluator.py -v` (18/18 passing) after every sub-change. Full batch audit (`run_weeks_1_to_4_2025.py`, 64 games x 1000 iterations) rerun ~10 times across the session to measure each change against real 2021-2025 data.

- **Current System Status:** All 18 tests passing. Engine runs cleanly end-to-end. Completion rate is essentially exact (64.15% vs. real 64.40% target). Offensive/total snap count is currently running **+6.8%/+6.2% over real** — this got worse (not better) after the Round 7 completion-rate fixes and is the main open thread (see below). Sacks remain ~30% under real (deferred, not a bug in this session's work).

- **Immediate Next Steps for the Next Agent:**
  1. **Do not start a new investigation without reading [docs/audit/clock_physics_v020/README.md](docs/audit/clock_physics_v020/README.md) first** — several plausible-sounding hypotheses were already tested and ruled out this session (yards/big-play rate, drive count, OOB reduction magnitude — see §11.5 of AGENTS.md for the list). Re-litigating these wastes a turn.
  2. **Open decision point:** the play-count overshoot (+6.8% offensive snaps) most likely traces back to the squeeze-play mechanic (Round 5 finding: drive count matches real almost exactly, but plays-per-drive runs hot — squeeze plays add a down within a drive without ending it). The 50% second-squeeze probability (trailing 2+ scores) was Cam's explicit, deliberate spec — do not change it unilaterally; ask him directly whether to dial it back, now that the completion-rate fix has shifted the baseline again.
  3. **Deferred to v0.4.0** (has Cam's scope agreement, not yet started): sacks recalibration (add trench-tier OL/DL matchup to chaos gate 2, retrain) bundled with injury stoppages and other administrative clock stoppages (replay review, etc.).
  4. **Deferred, parameters already agreed:** throwaway mechanism rework — target ~5% of dropbacks, 75% pressure-related / 25% clean-pocket. Current mechanism only fires as a 20% escape from would-be-sacks; needs a clean-pocket path independent of `sack_prob`.
  5. Re-run the full batch audit after any of the above to keep `docs/audit/clock_physics_v020/README.md`'s round-by-round numbers current.

---

### [2026-06-23] Handoff from Claude Sonnet 4.6 (session 3)

- **Active Task:** HistoricalLab score + lines framing fixes; EFSD design discussion
- **Files Modified:**
  - [`src/nfl_sim/week1_2025.py`](src/nfl_sim/week1_2025.py): Replaced `home_score`/`away_score` (which nfl_data_py stores as FINAL game score on every row) with `posteam_score`/`defteam_score` (running score at each play). Added both to `REQUIRED_COLS` and per-play row dict.
  - [`frontend_analysis/src/pages/HistoricalLab.jsx`](frontend_analysis/src/pages/HistoricalLab.jsx):
    - **Score fix:** Derive running home/away score from `posteam_score`/`defteam_score` + possession. `homeScoreNow = isHomePoss ? posteam_score : defteam_score`. Scoreboard now shows score at the selected moment in the game, not the final score.
    - **Lines framing fix:** `LinesPanel` now converts offense-reference `delta_kep` to home-team reference (`delta_home = isHomePoss ? delta_kep : -delta_kep`) and re-sorts: home possession → descending (optimizing for +), away possession → ascending (optimizing for −). BEST badge goes on the play that most benefits the possessing team in home-KEP terms.
    - Added possession context header: "PHI possesses — optimizing for +" / "DAL possesses — optimizing for −" with team color swatch.
    - BEST badge now uses the possessing team's color instead of generic cyan.
    - KEP trajectory in lines panel now labeled "home KEP" with team-colored values.
    - Tooltip score now derived from `posteam_score`/`defteam_score` correctly.
  - [`frontend_analysis/src/pages/HistoricalLab.jsx`](frontend_analysis/src/pages/HistoricalLab.jsx): Added `frontend_analysis/.claude/launch.json` for preview server (port 5174).

- **Root cause of "No data" issue:** Race condition — frontend loads before backend finishes warming up (~10s to load PBP + models). Silent `.catch(() => {})` hides the error. Fix: hard-refresh browser after backend is ready. Long-term fix: add retry or error state with a "Reload" button.

- **Verified working:** Q1 first play shows DAL 0 – PHI 0. Q2 0:27 with DAL possession shows correct running score + lines optimizing for − (away team). BEST badge in Cowboys navy on the play that most pushes home KEP negative.

- **Immediate Next Steps:**
  1. Train EFSD model (see design session entry below)
  2. Write implementation plan (Gemini handoff)
  3. Fix silent fetch failure UX in HistoricalLab (add error state + retry button)
  4. Full site walkthrough when ready

---

### [2026-06-23] Design session — KEP alternatives and EFSD

- **Topic:** Conceptual discussion on KEP metric definition and alternatives
- **Key decision:** EFSD (Expected Final Score Differential) is the preferred next iteration of KEP

**Problem with current KEP:**
Current KEP inverts the WP model to find "what kickoff margin yields this same WP?" — it's a nonlinear rescaling of WP, not an independent measure. WP defines WP. Circular.

**EFSD approach:**
- Training: pull historical PBP, join each play to its game's final score margin (home - away), train XGBoost regressor to predict that margin from game state features (same features as WP model).
- Output IS the metric — no inversion step, no WP in the chain.
- Scale: naturally continuous (regression outputs weighted averages across many outcomes), not discrete even though NFL margins cluster around 3/7/10.
- Discretization risk: NFL margin distribution is lumpy (3/7/10/14 are overrepresented). Fix if needed: apply Gaussian smoothing (σ ≈ 1.5) to target during training. Build first, check chart smoothness, add smoothing only if prediction surface looks stepped.

**Key property: time-invariant bands**
EFSD already encodes clock into its predictions (clock is a feature). An EFSD of +7 at halftime and +7 with 30 seconds left represent genuinely equivalent expected outcomes — the model gives different scores to each game state, so same output = same expected destination.

**Preliminary band hypothesis (to validate empirically):**
| Band | EFSD | Football meaning |
|---|---|---|
| Even | ±3 | Within a field goal, anyone's game |
| Advantage | ±3 to ±8 | One possession edge, not safe |
| Clear advantage | ±8 to ±16 | Two possessions, opponent needs multiple things |
| Decisive | ±16+ | Three+ possessions, very hard to recover |

Validation method: on holdout set, bucket EFSD predictions and check actual win rates per band. Target: Even→~50%, Advantage→60-70%, Clear→75-85%, Decisive→90%+. Adjust band boundaries until calibrated.

**What EFSD does better than current KEP:**
- No artificial ±24 ceiling (a team up 35-0 late can show EFSD +32, not clamped)
- More intuitive at end of game ("expected to win by 9" vs "equivalent to +18 at kickoff")
- Independent of WP model — anchors to scoring outcomes directly

**What to watch for:**
- EFSD values will NOT map linearly to current score differential (e.g., up 10 at halftime → EFSD ≈ +5 due to regression to mean). This will surprise users at first. Display current score alongside EFSD prominently.
- "Feels different" intuition (halftime vs 30 seconds) is path anxiety, not positional difference. If EFSD is the same, the situations are equivalent — correct behavior, not a bug.

**Status:** Design agreed. Implementation deferred until current Historical Lab is further validated. Next step when ready: write `train_efsd.py` modeled on `train_positional_ep.py`, swap target variable, validate bands on holdout set.

---

### [2026-06-23] Handoff from Claude Sonnet 4.6 (session 2)

- **Active Task:** HistoricalLab polish — team colors, score display, chart click reliability
- **Files Modified:**
  - [`frontend_analysis/src/pages/HistoricalLab.jsx`](frontend_analysis/src/pages/HistoricalLab.jsx):
    - Added `TEAM_COLORS` lookup table (all 32 teams, primary colors) and `teamColor(abbr)` helper
    - Eval bar now uses team-specific colors for fill gradients, divider dot, and KEP value (no more generic cyan/orange)
    - Added live scoreboard block inside `EvalBar` — shows `awayScore – homeScore` with the leading score in its team color; updates with selected play
    - X-axis label corrected to "← Game Progression →" (was incorrectly showing team names)
    - Y-axis label updated to "↑ {homeTeam}  KEP  {awayTeam} ↓" to convey which direction favors which team
    - Chart tooltip now shows running score at hover point ("DAL 7 – PHI 14") above situation line, using team colors
    - Chart dots: every play now shows a small persistent dot (r=2.5, translucent) — selected play shows large solid dot (r=6). Each dot has a direct `onClick` handler on the SVG circle, bypassing Recharts synthetic event limitations.
    - Added `useRef(topRef)` + `scrollIntoView({ behavior: 'smooth' })` in `selectPlay()` — clicking a chart dot scrolls the eval bar + lines panel into view.
  - [`src/nfl_sim/week1_2025.py`](src/nfl_sim/week1_2025.py): Added `home_score` and `away_score` to `REQUIRED_COLS` and per-play row dict so running score is available at every play.

- **Backend restart:** PID 7564 → new process after Python change (cleared PBP cache, reloaded with score fields).

- **Open UX issue (not resolved):** Chart point clicks — user still not seeing the expected behavior (score at clicked play vs. end-of-game score). Root cause not fully identified this session. Dot onClick handlers are wired; `activeDot onClick` and `LineChart onClick` both call `selectPlay`. Possible remaining causes: (a) dots are small and hard to hit precisely, (b) Recharts `dot` render function creates new elements on every render which may interfere with event binding. Consider replacing chart click with a scrollable play list below the chart as a more reliable interaction model.

- **Immediate Next Steps for the Next Agent:**
  1. Resolve chart click UX — verify whether dot onClick is actually firing (add temporary console.log). If not, replace with a play list below the chart (each row is a clickable play, shows clock + situation + KEP).
  2. Full UI review walkthrough with Cam (both sites, all tabs).
  3. Remove `HistoricalLab` from nav/routing before production launch.

---

### [2026-06-23] Handoff from Claude Sonnet 4.6 (session 1)

- **Active Task:** Historical Testing Lab — chess evaluator over Week 1 2025 NFL games
- **Files Modified / Created:**
  - [`src/nfl_sim/nfl_positional_evaluator.py`](src/nfl_sim/nfl_positional_evaluator.py): Added `evaluate_one_step()` (simulates exactly ONE scrimmage play per lane, computes KEP of resulting state via three-case logic: mid-drive → direct WP→KEP, scored/terminal → `_drive_end_kep`, turnover → opponent KEP from turnover yardline) and `suggest_lines()` (chains `evaluate_one_step` calls using `mean_next_state` to build depth-2 principal-variation concept chains).
  - [`src/nfl_sim/week1_2025.py`](src/nfl_sim/week1_2025.py): New file (directly in `src/nfl_sim/`, NOT a subpackage — see fragile notes). Loads Week 1 2025 schedule and PBP via `nfl_data_py`. `get_game_list()` returns 16 games. `get_game_plays(game_id, evaluator)` returns scrimmage plays with `home_kep` (home-team reference) and `ep` pre-computed per play.
  - [`src/api/app.py`](src/api/app.py): Added `_WEEK1_MODULE` lazy singleton and three new endpoints: `GET /api/historical/week1-2025`, `GET /api/historical/plays/{game_id}`, `GET /api/historical/suggest-lines`.
  - [`frontend_analysis/src/pages/HistoricalLab.jsx`](frontend_analysis/src/pages/HistoricalLab.jsx): New page. Lichess-style layout: `EvalBar` (horizontal split, home=positive/cyan, away=negative/orange, proportional fill, divider dot), `LinesPanel` (top-3 concept chains with BEST badge and KEP trajectory), `KEPTooltip`, and full-game KEP timeline (Recharts LineChart, home_kep solid cyan, EP dashed orange, zero reference line, click to select play and fetch lines).
  - [`frontend_analysis/src/pagesConfig.js`](frontend_analysis/src/pagesConfig.js): Added `historical-lab` page entry (showInNavbar: true, "⚗ Testing Lab" label).
  - [`frontend_analysis/src/App.jsx`](frontend_analysis/src/App.jsx): Imported `HistoricalLab`, added `case 'historical-lab'` to render switch.

- **Verification Performed:**
  - DAL @ PHI (20-24): 128 plays loaded, PHI home_kep climbs to +24 at final kneel — chart shows steady PHI advantage after halftime, correct.
  - BAL @ BUF (40-41): BUF (home, positive) chart shows deep dip when BAL had momentum, then late surge to +24. 41-40 thriller clearly visible.
  - Suggested lines computed and displayed on play selection. Three concept chains rendered with BEST badge, KEP trajectory, and delta_kep deltas.
  - Eval bar animates correctly between plays (home positive = cyan fills right, away negative = orange fills left).
  - Nav link "⚗ Testing Lab" visible in header, routes correctly.

- **Key Design Decisions:**
  - **One-step KEP (not drive KEP):** Chosen because drive KEP's +17 for a 4th-down conversion was counterintuitive. One-step shows the immediate value of a play call, not the full drive. Analogous to chess showing the immediate move value, not the endgame.
  - **Home-team KEP reference:** `home_kep = kep_off if posteam == home_team else -kep_off`. Positive = home winning. Sign never flips on possession change — no sine waves. This is the only correct framing for a game-wide timeline chart.
  - **`receive_2h_ko` defaulted to 0.0:** Field not present in nfl_data_py; acceptable approximation for testing.

- **Current System Status:** Backend PID from prior session; verify still running before testing. All 18 pytest tests unaffected (no changes to tested code paths). Historical Lab page fully functional end-to-end with live backend.

- **Immediate Next Steps for the Next Agent:**
  1. Full UI review walkthrough with Cam (both sites, all tabs).
  2. Remove `HistoricalLab` from nav/routing before production launch (`pagesConfig.js` + `App.jsx`).
  3. Consider performance optimization for `get_game_plays` — currently re-computes KEP for all plays on every request (fine for testing, slow for scale).
  4. Consider depth-3 lines once depth-2 is validated as correct.

---

### [2026-06-22] Handoff from Claude Sonnet 4.6

- **Active Task:** Chess Evaluator — UI wiring, concept recommendations, multi-scenario preview
- **Files Modified:**
  - [`frontend_analysis/vite.config.js`](frontend_analysis/vite.config.js): Added `/api` proxy to `http://127.0.0.1:8000`. Previously missing, which caused concept recommendation fetches to fail in the preview browser and created a hardcoded-URL dependency. All frontend JS now uses relative `/api/` paths.
  - [`frontend_analysis/src/api.js`](frontend_analysis/src/api.js): Changed `API_BASE` from `'http://localhost:8000/api'` to `'/api'` to respect the vite proxy. Added two new mock games: `live_game_4` (MIA@BUF blowout, BUF +21) and `live_game_5` (LAC@KC 10-14pt game) with full `MOCK_PLAY_BY_PLAY`, `MOCK_GAME_STATS`, `MOCK_FOURTH_DOWNS`, and `MOCK_CHESS_EVALUATOR` entries.
  - [`frontend_analysis/src/pages/GameSummary.jsx`](frontend_analysis/src/pages/GameSummary.jsx): Three improvements: (1) Added `conceptResult`/`conceptLoading` state and a `useEffect` that fires `GET /api/positional-evaluator` when a chess play is selected. (2) Fixed `selectedEval` fallback to use last evaluation when `selectedPlayId` is null. (3) Fixed concept useEffect to use `?? evals[evals.length-1]` when `selectedPlayId` is null (race condition: tab opened before async data load completes). Added "PLAY CONCEPT RECOMMENDATION" ranked bar section to chess play detail panel.
  - [`frontend/src/pages/InDevelopment.jsx`](frontend/src/pages/InDevelopment.jsx): Removed all chess code (was dead code — no `id: 'chess'` page in pagesConfig.js). Cleaned up imports and state vars.
  - [`frontend/vite.config.js`](frontend/vite.config.js): Added `port: 5173, strictPort: true` to pin DFS site to correct port.

- **Verification Performed:**
  - SF@LAR (live_game_2): Chess tab shows KEP +5.44, concept recommendation loads live — Medium (39 sims) BEST at +6.063 delta_kep; all concepts positive (dominant position, flexibility doesn't matter much).
  - BUF@MIA (live_game_4): KEP ceiling +24.00; all five concepts show delta_kep = 0.000 — correct behavior, game is decided, no call matters.
  - Backend confirmed running: `GET /api/positional-evaluator` returns correct JSON from both `curl` and in-browser `fetch`.
  - Root cause of "Start the backend to see..." bug identified and fixed: `selectedPlayId` was null at tab-switch time due to async load race, so `ev` lookup returned undefined and early-returned before the fetch.

- **Decisions / Design Notes:**
  - KEP framing open question: should the chart show **play-start KEP** (current impl — KEP of the game state when the play begins) or **drive KEP** (the KEP of the full drive that started with this play)? See conceptual discussion in WORKLOG entry below for pros/cons.
  - Future idea logged: show **top "lines"** from sims like chess engines do — the chain of play concepts that produced the best KEP trajectory.

- **Current System Status:** All 18 tests still green (not re-run this session; no Python changes). Five mock games visible on analytics home. Chess evaluator + concept recommendation working end-to-end. Backend must be running (`python -m uvicorn src.api.app:app --host 0.0.0.0 --port 8000`) for live data.

- **Immediate Next Steps for the Next Agent:**
  1. Decide play-KEP vs. drive-KEP framing (see Cam's open question in this entry).
  2. Implement "top lines" display — show the sequence of drive concepts with best simulated KEP from each evaluation.
  3. Full UI review walkthrough when Cam is ready.
  4. Consider making the KEP chart clickable (currently Recharts dot clicks don't update state because native DOM events bypass React synthetic handlers — use a list of plays below the chart as the click target instead).

---

### [2026-06-21] Handoff from Claude Sonnet 4.6

- **Active Task:** Chess-Style Positional Evaluator — V1 implementation (Phase 1–5 complete)
- **Files Modified:**
  - [`src/nfl_sim/game_engine.py`](src/nfl_sim/game_engine.py): Added 6 `last_play_*` instance attributes and snapshot writes at end of `simulate_play_step()` to expose first-play concept classification to the evaluator.
  - [`src/nfl_sim/nfl_positional_evaluator.py`](src/nfl_sim/nfl_positional_evaluator.py): New file. Contains `KICKOFF_REFERENCE` constant, `KEPConverter` (WP-inversion via isotonic regression), and `PositionalEvaluator` (drive-rollout harness with snapshot-and-freeze lane isolation; returns `ep_start`, `kep_start`, per-concept `delta_kep`).
  - [`src/nfl_sim/models/positional_ep_v_0_1_0/train_positional_ep.py`](src/nfl_sim/models/positional_ep_v_0_1_0/train_positional_ep.py): XGBoost EP regression training script (val RMSE 0.3675, test RMSE 0.3665).
  - [`src/nfl_sim/models/positional_ep_v_0_1_0/positional_ep_inference.py`](src/nfl_sim/models/positional_ep_v_0_1_0/positional_ep_inference.py): `PositionalEPModelV010` inference wrapper with `predict_expected_points()` and `predict_batch()`.
  - [`src/nfl_sim/models/positional_ep_v_0_1_0/positional_ep_model.json`](src/nfl_sim/models/positional_ep_v_0_1_0/positional_ep_model.json): Trained XGBoost artifact (531KB).
  - [`src/nfl_sim/models/positional_ep_v_0_1_0/metadata.json`](src/nfl_sim/models/positional_ep_v_0_1_0/metadata.json): Version, features, target, and training metrics.
  - [`src/live/espn_adapter.py`](src/live/espn_adapter.py): Added `parse_plays_to_states()` — sibling of `parse_plays_to_fd_rows` that keeps all scrimmage plays (downs 1–4) for the play-stream endpoint.
  - [`src/api/app.py`](src/api/app.py): Added `_POSITIONAL_EVALUATOR` lazy singleton, `get_positional_evaluator()`, `DEFAULT_OFF_TEAM/DEFAULT_DEF_TEAM` constants; `GET /api/positional-evaluator` (slider tool) and `GET /api/games/{game_id}/positional-eval` (play-stream) endpoints; updated CORS to include both frontend ports (5173 + 5174).
  - [`frontend/src/pages/InDevelopment.jsx`](frontend/src/pages/InDevelopment.jsx): Chess section now wired live — sliders (down, distance, field pos, clock, score margin, timeouts) debounce-fetch `GET /api/positional-evaluator` on port 8000; renders real EP/KEP cards and per-concept `delta_kep` ranked bars with BEST badge.
  - [`frontend_analysis/src/api.js`](frontend_analysis/src/api.js): `MOCK_CHESS_EVALUATOR` replaced with per-play `evaluations[]` format matching real API; `getChessEvaluator` now calls `/api/games/{gameId}/positional-eval`.
  - [`frontend_analysis/src/pages/GameSummary.jsx`](frontend_analysis/src/pages/GameSummary.jsx): Chess tab redesigned — left panel shows KEP + EP dual-line Recharts timeline (clickable points select play); right panel shows selected play's positional detail with natural-language KEP interpretation.
  - [`tests/test_positional_evaluator.py`](tests/test_positional_evaluator.py): 18 unit tests across `PositionalEPModelV010`, `KEPConverter`, and `PositionalEvaluator` (monotonicity, range, drive-end rate, concept finiteness, clock-aware KEP ordering).

- **Verification Performed:**
  - `python -m pytest tests/test_positional_evaluator.py -v` → **18/18 passed** (8.1s)
  - Live API validated in prior session: slider endpoint returns correct EP/KEP/concepts; EP rises from 1.09 (midfield) to 5.61 (goal line); bad game_id → 502; live ESPN fetch verified via synthetic payload (Q4 late-game KEP = +14.99).

- **Current System Status:** All 18 tests green. V1 feature complete across all 5 phases. Frontend requires live backend on port 8000 to show live data (degrades gracefully to mock/error banner otherwise). No live NFL games currently so play-stream endpoint untestable end-to-end until season.

- **Immediate Next Steps for the Next Agent:**
  1. **Full UI review** — Cam wants to do a complete walkthrough once all parts are assembled. Load the strategy site (port 5174) and DFS site (port 5173) with the API running and verify the chess slider + GameSummary chess tab render correctly.
  2. **KEP absolute magnitude calibration** — noted as future work. Relative concept ranking is stable but absolute KEP values shift with clock calibration quirks in the WP model at t=3600. Address once the V1 full review is done.
  3. **AGENTS.md update** — the new files (`nfl_positional_evaluator.py`, `positional_ep_v_0_1_0/`, test file, two new API endpoints) should be added to the active-files section of AGENTS.md.
  4. **Season readiness** — when NFL season starts, smoke-test `GET /api/games/{live_game_id}/positional-eval` against a real ESPN PBP feed end-to-end.
  5. **CORS note** — default `ALLOWED_ORIGINS` in `app.py` now includes 5173 and 5174. If either frontend moves ports, update or set `ALLOWED_ORIGINS` env var.
