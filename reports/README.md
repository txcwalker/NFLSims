# Reports

Active simulation summaries and reports are stored and tracked in the `docs/reports/` directory.

The root-level `reports/` directory was audited and cleaned up as part of the project-wide audit. Stale local CSV outputs and Monte Carlo logs have been removed to maintain a clean codebase.
